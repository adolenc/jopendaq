// This file is autogenerated by tools/generate_high_level_bindings.py. Do not edit.

package org.opendaq;

import java.lang.foreign.Arena;
import java.lang.foreign.MemorySegment;
import java.lang.foreign.ValueLayout;
import java.lang.ref.Reference;
import java.util.List;
import java.util.Map;

import org.opendaq.lowlevel.*;

/**
 * Rule that defines how a signal value is calculated from an implicit initialization value when the rule type is not `Explicit`.
 * Data rule objects implement the Struct methods internally and are Core type `ctStruct`.
 * &#64;subsection data_rule_explicit Explicit rule
 * When the rule type of the Data rule is set to `Explicit`, the values passed through the signal path, described by
 * the Value descriptor are stored in packet buffers.
 * The Explicit rule can have 2 optional parameters:
 * - `minExpectedDelta`: Specifies the minimum difference in value between two subsequent samples
 * - `maxExpectedDelta`: Specifies the maximum difference in value between two subsequent samples
 * These are mostly used for domain signals to specify the expected rate of a signal, or the expected timeout of a signal.
 * The delta parameters should be configured to match the deltas in terms of the raw signal values (before scaling/resolution
 * are applied).
 * An explicit rule must have either both or none of these parameters. To use only one, the other must be set to 0.
 * &#64;subsection data_rule_implicit Implicit rule
 * When the rule type of the Data rule is not `Explicit`, the buffers of packets are empty. The values must instead be
 * calculated via the Implicit value found in the packet buffers in conjunction with the parameters of the rule. Each
 * implicit rule type specifies its own required set of parameters.
 * &#64;subsubsection data_rule_linear Linear rule
 * The parameters include a `delta` and `start` integer members. The values are calculated according to the following
 * equation: &lt;em&gt;packetOffset + sampleIndex * delta + start&lt;/em&gt;.
 * &#64;subsubsection data_rule_linear Constant rule
 * The parameters contain a `constant` number member. The value described by the constant rule is always equal to the
 * constant.
 *
 * &lt;p&gt;Wraps the openDAQ {&#64;code daqDataRule} interface.
 */
public class DataRule extends BaseObject {

    /** Adopt an owned reference (see {@link DaqObject#DaqObject}). */
    public DataRule(MemorySegment pointer) {
        super(pointer);
    }

    /**
     * Creates a DataRule with an Explicit rule type configuration and parameters.
     * &#64;param ruleType .
     * &#64;param parameters .
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqDataRule_createDataRule()}.
     */
    public DataRule(DataRuleType ruleType, java.util.Map<?, ?> parameters) {
        super(create$createDataRule(ruleType, parameters));
    }

    private static MemorySegment create$createDataRule(DataRuleType ruleType, java.util.Map<?, ?> parameters) {
        MemorySegment parameters$d = (parameters == null ? MemorySegment.NULL : Box.boxDictOwned(parameters));
        try {
            return DaqDataRule.createDataRule(ruleType, parameters$d);
        } finally { Ffi.releaseRef(parameters$d); }
    }

    /**
     * Creates a DataRulePtr from Builder.
     * &#64;param builder DataRule Builder
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqDataRule_createDataRuleFromBuilder()}.
     */
    public DataRule(DataRuleBuilder builder) {
        super(create$createDataRuleFromBuilder(builder));
    }

    private static MemorySegment create$createDataRuleFromBuilder(DataRuleBuilder builder) {
        try {
            return DaqDataRule.createDataRuleFromBuilder((builder == null ? MemorySegment.NULL : builder.requireLivePointer()));
        } finally {
            Reference.reachabilityFence(builder);
        }
    }

    /**
     * Creates a DataRule with a Constant rule type configuration.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqDataRule_createConstantDataRule()}.
     */
    public static DataRule createConstantDataRule() {
        return DaqObject.wrap(create$createConstantDataRule(), DataRule.class);
    }

    private static MemorySegment create$createConstantDataRule() {
        return DaqDataRule.createConstantDataRule();
    }

    /**
     * Creates a DataRule with an Explicit rule type configuration and no parameters.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqDataRule_createExplicitDataRule()}.
     */
    public static DataRule createExplicitDataRule() {
        return DaqObject.wrap(create$createExplicitDataRule(), DataRule.class);
    }

    private static MemorySegment create$createExplicitDataRule() {
        return DaqDataRule.createExplicitDataRule();
    }

    /**
     * Creates a DataRule with an Explicit rule type configuration two optional parameters.
     * &#64;param minExpectedDelta The lowest expected distance between two samples.
     * &#64;param maxExpectedDelta The highest expected distance between two samples.
     * Most often used for domain signals to specify estimates on how close together/far apart two
     * subsequent samples might be.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqDataRule_createExplicitDomainDataRule()}.
     */
    public static DataRule createExplicitDomainDataRule(Object minExpectedDelta, Object maxExpectedDelta) {
        return DaqObject.wrap(create$createExplicitDomainDataRule(minExpectedDelta, maxExpectedDelta), DataRule.class);
    }

    private static MemorySegment create$createExplicitDomainDataRule(Object minExpectedDelta, Object maxExpectedDelta) {
        MemorySegment minExpectedDelta$n = Box.boxNumberOwned(minExpectedDelta);
        try {
            MemorySegment maxExpectedDelta$n = Box.boxNumberOwned(maxExpectedDelta);
            try {
                return DaqDataRule.createExplicitDomainDataRule(minExpectedDelta$n, maxExpectedDelta$n);
            } finally { Ffi.releaseRef(maxExpectedDelta$n); }
        } finally { Ffi.releaseRef(minExpectedDelta$n); }
    }

    /**
     * Creates a DataRule with a Linear rule type configuration.
     * &#64;param delta Coefficient by which the input data is to be multiplied.
     * &#64;param start Constant that is added to the &lt;em&gt;scale * value&lt;/em&gt; multiplication result.
     * The scale and offset are stored within the `parameters` member of the Rule object
     * with the scale being at the first position of the list, and the offset at the second.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqDataRule_createLinearDataRule()}.
     */
    public static DataRule createLinearDataRule(Object delta, Object start) {
        return DaqObject.wrap(create$createLinearDataRule(delta, start), DataRule.class);
    }

    private static MemorySegment create$createLinearDataRule(Object delta, Object start) {
        MemorySegment delta$n = Box.boxNumberOwned(delta);
        try {
            MemorySegment start$n = Box.boxNumberOwned(start);
            try {
                return DaqDataRule.createLinearDataRule(delta$n, start$n);
            } finally { Ffi.releaseRef(start$n); }
        } finally { Ffi.releaseRef(delta$n); }
    }

    /**
     * Gets a dictionary of string-object key-value pairs representing the parameters used to evaluate the rule.
     * &#64;param[out] parameters The dictionary containing the rule parameter members.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqDataRule_getParameters()}.
     */
    public java.util.Map<String, Object> getParameters() {
        try {
            return Wrap.map(DaqDataRule.getParameters(requireLivePointer()), p -> (String) Wrap.unboxOwned(p), Wrap::unboxOwned);
        } finally {
            Reference.reachabilityFence(this);
        }
    }

    /**
     * Gets the type of the data rule.
     * &#64;param[out] type The type of the data rule.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqDataRule_getType()}.
     */
    public DataRuleType getType() {
        try {
            return DaqDataRule.getType(requireLivePointer());
        } finally {
            Reference.reachabilityFence(this);
        }
    }

}
