// This file is autogenerated by tools/generate_high_level_bindings.py. Do not edit.

package org.opendaq;

import java.lang.foreign.Arena;
import java.lang.foreign.MemorySegment;
import java.lang.foreign.ValueLayout;
import java.lang.ref.Reference;
import java.util.List;
import java.util.Map;

import org.opendaq.lowlevel.*;

/**
 * This class manages the lock state of an object, usually a device. A device can be locked either with a specific user or without one. If the device is locked with a specific user, only that user can unlock it. If the device is locked without a user, any user can unlock it.
 *
 * &lt;p&gt;Wraps the openDAQ {&#64;code daqUserLock} interface.
 */
public class UserLock extends BaseObject {

    /** Adopt an owned reference (see {@link DaqObject#DaqObject}). */
    public UserLock(MemorySegment pointer) {
        super(pointer);
    }

    /**
     * Creates an unlocked UserLock object.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqUserLock_createUserLock()}.
     */
    public UserLock() {
        super(create$createUserLock());
    }

    private static MemorySegment create$createUserLock() {
        return DaqUserLock.createUserLock();
    }

    /**
     * Forcefully unlock the object. A force unlock will always succeed, regardless of which user initially locked the object.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqUserLock_forceUnlock()}.
     */
    public void forceUnlock() {
        try {
            DaqUserLock.forceUnlock(requireLivePointer());
        } finally {
            Reference.reachabilityFence(this);
        }
    }

    /**
     * Returns true if the object is locked.
     * &#64;param isLockedOut[out] True if the object is locked.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqUserLock_isLocked()}.
     */
    public boolean isLocked() {
        try {
            return DaqUserLock.isLocked(requireLivePointer());
        } finally {
            Reference.reachabilityFence(this);
        }
    }

    /**
     * Lock the object. Only the user who locked the object can unlock it. If the object was locked without a specific user, or with an anonymous user, any user can unlock it.
     * &#64;param user User performing the lock action.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqUserLock_lock()}.
     */
    public void lock(User user) {
        try {
            DaqUserLock.lock(requireLivePointer(), (user == null ? MemorySegment.NULL : user.requireLivePointer()));
        } finally {
            Reference.reachabilityFence(this);
            Reference.reachabilityFence(user);
        }
    }

    /** lock with trailing arguments at their openDAQ defaults. */
    public void lock() {
        lock((User) null);
    }

    /**
     * Unlock the object. Only the user who locked the object can unlock it. If the object was locked without a specific user, or with an anonymous user, any user can unlock it.
     * &#64;param user User performing the unlock action.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqUserLock_unlock()}.
     */
    public void unlock(User user) {
        try {
            DaqUserLock.unlock(requireLivePointer(), (user == null ? MemorySegment.NULL : user.requireLivePointer()));
        } finally {
            Reference.reachabilityFence(this);
            Reference.reachabilityFence(user);
        }
    }

    /** unlock with trailing arguments at their openDAQ defaults. */
    public void unlock() {
        unlock((User) null);
    }

}
