// This file is autogenerated by tools/generate_high_level_bindings.py. Do not edit.

package org.opendaq;

import java.lang.foreign.Arena;
import java.lang.foreign.MemorySegment;
import java.lang.foreign.ValueLayout;
import java.lang.ref.Reference;
import java.util.List;
import java.util.Map;

import org.opendaq.lowlevel.*;

/**
 * Represents an openDAQ device. The device contains a list of signals and physical channels. Some devices support adding function blocks, or connecting to devices. The list of available function blocks/devices can be obtained via the `getAvailable` functions, and added via the `add` functions.
 * Devices can be split up into three different types, with each devices supporting one or more:
 * 1. Physical devices
 * Physical devices provide access to physical channels. They measure real-world data and send
 * it via packets through output signals of channels. The list of channels can be obtained via
 * `getChannels` as a flat list.
 * 2. Client devices
 * Client devices can connect to other devices via their supported connection protocol. openDAQ
 * natively supports connecting to TMS devices via its openDAQ OpcUa Client Module. A list of available
 * devices a client device can connect to can be obtained via `getAvailableDevices`. The
 * `addDevice` is used to connect to/add a device.
 * 3. Function block devices
 * Function block devices provide a dictionary of available function block types that can be added to them
 * and configured. The calculation of function blocks is done on the device itself. The dictionary
 * of available function block types can be obtained via `getAvailableFunctionBlockTypes`. They
 * can then be added via `addFunctionBlock`.
 * All devices also provide access to their Device information, containing metadata such as the
 * device's serial number, location... They can also be queried for their current domain values
 * (time) through its device domain.
 * As each device is a property object, a device has access to all Property object methods, allowing
 * each device to expose a list of custom properties such as sample rate, scaling factor and many
 * others. By default, openDAQ devices have the UserName and Location string Properties.
 *
 * &lt;p&gt;Wraps the openDAQ {&#64;code daqDevice} interface.
 */
public class Device extends Folder {

    /** Adopt an owned reference (see {@link DaqObject#DaqObject}). */
    public Device(MemorySegment pointer) {
        super(pointer);
    }

    /**
     * Creates an openDAQ client.
     * &#64;param ctx The context object.
     * &#64;param localId The localID of the client.
     * &#64;param defaultDeviceInfo The DeviceInfo to be used by the client device.
     * &#64;param parent The parent component of the client.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqDevice_createClient()}.
     */
    public Device(Context ctx, String localId, DeviceInfo defaultDeviceInfo, Component parent) {
        super(create$createClient(ctx, localId, defaultDeviceInfo, parent));
    }

    private static MemorySegment create$createClient(Context ctx, String localId, DeviceInfo defaultDeviceInfo, Component parent) {
        try {
            MemorySegment localId$s = Wrap.daqStringOrNull(localId);
            try {
                return DaqDevice.createClient((ctx == null ? MemorySegment.NULL : ctx.requireLivePointer()), localId$s, (defaultDeviceInfo == null ? MemorySegment.NULL : defaultDeviceInfo.requireLivePointer()), (parent == null ? MemorySegment.NULL : parent.requireLivePointer()));
            } finally { Ffi.releaseRef(localId$s); }
        } finally {
            Reference.reachabilityFence(ctx);
            Reference.reachabilityFence(defaultDeviceInfo);
            Reference.reachabilityFence(parent);
        }
    }

    /**
     * Connects to a device at the given connection string and returns it.
     * &#64;param[out] device The added device.
     * &#64;param connectionString The connection string containing the address of the device. In example an IPv4/IPv6 address. The connection string can be found in the Device Info objects returned by `getAvailableDevices`.
     * &#64;param config A config object to configure a client device. This object can contain properties like max sample rate, port to use for 3rd party communication, number of channels to generate, or other device specific settings. Can be created from its corresponding Device type object. In case of a null value, it will use the default configuration.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqDevice_addDevice()}.
     */
    public Device addDevice(String connectionString, PropertyObject config) {
        try {
            MemorySegment connectionString$s = Wrap.daqStringOrNull(connectionString);
            try {
                return DaqObject.wrap(DaqDevice.addDevice(requireLivePointer(), connectionString$s, (config == null ? MemorySegment.NULL : config.requireLivePointer())), Device.class);
            } finally { Ffi.releaseRef(connectionString$s); }
        } finally {
            Reference.reachabilityFence(this);
            Reference.reachabilityFence(config);
        }
    }

    /** addDevice with trailing arguments at their openDAQ defaults. */
    public Device addDevice(String connectionString) {
        return addDevice(connectionString, (PropertyObject) null);
    }

    /**
     * Connects to multiple devices in parallel using the provided connection strings and returns the connected devices. Each connection is established concurrently to improve performance when handling multiple devices. The additions, in turn, are performed sequentially in the order specified by connectionArgs.
     * &#64;param[out] devices A dictionary that maps each connection string to the corresponding added device object. If a device connection or addition attempt fails, the value will be `nullptr` for that entry.
     * &#64;param connectionArgs A dictionary where each key is a connection string identifying the target device (e.g., IPv4/IPv6), and each value is a configuration object that customizes the connection. The configuration may specify parameters such as maximum sample rate, communication port, number of channels, or other device-specific settings. A `nullptr` value indicates that the default configuration should be used.
     * &#64;param errCodes An optional dictionary used to populate error codes for failed connection or addition attempts. For each failed attempt, the key is the connection string, and the value contains the error code.
     * &#64;param errorInfos An optional dictionary used to populate detailed error info for failed connection or addition attempts. For each failed attempt, the key is the connection string, and the value contains the error info object.
     * &#64;return OPENDAQ_PARTIAL_SUCCESS if at least one device was successfully created and added, but not all of them;
     * OPENDAQ_ERR_GENERALERROR if no devices were created or added;
     * OPENDAQ_IGNORED if adding the devices from modules is not allowed within the device.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqDevice_addDevices()}.
     */
    public java.util.Map<String, Device> addDevices(java.util.Map<?, ?> connectionArgs, java.util.Map<?, ?> errCodes, java.util.Map<?, ?> errorInfos) {
        try {
            MemorySegment connectionArgs$d = (connectionArgs == null ? MemorySegment.NULL : Box.boxDictOwned(connectionArgs));
            try {
                MemorySegment errCodes$d = (errCodes == null ? MemorySegment.NULL : Box.boxDictOwned(errCodes));
                try {
                    MemorySegment errorInfos$d = (errorInfos == null ? MemorySegment.NULL : Box.boxDictOwned(errorInfos));
                    try {
                        return Wrap.map(DaqDevice.addDevices(requireLivePointer(), connectionArgs$d, errCodes$d, errorInfos$d), p -> (String) Wrap.unboxOwned(p), p -> Wrap.castOwned(p, Device.class));
                    } finally { Ffi.releaseRef(errorInfos$d); }
                } finally { Ffi.releaseRef(errCodes$d); }
            } finally { Ffi.releaseRef(connectionArgs$d); }
        } finally {
            Reference.reachabilityFence(this);
        }
    }

    /** addDevices with trailing arguments at their openDAQ defaults. */
    public java.util.Map<String, Device> addDevices(java.util.Map<?, ?> connectionArgs) {
        return addDevices(connectionArgs, (java.util.Map<?, ?>) null, (java.util.Map<?, ?>) null);
    }

    /** addDevices with trailing arguments at their openDAQ defaults. */
    public java.util.Map<String, Device> addDevices(java.util.Map<?, ?> connectionArgs, java.util.Map<?, ?> errCodes) {
        return addDevices(connectionArgs, errCodes, (java.util.Map<?, ?>) null);
    }

    /**
     * Creates and adds a function block to the device with the provided unique ID and returns it.
     * &#64;param[out] functionBlock The added function block.
     * &#64;param typeId The unique ID of the function block. Can be obtained from its corresponding Function Block Info object.
     * &#64;param config A config object to configure a function block with custom settings specific to that function block type.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqDevice_addFunctionBlock()}.
     */
    public FunctionBlock addFunctionBlock(String typeId, PropertyObject config) {
        try {
            MemorySegment typeId$s = Wrap.daqStringOrNull(typeId);
            try {
                return DaqObject.wrap(DaqDevice.addFunctionBlock(requireLivePointer(), typeId$s, (config == null ? MemorySegment.NULL : config.requireLivePointer())), FunctionBlock.class);
            } finally { Ffi.releaseRef(typeId$s); }
        } finally {
            Reference.reachabilityFence(this);
            Reference.reachabilityFence(config);
        }
    }

    /** addFunctionBlock with trailing arguments at their openDAQ defaults. */
    public FunctionBlock addFunctionBlock(String typeId) {
        return addFunctionBlock(typeId, (PropertyObject) null);
    }

    /**
     * Creates and adds to the device a server with the provided unique type ID and returns it.
     * &#64;param[out] server The added server.
     * &#64;param typeId The unique type ID of the server. Can be obtained from its corresponding Server type object.
     * &#64;param config A config object to configure a server with custom settings specific to that server type.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqDevice_addServer()}.
     */
    public Server addServer(String typeId, PropertyObject config) {
        try {
            MemorySegment typeId$s = Wrap.daqStringOrNull(typeId);
            try {
                return DaqObject.wrap(DaqDevice.addServer(requireLivePointer(), typeId$s, (config == null ? MemorySegment.NULL : config.requireLivePointer())), Server.class);
            } finally { Ffi.releaseRef(typeId$s); }
        } finally {
            Reference.reachabilityFence(this);
            Reference.reachabilityFence(config);
        }
    }

    /**
     * Connects to a streaming at the given connection string, adds it as a streaming source of device and returns created streaming object.
     * &#64;param[out] streaming The added streaming source.
     * &#64;param connectionString The connection string containing the address of the streaming. In example an IPv4/IPv6 address. The connection string can be found in the Server Capability objects returned by `getInfo().getServerCapabilities()`.
     * &#64;param config A config object to configure a streaming connection. This object can contain properties like various connection timeouts or other streaming protocol specific settings. Can be created from its corresponding Streaming type object. In case of a null value, it will use the default configuration.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqDevice_addStreaming()}.
     */
    public Streaming addStreaming(String connectionString, PropertyObject config) {
        try {
            MemorySegment connectionString$s = Wrap.daqStringOrNull(connectionString);
            try {
                return DaqObject.wrap(DaqDevice.addStreaming(requireLivePointer(), connectionString$s, (config == null ? MemorySegment.NULL : config.requireLivePointer())), Streaming.class);
            } finally { Ffi.releaseRef(connectionString$s); }
        } finally {
            Reference.reachabilityFence(this);
            Reference.reachabilityFence(config);
        }
    }

    /** addStreaming with trailing arguments at their openDAQ defaults. */
    public Streaming addStreaming(String connectionString) {
        return addStreaming(connectionString, (PropertyObject) null);
    }

    /**
     * Creates config object that can be used when adding a device. Contains Device and Streaming default configuration for all available Device/Streaming types. Also contains general add-device configuration settings.
     * &#64;param[out] defaultConfig The configuration object containing default settings for adding a device.
     * The default config object is organized to always have 3 object-type properties:
     * - "General" Contains general properties such as "AutomaticallyConnectStreaming"
     * - "Device": Contains a child object-type property for each available device type, with the key of each property
     * being the ID of the device type. These can be configured to customize the `addDevice` call when using
     * connecting to the selected device type (eg. via the native or OPC UA protocols).
     * - "Streaming": Same as device, but used to configure each individual streaming connection established
     * when calling `addDevice`.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqDevice_createDefaultAddDeviceConfig()}.
     */
    public PropertyObject createDefaultAddDeviceConfig() {
        try {
            return DaqObject.wrap(DaqDevice.createDefaultAddDeviceConfig(requireLivePointer()), PropertyObject.class);
        } finally {
            Reference.reachabilityFence(this);
        }
    }

    /**
     * Get a dictionary of available device types as &lt;IString, IDeviceType&gt; pairs
     * &#64;param[out] deviceTypes The dictionary of available device types.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqDevice_getAvailableDeviceTypes()}.
     */
    public java.util.Map<String, DeviceType> getAvailableDeviceTypes() {
        try {
            return Wrap.map(DaqDevice.getAvailableDeviceTypes(requireLivePointer()), p -> (String) Wrap.unboxOwned(p), p -> Wrap.castOwned(p, DeviceType.class));
        } finally {
            Reference.reachabilityFence(this);
        }
    }

    /**
     * Gets a list of available devices, containing their Device Info.
     * &#64;param[out] availableDevices The list of available devices.
     * The getAvailableDevices most often runs a discovery client, querying for available devices that
     * a device module can connect to. The replies are formed into Device Info objects and inserted to the
     * list of available devices.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqDevice_getAvailableDevices()}.
     */
    public java.util.List<DeviceInfo> getAvailableDevices() {
        try {
            return Wrap.list(DaqDevice.getAvailableDevices(requireLivePointer()), p -> Wrap.castOwned(p, DeviceInfo.class));
        } finally {
            Reference.reachabilityFence(this);
        }
    }

    /**
     * Gets all function block types that are supported by the device, containing their description.
     * &#64;param[out] functionBlockTypes A dictionary of available function block types.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqDevice_getAvailableFunctionBlockTypes()}.
     */
    public java.util.Map<String, FunctionBlockType> getAvailableFunctionBlockTypes() {
        try {
            return Wrap.map(DaqDevice.getAvailableFunctionBlockTypes(requireLivePointer()), p -> (String) Wrap.unboxOwned(p), p -> Wrap.castOwned(p, FunctionBlockType.class));
        } finally {
            Reference.reachabilityFence(this);
        }
    }

    /**
     * Gets a list of available operation modes for the device.
     * &#64;param[out] availableOpModes The list of available operation modes.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqDevice_getAvailableOperationModes()}.
     */
    public java.util.List<Long> getAvailableOperationModes() {
        try {
            return Wrap.list(DaqDevice.getAvailableOperationModes(requireLivePointer()), p -> (Long) Wrap.unboxOwned(p));
        } finally {
            Reference.reachabilityFence(this);
        }
    }

    /**
     * Gets a flat list of the device's physical channels.
     * &#64;param searchFilter Provides an optional filter that filters out unwanted components and allows for recursion.
     * &#64;param[out] channels The flat list of channels.
     * If searchFilter is not provided, the returned list contains only visible channels and does not include those of
     * child devices.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqDevice_getChannels()}.
     */
    public java.util.List<Channel> getChannels(SearchFilter searchFilter) {
        try {
            return Wrap.list(DaqDevice.getChannels(requireLivePointer(), (searchFilter == null ? MemorySegment.NULL : searchFilter.requireLivePointer())), p -> Wrap.castOwned(p, Channel.class));
        } finally {
            Reference.reachabilityFence(this);
            Reference.reachabilityFence(searchFilter);
        }
    }

    /** getChannels with trailing arguments at their openDAQ defaults. */
    public java.util.List<Channel> getChannels() {
        return getChannels((SearchFilter) null);
    }

    /**
     * Gets a flat list of the device's physical channels. Also finds all visible channels of visible child devices
     * &#64;param searchFilter Provides an optional filter that filters out unwanted components and allows for recursion.
     * &#64;param[out] channels The flat list of channels.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqDevice_getChannelsRecursive()}.
     */
    public java.util.List<Channel> getChannelsRecursive(SearchFilter searchFilter) {
        try {
            return Wrap.list(DaqDevice.getChannelsRecursive(requireLivePointer(), (searchFilter == null ? MemorySegment.NULL : searchFilter.requireLivePointer())), p -> Wrap.castOwned(p, Channel.class));
        } finally {
            Reference.reachabilityFence(this);
            Reference.reachabilityFence(searchFilter);
        }
    }

    /** getChannelsRecursive with trailing arguments at their openDAQ defaults. */
    public java.util.List<Channel> getChannelsRecursive() {
        return getChannelsRecursive((SearchFilter) null);
    }

    /**
     * Gets the container holding the statuses of device configuration and streaming connections.
     * &#64;param[out] statusContainer The container for the device connection statuses.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqDevice_getConnectionStatusContainer()}.
     */
    public ComponentStatusContainer getConnectionStatusContainer() {
        try {
            return DaqObject.wrap(DaqDevice.getConnectionStatusContainer(requireLivePointer()), ComponentStatusContainer.class);
        } finally {
            Reference.reachabilityFence(this);
        }
    }

    /**
     * Gets a list of all components/folders in a device that are not titled 'IO', 'Sig', 'Dev', 'Synchronization' or 'FB'
     * &#64;param[out] customComponents The list of custom components.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqDevice_getCustomComponents()}.
     */
    public java.util.List<Component> getCustomComponents() {
        try {
            return Wrap.list(DaqDevice.getCustomComponents(requireLivePointer()), p -> Wrap.castOwned(p, Component.class));
        } finally {
            Reference.reachabilityFence(this);
        }
    }

    /**
     * Gets a list of child devices that the device is connected to.
     * &#64;param searchFilter Provides an optional filter that filters out unwanted components and allows for recursion.
     * &#64;param[out] devices The list of devices.
     * If searchFilter is not provided, the returned list contains only visible devices and does not include those of
     * child devices.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqDevice_getDevices()}.
     */
    public java.util.List<Device> getDevices(SearchFilter searchFilter) {
        try {
            return Wrap.list(DaqDevice.getDevices(requireLivePointer(), (searchFilter == null ? MemorySegment.NULL : searchFilter.requireLivePointer())), p -> Wrap.castOwned(p, Device.class));
        } finally {
            Reference.reachabilityFence(this);
            Reference.reachabilityFence(searchFilter);
        }
    }

    /** getDevices with trailing arguments at their openDAQ defaults. */
    public java.util.List<Device> getDevices() {
        return getDevices((SearchFilter) null);
    }

    /**
     * Gets the device's domain data. It allows for querying the device for its domain (time) values.
     * &#64;param[out] domain The device domain.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqDevice_getDomain()}.
     */
    public DeviceDomain getDomain() {
        try {
            return DaqObject.wrap(DaqDevice.getDomain(requireLivePointer()), DeviceDomain.class);
        } finally {
            Reference.reachabilityFence(this);
        }
    }

    /**
     * Gets the list of added function blocks.
     * &#64;param searchFilter Provides an optional filter that filters out unwanted components and allows for recursion.
     * &#64;param[out] functionBlocks The list of added function blocks.
     * If searchFilter is not provided, the returned list contains only visible function blocks and does not include those of
     * child function blocks, devices, or channels.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqDevice_getFunctionBlocks()}.
     */
    public java.util.List<FunctionBlock> getFunctionBlocks(SearchFilter searchFilter) {
        try {
            return Wrap.list(DaqDevice.getFunctionBlocks(requireLivePointer(), (searchFilter == null ? MemorySegment.NULL : searchFilter.requireLivePointer())), p -> Wrap.castOwned(p, FunctionBlock.class));
        } finally {
            Reference.reachabilityFence(this);
            Reference.reachabilityFence(searchFilter);
        }
    }

    /** getFunctionBlocks with trailing arguments at their openDAQ defaults. */
    public java.util.List<FunctionBlock> getFunctionBlocks() {
        return getFunctionBlocks((SearchFilter) null);
    }

    /**
     * Gets the device info. It contains data about the device such as the device's serial number, location, and connection string.
     * &#64;param[out] info The device info.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqDevice_getInfo()}.
     */
    public DeviceInfo getInfo() {
        try {
            return DaqObject.wrap(DaqDevice.getInfo(requireLivePointer()), DeviceInfo.class);
        } finally {
            Reference.reachabilityFence(this);
        }
    }

    /**
     * Gets a folder containing channels.
     * &#64;param[out] inputsOutputsFolder The folder that contains channels.
     * The InputsOutputs folder can contain other folders that themselves contain channels.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqDevice_getInputsOutputsFolder()}.
     */
    public Folder getInputsOutputsFolder() {
        try {
            return DaqObject.wrap(DaqDevice.getInputsOutputsFolder(requireLivePointer()), Folder.class);
        } finally {
            Reference.reachabilityFence(this);
        }
    }

    /**
     * Retrieves a chunk of the log file with the provided ID.
     * This function extracts a specified portion (or the entire content) of the log file, starting at the given offset.
     * If the size and offset are not specified, it will attempt to return the entire log file by default.
     * &#64;param[out] log A string which stores requested log chunk.
     * &#64;param id Rhe ID of the log file to retrieve.
     * &#64;param size The size of the log chunk to retrieve in bytes. Defaults to -1, which means it will return all remaining bytes from the offset.
     * &#64;param offset The offset, in bytes, from where the log chunk should be read. Defaults to 0 (start of the file).
     * If size is set to -1, and offset is 0, the entire log file will be returned.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqDevice_getLog()}.
     */
    public String getLog(String id, long size, long offset) {
        try {
            MemorySegment id$s = Wrap.daqStringOrNull(id);
            try {
                return Ffi.readDaqStringAndRelease(DaqDevice.getLog(requireLivePointer(), id$s, size, offset));
            } finally { Ffi.releaseRef(id$s); }
        } finally {
            Reference.reachabilityFence(this);
        }
    }

    /** getLog with trailing arguments at their openDAQ defaults. */
    public String getLog(String id) {
        return getLog(id, -1L, 0L);
    }

    /** getLog with trailing arguments at their openDAQ defaults. */
    public String getLog(String id, long size) {
        return getLog(id, size, 0L);
    }

    /**
     * Gets a list of available log files.
     * &#64;param[out] logFileInfos The list of available log files.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqDevice_getLogFileInfos()}.
     */
    public java.util.List<LogFileInfo> getLogFileInfos() {
        try {
            return Wrap.list(DaqDevice.getLogFileInfos(requireLivePointer()), p -> Wrap.castOwned(p, LogFileInfo.class));
        } finally {
            Reference.reachabilityFence(this);
        }
    }

    /**
     * Get list of added servers.
     * &#64;param[out] servers List of added servers.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqDevice_getServers()}.
     */
    public java.util.List<Server> getServers() {
        try {
            return Wrap.list(DaqDevice.getServers(requireLivePointer()), p -> Wrap.castOwned(p, Server.class));
        } finally {
            Reference.reachabilityFence(this);
        }
    }

    /**
     * Gets a list of the device's signals.
     * &#64;param searchFilter Provides an optional filter that filters out unwanted components and allows for recursion.
     * &#64;param[out] signals The flat list of signals.
     * If searchFilter is not provided, the returned list contains only visible signals and does not include those of
     * child function blocks, devices, or channels.
     * Device signals are most often domain signals shared by other signals that belong to channels and/or function blocks.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqDevice_getSignals()}.
     */
    public java.util.List<Signal> getSignals(SearchFilter searchFilter) {
        try {
            return Wrap.list(DaqDevice.getSignals(requireLivePointer(), (searchFilter == null ? MemorySegment.NULL : searchFilter.requireLivePointer())), p -> Wrap.castOwned(p, Signal.class));
        } finally {
            Reference.reachabilityFence(this);
            Reference.reachabilityFence(searchFilter);
        }
    }

    /** getSignals with trailing arguments at their openDAQ defaults. */
    public java.util.List<Signal> getSignals() {
        return getSignals((SearchFilter) null);
    }

    /**
     * Gets a list of the signals that belong to the device.
     * &#64;param searchFilter Provides an optional filter that filters out unwanted components and allows for recursion.
     * &#64;param[out] signals The flat list of signals.
     * The list includes visible signals that belong to visible channels, function blocks, or sub devices
     * of the device.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqDevice_getSignalsRecursive()}.
     */
    public java.util.List<Signal> getSignalsRecursive(SearchFilter searchFilter) {
        try {
            return Wrap.list(DaqDevice.getSignalsRecursive(requireLivePointer(), (searchFilter == null ? MemorySegment.NULL : searchFilter.requireLivePointer())), p -> Wrap.castOwned(p, Signal.class));
        } finally {
            Reference.reachabilityFence(this);
            Reference.reachabilityFence(searchFilter);
        }
    }

    /** getSignalsRecursive with trailing arguments at their openDAQ defaults. */
    public java.util.List<Signal> getSignalsRecursive() {
        return getSignalsRecursive((SearchFilter) null);
    }

    /**
     * Gets the sync component of the device.
     * &#64;param[out] sync The sync component.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqDevice_getSyncComponent()}.
     */
    public SyncComponent getSyncComponent() {
        try {
            return DaqObject.wrap(DaqDevice.getSyncComponent(requireLivePointer()), SyncComponent.class);
        } finally {
            Reference.reachabilityFence(this);
        }
    }

    /**
     * Gets the number of ticks passed since the device's absolute origin.
     * &#64;param[out] ticks The number of ticks.
     * To scale the ticks into a domain unit, the Device's Domain should be used.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqDevice_getTicksSinceOrigin()}.
     */
    public long getTicksSinceOrigin() {
        try {
            return DaqDevice.getTicksSinceOrigin(requireLivePointer());
        } finally {
            Reference.reachabilityFence(this);
        }
    }

    /**
     * Returns true if device is locked. Once locked, no properties of the device can be changed via the protocol layer.
     * &#64;param[out] locked True if device is locked.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqDevice_isLocked()}.
     */
    public boolean isLocked() {
        try {
            return DaqDevice.isLocked(requireLivePointer());
        } finally {
            Reference.reachabilityFence(this);
        }
    }

    /**
     * Loads the configuration of the device from string.
     * &#64;param configuration Serialized configuration of the device.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqDevice_loadConfiguration()}.
     */
    public void loadConfiguration(String configuration, UpdateParameters config) {
        try {
            MemorySegment configuration$s = Wrap.daqStringOrNull(configuration);
            try {
                DaqDevice.loadConfiguration(requireLivePointer(), configuration$s, (config == null ? MemorySegment.NULL : config.requireLivePointer()));
            } finally { Ffi.releaseRef(configuration$s); }
        } finally {
            Reference.reachabilityFence(this);
            Reference.reachabilityFence(config);
        }
    }

    /** loadConfiguration with trailing arguments at their openDAQ defaults. */
    public void loadConfiguration(String configuration) {
        loadConfiguration(configuration, (UpdateParameters) null);
    }

    /**
     * Lock a device with a session user. Once locked, no properties of the device can be changed via the protocol layer. Only the same user who locked the device can unlock it. If no user was specified when the device was locked, any user will be able to unlock it.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqDevice_lock()}.
     */
    public void lock() {
        try {
            DaqDevice.lock(requireLivePointer());
        } finally {
            Reference.reachabilityFence(this);
        }
    }

    /**
     * Disconnects from the device provided as argument and removes it from the internal list of devices.
     * &#64;param device The device to be removed.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqDevice_removeDevice()}.
     */
    public void removeDevice(Device device) {
        try {
            DaqDevice.removeDevice(requireLivePointer(), (device == null ? MemorySegment.NULL : device.requireLivePointer()));
        } finally {
            Reference.reachabilityFence(this);
            Reference.reachabilityFence(device);
        }
    }

    /**
     * Removes the function block provided as argument, disconnecting its signals and input ports.
     * &#64;param functionBlock The function block to be removed.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqDevice_removeFunctionBlock()}.
     */
    public void removeFunctionBlock(FunctionBlock functionBlock) {
        try {
            DaqDevice.removeFunctionBlock(requireLivePointer(), (functionBlock == null ? MemorySegment.NULL : functionBlock.requireLivePointer()));
        } finally {
            Reference.reachabilityFence(this);
            Reference.reachabilityFence(functionBlock);
        }
    }

    /**
     * Removes the server provided as argument.
     * &#64;param server The server to be removed.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqDevice_removeServer()}.
     */
    public void removeServer(Server server) {
        try {
            DaqDevice.removeServer(requireLivePointer(), (server == null ? MemorySegment.NULL : server.requireLivePointer()));
        } finally {
            Reference.reachabilityFence(this);
            Reference.reachabilityFence(server);
        }
    }

    /**
     * Saves the configuration of the device to string.
     * &#64;param[out] configuration Serialized configuration of the device.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqDevice_saveConfiguration()}.
     */
    public String saveConfiguration() {
        try {
            return Ffi.readDaqStringAndRelease(DaqDevice.saveConfiguration(requireLivePointer()));
        } finally {
            Reference.reachabilityFence(this);
        }
    }

    /**
     * Sets the operation mode of the device subtree excluding the sub-devices.
     * &#64;param modeType The operation mode to set.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqDevice_setOperationMode()}.
     */
    public void setOperationMode(OperationModeType modeType) {
        try {
            DaqDevice.setOperationMode(requireLivePointer(), modeType);
        } finally {
            Reference.reachabilityFence(this);
        }
    }

    /**
     * Sets the operation mode of the device subtree including the sub-devices.
     * &#64;param modeType The operation mode to set.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqDevice_setOperationModeRecursive()}.
     */
    public void setOperationModeRecursive(OperationModeType modeType) {
        try {
            DaqDevice.setOperationModeRecursive(requireLivePointer(), modeType);
        } finally {
            Reference.reachabilityFence(this);
        }
    }

    /**
     * Unlock a device with a session user. A device can only be unlocked by the same user who locked it. If no user was specified when the device was locked, any user will be able to unlock it.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqDevice_unlock()}.
     */
    public void unlock() {
        try {
            DaqDevice.unlock(requireLivePointer());
        } finally {
            Reference.reachabilityFence(this);
        }
    }

}
