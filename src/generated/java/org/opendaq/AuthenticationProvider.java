// This file is autogenerated by tools/generate_high_level_bindings.py. Do not edit.

package org.opendaq;

import java.lang.foreign.Arena;
import java.lang.foreign.MemorySegment;
import java.lang.foreign.ValueLayout;
import java.lang.ref.Reference;
import java.util.List;
import java.util.Map;

import org.opendaq.lowlevel.*;

/**
 * A class which is responsible for authenticating a user. The authentication is usually done by verifying the username and password. An authenticator implementation might use external services for achieving that. It might make a call to an external database, do a lookup to a json file with defined users or it might simply check the password against a hardcoded one.
 *
 * &lt;p&gt;Wraps the openDAQ {&#64;code daqAuthenticationProvider} interface.
 */
public class AuthenticationProvider extends BaseObject {

    /** Adopt an owned reference (see {@link DaqObject#DaqObject}). */
    public AuthenticationProvider(MemorySegment pointer) {
        super(pointer);
    }

    /**
     * Creates an empty authentication provider without any user.
     * &#64;param allowAnonymous True if anonymous authentication is allowed.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqAuthenticationProvider_createAuthenticationProvider()}.
     */
    public AuthenticationProvider(boolean allowAnonymous) {
        super(create$createAuthenticationProvider(allowAnonymous));
    }

    private static MemorySegment create$createAuthenticationProvider(boolean allowAnonymous) {
        return DaqAuthenticationProvider.createAuthenticationProvider(allowAnonymous);
    }

    /**
     * Creates an authentication provider out of json file.
     * &#64;param filename File path to a json file containing a list of serialized User objects.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqAuthenticationProvider_createJsonFileAuthenticationProvider()}.
     */
    public static AuthenticationProvider createJsonFileAuthenticationProvider(String filename) {
        return DaqObject.wrap(create$createJsonFileAuthenticationProvider(filename), AuthenticationProvider.class);
    }

    private static MemorySegment create$createJsonFileAuthenticationProvider(String filename) {
        MemorySegment filename$s = Wrap.daqStringOrNull(filename);
        try {
            return DaqAuthenticationProvider.createJsonFileAuthenticationProvider(filename$s);
        } finally { Ffi.releaseRef(filename$s); }
    }

    /**
     * Creates an authentication provider out of json string.
     * &#64;param jsonString Json string containg a list of serialized User objects.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqAuthenticationProvider_createJsonStringAuthenticationProvider()}.
     */
    public static AuthenticationProvider createJsonStringAuthenticationProvider(String jsonString) {
        return DaqObject.wrap(create$createJsonStringAuthenticationProvider(jsonString), AuthenticationProvider.class);
    }

    private static MemorySegment create$createJsonStringAuthenticationProvider(String jsonString) {
        MemorySegment jsonString$s = Wrap.daqStringOrNull(jsonString);
        try {
            return DaqAuthenticationProvider.createJsonStringAuthenticationProvider(jsonString$s);
        } finally { Ffi.releaseRef(jsonString$s); }
    }

    /**
     * Creates an authentication provider out of static list of users.
     * &#64;param allowAnonymous True if anonymous authentication is allowed.
     * &#64;param userList List of User objects.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqAuthenticationProvider_createStaticAuthenticationProvider()}.
     */
    public static AuthenticationProvider createStaticAuthenticationProvider(boolean allowAnonymous, java.util.List<?> userList) {
        return DaqObject.wrap(create$createStaticAuthenticationProvider(allowAnonymous, userList), AuthenticationProvider.class);
    }

    private static MemorySegment create$createStaticAuthenticationProvider(boolean allowAnonymous, java.util.List<?> userList) {
        MemorySegment userList$l = (userList == null ? MemorySegment.NULL : Box.boxListOwned(userList));
        try {
            return DaqAuthenticationProvider.createStaticAuthenticationProvider(allowAnonymous, userList$l);
        } finally { Ffi.releaseRef(userList$l); }
    }

    /**
     * Authenticate user using username and password. If authentication is successful, a User instance is returned. Otherwise an exception is thrown.
     * &#64;param username The username.
     * &#64;param password The password in plain text.
     * &#64;param user[out] And instance of successfully authenticated user. If authentication is not successful, an exception is thrown.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqAuthenticationProvider_authenticate()}.
     */
    public User authenticate(String username, String password) {
        try {
            MemorySegment username$s = Wrap.daqStringOrNull(username);
            try {
                MemorySegment password$s = Wrap.daqStringOrNull(password);
                try {
                    return DaqObject.wrap(DaqAuthenticationProvider.authenticate(requireLivePointer(), username$s, password$s), User.class);
                } finally { Ffi.releaseRef(password$s); }
            } finally { Ffi.releaseRef(username$s); }
        } finally {
            Reference.reachabilityFence(this);
        }
    }

    /**
     * Authenticate as anonymous user. If anonymous authentication is not allowed, an exception is thrown.
     * &#64;param user[out] Pointer to anonymous user instance. If authentication is not successful, an exception is thrown.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqAuthenticationProvider_authenticateAnonymous()}.
     */
    public User authenticateAnonymous() {
        try {
            return DaqObject.wrap(DaqAuthenticationProvider.authenticateAnonymous(requireLivePointer()), User.class);
        } finally {
            Reference.reachabilityFence(this);
        }
    }

    /**
     * Find a user instance by its username. If no user with maching username is not found, null is returned.
     * &#64;param username The username.
     * &#64;param userOut[out] Found user instance. If no user with maching username is not found, null is returned.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqAuthenticationProvider_findUser()}.
     */
    public User findUser(String username) {
        try {
            MemorySegment username$s = Wrap.daqStringOrNull(username);
            try {
                return DaqObject.wrap(DaqAuthenticationProvider.findUser(requireLivePointer(), username$s), User.class);
            } finally { Ffi.releaseRef(username$s); }
        } finally {
            Reference.reachabilityFence(this);
        }
    }

    /**
     * Returns true if anonymous authentication is allowed. When anonymous authentication is enabled, user can connect to the server without providing username or password.
     * &#64;param allowedOut[out] True if anonymous authentication is allowed.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqAuthenticationProvider_isAnonymousAllowed()}.
     */
    public boolean isAnonymousAllowed() {
        try {
            return DaqAuthenticationProvider.isAnonymousAllowed(requireLivePointer());
        } finally {
            Reference.reachabilityFence(this);
        }
    }

}
