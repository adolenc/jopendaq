// This file is autogenerated by tools/generate_low_level_bindings.py. Do not edit.

package org.opendaq.lowlevel;

import java.lang.foreign.Arena;
import java.lang.foreign.FunctionDescriptor;
import java.lang.foreign.MemorySegment;
import java.lang.invoke.MethodHandle;

import static java.lang.foreign.ValueLayout.*;

/** Raw FFI bindings for the daqTags_* functions of the openDAQ C API. */
public final class DaqTags {

    private DaqTags() {}

    private static final MethodHandle MH_contains =
        Ffi.downcallOrNull("daqTags_contains", FunctionDescriptor.of(JAVA_INT, ADDRESS, ADDRESS, ADDRESS));
    private static final MethodHandle MH_createTags =
        Ffi.downcallOrNull("daqTags_createTags", FunctionDescriptor.of(JAVA_INT, ADDRESS));
    private static final MethodHandle MH_getInterfaceId =
        Ffi.downcallOrNull("daqTags_getInterfaceId", FunctionDescriptor.ofVoid(ADDRESS));
    private static final MethodHandle MH_getList =
        Ffi.downcallOrNull("daqTags_getList", FunctionDescriptor.of(JAVA_INT, ADDRESS, ADDRESS));
    private static final MethodHandle MH_query =
        Ffi.downcallOrNull("daqTags_query", FunctionDescriptor.of(JAVA_INT, ADDRESS, ADDRESS, ADDRESS));

    /** Checks whether a tag is present in the list of tags.  Calls {@code daqTags_contains()}. */
    public static boolean contains(MemorySegment self, MemorySegment name) {
        try (Arena arena = Arena.ofConfined()) {
            MemorySegment valueSlot = arena.allocate(JAVA_BYTE);
            int err = (int) Ffi.require(MH_contains, "daqTags_contains").invokeExact(Ffi.orNull(self), Ffi.orNull(name), valueSlot);
            Ffi.checkError(err, "daqTags_contains");
            return valueSlot.get(JAVA_BYTE, 0) != 0;
        } catch (Throwable t) { throw Ffi.rethrow(t); }
    }

    /** Calls {@code daqTags_createTags()}. */
    public static MemorySegment createTags() {
        try (Arena arena = Arena.ofConfined()) {
            MemorySegment objSlot = arena.allocate(ADDRESS);
            int err = (int) Ffi.require(MH_createTags, "daqTags_createTags").invokeExact(objSlot);
            Ffi.checkError(err, "daqTags_createTags");
            return objSlot.get(ADDRESS, 0);
        } catch (Throwable t) { throw Ffi.rethrow(t); }
    }

    /** Calls {@code daqTags_getInterfaceId()} with caller-allocated out slots. */
    public static void getInterfaceId(MemorySegment intfId) {
        try {
            Ffi.require(MH_getInterfaceId, "daqTags_getInterfaceId").invokeExact(Ffi.orNull(intfId));
        } catch (Throwable t) { throw Ffi.rethrow(t); }
    }

    /** Gets the list of all tags in the list.  Calls {@code daqTags_getList()}. */
    public static MemorySegment getList(MemorySegment self) {
        try (Arena arena = Arena.ofConfined()) {
            MemorySegment valueSlot = arena.allocate(ADDRESS);
            int err = (int) Ffi.require(MH_getList, "daqTags_getList").invokeExact(Ffi.orNull(self), valueSlot);
            Ffi.checkError(err, "daqTags_getList");
            return valueSlot.get(ADDRESS, 0);
        } catch (Throwable t) { throw Ffi.rethrow(t); }
    }

    /** Queries the list of tags, creating an EvalValue expression from the `query` string. Returns true if the list of tags matches the query, false otherwise.  Calls {@code daqTags_query()}. */
    public static boolean query(MemorySegment self, MemorySegment query) {
        try (Arena arena = Arena.ofConfined()) {
            MemorySegment valueSlot = arena.allocate(JAVA_BYTE);
            int err = (int) Ffi.require(MH_query, "daqTags_query").invokeExact(Ffi.orNull(self), Ffi.orNull(query), valueSlot);
            Ffi.checkError(err, "daqTags_query");
            return valueSlot.get(JAVA_BYTE, 0) != 0;
        } catch (Throwable t) { throw Ffi.rethrow(t); }
    }

}
