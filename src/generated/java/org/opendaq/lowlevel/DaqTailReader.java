// This file is autogenerated by tools/generate_low_level_bindings.py. Do not edit.

package org.opendaq.lowlevel;

import java.lang.foreign.Arena;
import java.lang.foreign.FunctionDescriptor;
import java.lang.foreign.MemorySegment;
import java.lang.invoke.MethodHandle;

import static java.lang.foreign.ValueLayout.*;
import org.opendaq.ReadMode;
import org.opendaq.SampleType;

/** Raw FFI bindings for the daqTailReader_* functions of the openDAQ C API. */
public final class DaqTailReader {

    private DaqTailReader() {}

    private static final MethodHandle MH_createTailReader =
        Ffi.downcallOrNull("daqTailReader_createTailReader", FunctionDescriptor.of(JAVA_INT, ADDRESS, ADDRESS, JAVA_LONG, JAVA_INT, JAVA_INT, JAVA_INT));
    private static final MethodHandle MH_createTailReaderFromExisting =
        Ffi.downcallOrNull("daqTailReader_createTailReaderFromExisting", FunctionDescriptor.of(JAVA_INT, ADDRESS, ADDRESS, JAVA_LONG, JAVA_INT, JAVA_INT));
    private static final MethodHandle MH_createTailReaderFromPort =
        Ffi.downcallOrNull("daqTailReader_createTailReaderFromPort", FunctionDescriptor.of(JAVA_INT, ADDRESS, ADDRESS, JAVA_LONG, JAVA_INT, JAVA_INT, JAVA_INT));
    private static final MethodHandle MH_getHistorySize =
        Ffi.downcallOrNull("daqTailReader_getHistorySize", FunctionDescriptor.of(JAVA_INT, ADDRESS, ADDRESS));
    private static final MethodHandle MH_getInterfaceId =
        Ffi.downcallOrNull("daqTailReader_getInterfaceId", FunctionDescriptor.ofVoid(ADDRESS));
    private static final MethodHandle MH_read =
        Ffi.downcallOrNull("daqTailReader_read", FunctionDescriptor.of(JAVA_INT, ADDRESS, ADDRESS, ADDRESS, ADDRESS));
    private static final MethodHandle MH_readWithDomain =
        Ffi.downcallOrNull("daqTailReader_readWithDomain", FunctionDescriptor.of(JAVA_INT, ADDRESS, ADDRESS, ADDRESS, ADDRESS, ADDRESS));

    /** Calls {@code daqTailReader_createTailReader()}. */
    public static MemorySegment createTailReader(MemorySegment signal, long historySize, SampleType valueReadType, SampleType domainReadType, ReadMode mode) {
        try (Arena arena = Arena.ofConfined()) {
            MemorySegment objSlot = arena.allocate(ADDRESS);
            int err = (int) Ffi.require(MH_createTailReader, "daqTailReader_createTailReader").invokeExact(objSlot, Ffi.orNull(signal), historySize, valueReadType.value(), domainReadType.value(), mode.value());
            Ffi.checkError(err, "daqTailReader_createTailReader");
            return objSlot.get(ADDRESS, 0);
        } catch (Throwable t) { throw Ffi.rethrow(t); }
    }

    /** Calls {@code daqTailReader_createTailReaderFromExisting()}. */
    public static MemorySegment createTailReaderFromExisting(MemorySegment invalidatedReader, long historySize, SampleType valueReadType, SampleType domainReadType) {
        try (Arena arena = Arena.ofConfined()) {
            MemorySegment objSlot = arena.allocate(ADDRESS);
            int err = (int) Ffi.require(MH_createTailReaderFromExisting, "daqTailReader_createTailReaderFromExisting").invokeExact(objSlot, Ffi.orNull(invalidatedReader), historySize, valueReadType.value(), domainReadType.value());
            Ffi.checkError(err, "daqTailReader_createTailReaderFromExisting");
            return objSlot.get(ADDRESS, 0);
        } catch (Throwable t) { throw Ffi.rethrow(t); }
    }

    /** Calls {@code daqTailReader_createTailReaderFromPort()}. */
    public static MemorySegment createTailReaderFromPort(MemorySegment port, long historySize, SampleType valueReadType, SampleType domainReadType, ReadMode mode) {
        try (Arena arena = Arena.ofConfined()) {
            MemorySegment objSlot = arena.allocate(ADDRESS);
            int err = (int) Ffi.require(MH_createTailReaderFromPort, "daqTailReader_createTailReaderFromPort").invokeExact(objSlot, Ffi.orNull(port), historySize, valueReadType.value(), domainReadType.value(), mode.value());
            Ffi.checkError(err, "daqTailReader_createTailReaderFromPort");
            return objSlot.get(ADDRESS, 0);
        } catch (Throwable t) { throw Ffi.rethrow(t); }
    }

    /** The maximum amount of samples in history to keep.  Calls {@code daqTailReader_getHistorySize()}. */
    public static long getHistorySize(MemorySegment self) {
        try (Arena arena = Arena.ofConfined()) {
            MemorySegment sizeSlot = arena.allocate(JAVA_LONG);
            int err = (int) Ffi.require(MH_getHistorySize, "daqTailReader_getHistorySize").invokeExact(Ffi.orNull(self), sizeSlot);
            Ffi.checkError(err, "daqTailReader_getHistorySize");
            return sizeSlot.get(JAVA_LONG, 0);
        } catch (Throwable t) { throw Ffi.rethrow(t); }
    }

    /** Calls {@code daqTailReader_getInterfaceId()} with caller-allocated out slots. */
    public static void getInterfaceId(MemorySegment intfId) {
        try {
            Ffi.require(MH_getInterfaceId, "daqTailReader_getInterfaceId").invokeExact(Ffi.orNull(intfId));
        } catch (Throwable t) { throw Ffi.rethrow(t); }
    }

    /** Result of {@link #read}. */
    public record ReadResult(long count, MemorySegment status) {}

    /** Copies at maximum the next `count` unread samples to the values buffer. The amount actually read is returned through the `count` parameter.  Calls {@code daqTailReader_read()}. */
    public static ReadResult read(MemorySegment self, MemorySegment values, long count) {
        try (Arena arena = Arena.ofConfined()) {
            MemorySegment countSlot = arena.allocate(JAVA_LONG);
            countSlot.set(JAVA_LONG, 0, count);
            MemorySegment statusSlot = arena.allocate(ADDRESS);
            int err = (int) Ffi.require(MH_read, "daqTailReader_read").invokeExact(Ffi.orNull(self), Ffi.orNull(values), countSlot, statusSlot);
            Ffi.checkError(err, "daqTailReader_read");
            return new ReadResult(countSlot.get(JAVA_LONG, 0), statusSlot.get(ADDRESS, 0));
        } catch (Throwable t) { throw Ffi.rethrow(t); }
    }

    /** Result of {@link #readWithDomain}. */
    public record ReadWithDomainResult(long count, MemorySegment status) {}

    /** Copies at maximum the next `count` unread samples and clock-stamps to the `values` and `stamps` buffers. The amount actually read is returned through the `count` parameter.  Calls {@code daqTailReader_readWithDomain()}. */
    public static ReadWithDomainResult readWithDomain(MemorySegment self, MemorySegment values, MemorySegment domain, long count) {
        try (Arena arena = Arena.ofConfined()) {
            MemorySegment countSlot = arena.allocate(JAVA_LONG);
            countSlot.set(JAVA_LONG, 0, count);
            MemorySegment statusSlot = arena.allocate(ADDRESS);
            int err = (int) Ffi.require(MH_readWithDomain, "daqTailReader_readWithDomain").invokeExact(Ffi.orNull(self), Ffi.orNull(values), Ffi.orNull(domain), countSlot, statusSlot);
            Ffi.checkError(err, "daqTailReader_readWithDomain");
            return new ReadWithDomainResult(countSlot.get(JAVA_LONG, 0), statusSlot.get(ADDRESS, 0));
        } catch (Throwable t) { throw Ffi.rethrow(t); }
    }

}
