// This file is autogenerated by tools/generate_low_level_bindings.py. Do not edit.

package org.opendaq.lowlevel;

import java.lang.foreign.Arena;
import java.lang.foreign.FunctionDescriptor;
import java.lang.foreign.MemorySegment;
import java.lang.invoke.MethodHandle;

import static java.lang.foreign.ValueLayout.*;
import org.opendaq.LogLevel;

/** Raw FFI bindings for the daqLogger_* functions of the openDAQ C API. */
public final class DaqLogger {

    private DaqLogger() {}

    private static final MethodHandle MH_addComponent =
        Ffi.downcallOrNull("daqLogger_addComponent", FunctionDescriptor.of(JAVA_INT, ADDRESS, ADDRESS, ADDRESS));
    private static final MethodHandle MH_createLogger =
        Ffi.downcallOrNull("daqLogger_createLogger", FunctionDescriptor.of(JAVA_INT, ADDRESS, ADDRESS, JAVA_INT));
    private static final MethodHandle MH_flush =
        Ffi.downcallOrNull("daqLogger_flush", FunctionDescriptor.of(JAVA_INT, ADDRESS));
    private static final MethodHandle MH_flushOnLevel =
        Ffi.downcallOrNull("daqLogger_flushOnLevel", FunctionDescriptor.of(JAVA_INT, ADDRESS, JAVA_INT));
    private static final MethodHandle MH_getComponent =
        Ffi.downcallOrNull("daqLogger_getComponent", FunctionDescriptor.of(JAVA_INT, ADDRESS, ADDRESS, ADDRESS));
    private static final MethodHandle MH_getComponents =
        Ffi.downcallOrNull("daqLogger_getComponents", FunctionDescriptor.of(JAVA_INT, ADDRESS, ADDRESS));
    private static final MethodHandle MH_getInterfaceId =
        Ffi.downcallOrNull("daqLogger_getInterfaceId", FunctionDescriptor.ofVoid(ADDRESS));
    private static final MethodHandle MH_getLevel =
        Ffi.downcallOrNull("daqLogger_getLevel", FunctionDescriptor.of(JAVA_INT, ADDRESS, ADDRESS));
    private static final MethodHandle MH_getOrAddComponent =
        Ffi.downcallOrNull("daqLogger_getOrAddComponent", FunctionDescriptor.of(JAVA_INT, ADDRESS, ADDRESS, ADDRESS));
    private static final MethodHandle MH_removeComponent =
        Ffi.downcallOrNull("daqLogger_removeComponent", FunctionDescriptor.of(JAVA_INT, ADDRESS, ADDRESS));
    private static final MethodHandle MH_setLevel =
        Ffi.downcallOrNull("daqLogger_setLevel", FunctionDescriptor.of(JAVA_INT, ADDRESS, JAVA_INT));

    /** Creates a component with a given name and adds it to the Logger object.  Calls {@code daqLogger_addComponent()}. */
    public static MemorySegment addComponent(MemorySegment self, MemorySegment name) {
        try (Arena arena = Arena.ofConfined()) {
            MemorySegment componentSlot = arena.allocate(ADDRESS);
            int err = (int) Ffi.require(MH_addComponent, "daqLogger_addComponent").invokeExact(Ffi.orNull(self), Ffi.orNull(name), componentSlot);
            Ffi.checkError(err, "daqLogger_addComponent");
            return componentSlot.get(ADDRESS, 0);
        } catch (Throwable t) { throw Ffi.rethrow(t); }
    }

    /** Calls {@code daqLogger_createLogger()}. */
    public static MemorySegment createLogger(MemorySegment sinks, LogLevel level) {
        try (Arena arena = Arena.ofConfined()) {
            MemorySegment objSlot = arena.allocate(ADDRESS);
            int err = (int) Ffi.require(MH_createLogger, "daqLogger_createLogger").invokeExact(objSlot, Ffi.orNull(sinks), level.value());
            Ffi.checkError(err, "daqLogger_createLogger");
            return objSlot.get(ADDRESS, 0);
        } catch (Throwable t) { throw Ffi.rethrow(t); }
    }

    /** Triggers writing out the messages stored in temporary buffers for added components and sinks associated with the Logger object.  Calls {@code daqLogger_flush()}. */
    public static void flush(MemorySegment self) {
        try {
            int err = (int) Ffi.require(MH_flush, "daqLogger_flush").invokeExact(Ffi.orNull(self));
            Ffi.checkError(err, "daqLogger_flush");
        } catch (Throwable t) { throw Ffi.rethrow(t); }
    }

    /** Sets the minimum severity level of messages to be automatically flushed by components of Logger object.  Calls {@code daqLogger_flushOnLevel()}. */
    public static void flushOnLevel(MemorySegment self, LogLevel level) {
        try {
            int err = (int) Ffi.require(MH_flushOnLevel, "daqLogger_flushOnLevel").invokeExact(Ffi.orNull(self), level.value());
            Ffi.checkError(err, "daqLogger_flushOnLevel");
        } catch (Throwable t) { throw Ffi.rethrow(t); }
    }

    /** Gets an added component by name.  Calls {@code daqLogger_getComponent()}. */
    public static MemorySegment getComponent(MemorySegment self, MemorySegment name) {
        try (Arena arena = Arena.ofConfined()) {
            MemorySegment componentSlot = arena.allocate(ADDRESS);
            int err = (int) Ffi.require(MH_getComponent, "daqLogger_getComponent").invokeExact(Ffi.orNull(self), Ffi.orNull(name), componentSlot);
            Ffi.checkError(err, "daqLogger_getComponent");
            return componentSlot.get(ADDRESS, 0);
        } catch (Throwable t) { throw Ffi.rethrow(t); }
    }

    /** Gets a list of added components.  Calls {@code daqLogger_getComponents()}. */
    public static MemorySegment getComponents(MemorySegment self) {
        try (Arena arena = Arena.ofConfined()) {
            MemorySegment componentsSlot = arena.allocate(ADDRESS);
            int err = (int) Ffi.require(MH_getComponents, "daqLogger_getComponents").invokeExact(Ffi.orNull(self), componentsSlot);
            Ffi.checkError(err, "daqLogger_getComponents");
            return componentsSlot.get(ADDRESS, 0);
        } catch (Throwable t) { throw Ffi.rethrow(t); }
    }

    /** Calls {@code daqLogger_getInterfaceId()} with caller-allocated out slots. */
    public static void getInterfaceId(MemorySegment intfId) {
        try {
            Ffi.require(MH_getInterfaceId, "daqLogger_getInterfaceId").invokeExact(Ffi.orNull(intfId));
        } catch (Throwable t) { throw Ffi.rethrow(t); }
    }

    /** Gets the default log severity level.  Calls {@code daqLogger_getLevel()}. */
    public static LogLevel getLevel(MemorySegment self) {
        try (Arena arena = Arena.ofConfined()) {
            MemorySegment levelSlot = arena.allocate(JAVA_INT);
            int err = (int) Ffi.require(MH_getLevel, "daqLogger_getLevel").invokeExact(Ffi.orNull(self), levelSlot);
            Ffi.checkError(err, "daqLogger_getLevel");
            return LogLevel.fromValue(levelSlot.get(JAVA_INT, 0));
        } catch (Throwable t) { throw Ffi.rethrow(t); }
    }

    /** Gets an added component by name or creates a new one with a given name and adds it to the Logger object.  Calls {@code daqLogger_getOrAddComponent()}. */
    public static MemorySegment getOrAddComponent(MemorySegment self, MemorySegment name) {
        try (Arena arena = Arena.ofConfined()) {
            MemorySegment componentSlot = arena.allocate(ADDRESS);
            int err = (int) Ffi.require(MH_getOrAddComponent, "daqLogger_getOrAddComponent").invokeExact(Ffi.orNull(self), Ffi.orNull(name), componentSlot);
            Ffi.checkError(err, "daqLogger_getOrAddComponent");
            return componentSlot.get(ADDRESS, 0);
        } catch (Throwable t) { throw Ffi.rethrow(t); }
    }

    /** Removes the component with a given name from the Logger object.  Calls {@code daqLogger_removeComponent()}. */
    public static void removeComponent(MemorySegment self, MemorySegment name) {
        try {
            int err = (int) Ffi.require(MH_removeComponent, "daqLogger_removeComponent").invokeExact(Ffi.orNull(self), Ffi.orNull(name));
            Ffi.checkError(err, "daqLogger_removeComponent");
        } catch (Throwable t) { throw Ffi.rethrow(t); }
    }

    /** Sets the default log severity level.  Calls {@code daqLogger_setLevel()}. */
    public static void setLevel(MemorySegment self, LogLevel level) {
        try {
            int err = (int) Ffi.require(MH_setLevel, "daqLogger_setLevel").invokeExact(Ffi.orNull(self), level.value());
            Ffi.checkError(err, "daqLogger_setLevel");
        } catch (Throwable t) { throw Ffi.rethrow(t); }
    }

}
