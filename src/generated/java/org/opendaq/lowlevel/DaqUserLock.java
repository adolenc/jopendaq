// This file is autogenerated by tools/generate_low_level_bindings.py. Do not edit.

package org.opendaq.lowlevel;

import java.lang.foreign.Arena;
import java.lang.foreign.FunctionDescriptor;
import java.lang.foreign.MemorySegment;
import java.lang.invoke.MethodHandle;

import static java.lang.foreign.ValueLayout.*;

/** Raw FFI bindings for the daqUserLock_* functions of the openDAQ C API. */
public final class DaqUserLock {

    private DaqUserLock() {}

    private static final MethodHandle MH_createUserLock =
        Ffi.downcallOrNull("daqUserLock_createUserLock", FunctionDescriptor.of(JAVA_INT, ADDRESS));
    private static final MethodHandle MH_forceUnlock =
        Ffi.downcallOrNull("daqUserLock_forceUnlock", FunctionDescriptor.of(JAVA_INT, ADDRESS));
    private static final MethodHandle MH_getInterfaceId =
        Ffi.downcallOrNull("daqUserLock_getInterfaceId", FunctionDescriptor.ofVoid(ADDRESS));
    private static final MethodHandle MH_isLocked =
        Ffi.downcallOrNull("daqUserLock_isLocked", FunctionDescriptor.of(JAVA_INT, ADDRESS, ADDRESS));
    private static final MethodHandle MH_lock =
        Ffi.downcallOrNull("daqUserLock_lock", FunctionDescriptor.of(JAVA_INT, ADDRESS, ADDRESS));
    private static final MethodHandle MH_unlock =
        Ffi.downcallOrNull("daqUserLock_unlock", FunctionDescriptor.of(JAVA_INT, ADDRESS, ADDRESS));

    /** Creates an unlocked UserLock object.  Calls {@code daqUserLock_createUserLock()}. */
    public static MemorySegment createUserLock() {
        try (Arena arena = Arena.ofConfined()) {
            MemorySegment objSlot = arena.allocate(ADDRESS);
            int err = (int) Ffi.require(MH_createUserLock, "daqUserLock_createUserLock").invokeExact(objSlot);
            Ffi.checkError(err, "daqUserLock_createUserLock");
            return objSlot.get(ADDRESS, 0);
        } catch (Throwable t) { throw Ffi.rethrow(t); }
    }

    /** Forcefully unlock the object. A force unlock will always succeed, regardless of which user initially locked the object.  Calls {@code daqUserLock_forceUnlock()}. */
    public static void forceUnlock(MemorySegment self) {
        try {
            int err = (int) Ffi.require(MH_forceUnlock, "daqUserLock_forceUnlock").invokeExact(Ffi.orNull(self));
            Ffi.checkError(err, "daqUserLock_forceUnlock");
        } catch (Throwable t) { throw Ffi.rethrow(t); }
    }

    /** Calls {@code daqUserLock_getInterfaceId()} with caller-allocated out slots. */
    public static void getInterfaceId(MemorySegment intfId) {
        try {
            Ffi.require(MH_getInterfaceId, "daqUserLock_getInterfaceId").invokeExact(Ffi.orNull(intfId));
        } catch (Throwable t) { throw Ffi.rethrow(t); }
    }

    /** Returns true if the object is locked.  Calls {@code daqUserLock_isLocked()}. */
    public static boolean isLocked(MemorySegment self) {
        try (Arena arena = Arena.ofConfined()) {
            MemorySegment isLockedOutSlot = arena.allocate(JAVA_BYTE);
            int err = (int) Ffi.require(MH_isLocked, "daqUserLock_isLocked").invokeExact(Ffi.orNull(self), isLockedOutSlot);
            Ffi.checkError(err, "daqUserLock_isLocked");
            return isLockedOutSlot.get(JAVA_BYTE, 0) != 0;
        } catch (Throwable t) { throw Ffi.rethrow(t); }
    }

    /** Lock the object. Only the user who locked the object can unlock it. If the object was locked without a specific user, or with an anonymous user, any user can unlock it.  Calls {@code daqUserLock_lock()}. */
    public static void lock(MemorySegment self, MemorySegment user) {
        try {
            int err = (int) Ffi.require(MH_lock, "daqUserLock_lock").invokeExact(Ffi.orNull(self), Ffi.orNull(user));
            Ffi.checkError(err, "daqUserLock_lock");
        } catch (Throwable t) { throw Ffi.rethrow(t); }
    }

    /** Unlock the object. Only the user who locked the object can unlock it. If the object was locked without a specific user, or with an anonymous user, any user can unlock it.  Calls {@code daqUserLock_unlock()}. */
    public static void unlock(MemorySegment self, MemorySegment user) {
        try {
            int err = (int) Ffi.require(MH_unlock, "daqUserLock_unlock").invokeExact(Ffi.orNull(self), Ffi.orNull(user));
            Ffi.checkError(err, "daqUserLock_unlock");
        } catch (Throwable t) { throw Ffi.rethrow(t); }
    }

}
