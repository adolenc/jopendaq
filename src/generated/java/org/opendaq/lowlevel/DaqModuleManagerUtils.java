// This file is autogenerated by tools/generate_low_level_bindings.py. Do not edit.

package org.opendaq.lowlevel;

import java.lang.foreign.Arena;
import java.lang.foreign.FunctionDescriptor;
import java.lang.foreign.MemorySegment;
import java.lang.invoke.MethodHandle;

import static java.lang.foreign.ValueLayout.*;

/** Raw FFI bindings for the daqModuleManagerUtils_* functions of the openDAQ C API. */
public final class DaqModuleManagerUtils {

    private DaqModuleManagerUtils() {}

    private static final MethodHandle MH_changeIpConfig =
        Ffi.downcallOrNull("daqModuleManagerUtils_changeIpConfig", FunctionDescriptor.of(JAVA_INT, ADDRESS, ADDRESS, ADDRESS, ADDRESS, ADDRESS));
    private static final MethodHandle MH_completeDeviceCapabilities =
        Ffi.downcallOrNull("daqModuleManagerUtils_completeDeviceCapabilities", FunctionDescriptor.of(JAVA_INT, ADDRESS, ADDRESS));
    private static final MethodHandle MH_createDefaultAddDeviceConfig =
        Ffi.downcallOrNull("daqModuleManagerUtils_createDefaultAddDeviceConfig", FunctionDescriptor.of(JAVA_INT, ADDRESS, ADDRESS));
    private static final MethodHandle MH_createDevice =
        Ffi.downcallOrNull("daqModuleManagerUtils_createDevice", FunctionDescriptor.of(JAVA_INT, ADDRESS, ADDRESS, ADDRESS, ADDRESS, ADDRESS));
    private static final MethodHandle MH_createDevices =
        Ffi.downcallOrNull("daqModuleManagerUtils_createDevices", FunctionDescriptor.of(JAVA_INT, ADDRESS, ADDRESS, ADDRESS, ADDRESS, ADDRESS, ADDRESS));
    private static final MethodHandle MH_createFunctionBlock =
        Ffi.downcallOrNull("daqModuleManagerUtils_createFunctionBlock", FunctionDescriptor.of(JAVA_INT, ADDRESS, ADDRESS, ADDRESS, ADDRESS, ADDRESS, ADDRESS));
    private static final MethodHandle MH_createServer =
        Ffi.downcallOrNull("daqModuleManagerUtils_createServer", FunctionDescriptor.of(JAVA_INT, ADDRESS, ADDRESS, ADDRESS, ADDRESS, ADDRESS));
    private static final MethodHandle MH_createStreaming =
        Ffi.downcallOrNull("daqModuleManagerUtils_createStreaming", FunctionDescriptor.of(JAVA_INT, ADDRESS, ADDRESS, ADDRESS, ADDRESS));
    private static final MethodHandle MH_getAvailableDeviceTypes =
        Ffi.downcallOrNull("daqModuleManagerUtils_getAvailableDeviceTypes", FunctionDescriptor.of(JAVA_INT, ADDRESS, ADDRESS));
    private static final MethodHandle MH_getAvailableDevices =
        Ffi.downcallOrNull("daqModuleManagerUtils_getAvailableDevices", FunctionDescriptor.of(JAVA_INT, ADDRESS, ADDRESS));
    private static final MethodHandle MH_getAvailableFunctionBlockTypes =
        Ffi.downcallOrNull("daqModuleManagerUtils_getAvailableFunctionBlockTypes", FunctionDescriptor.of(JAVA_INT, ADDRESS, ADDRESS));
    private static final MethodHandle MH_getAvailableStreamingTypes =
        Ffi.downcallOrNull("daqModuleManagerUtils_getAvailableStreamingTypes", FunctionDescriptor.of(JAVA_INT, ADDRESS, ADDRESS));
    private static final MethodHandle MH_getDiscoveryInfo =
        Ffi.downcallOrNull("daqModuleManagerUtils_getDiscoveryInfo", FunctionDescriptor.of(JAVA_INT, ADDRESS, ADDRESS, ADDRESS, ADDRESS));
    private static final MethodHandle MH_getInterfaceId =
        Ffi.downcallOrNull("daqModuleManagerUtils_getInterfaceId", FunctionDescriptor.ofVoid(ADDRESS));
    private static final MethodHandle MH_requestIpConfig =
        Ffi.downcallOrNull("daqModuleManagerUtils_requestIpConfig", FunctionDescriptor.of(JAVA_INT, ADDRESS, ADDRESS, ADDRESS, ADDRESS, ADDRESS));

    /** Initiates the modification of IP configuration parameters for a specified network interface associated with a target device.  Calls {@code daqModuleManagerUtils_changeIpConfig()}. */
    public static void changeIpConfig(MemorySegment self, MemorySegment iface, MemorySegment manufacturer, MemorySegment serialNumber, MemorySegment config) {
        try {
            int err = (int) Ffi.require(MH_changeIpConfig, "daqModuleManagerUtils_changeIpConfig").invokeExact(Ffi.orNull(self), Ffi.orNull(iface), Ffi.orNull(manufacturer), Ffi.orNull(serialNumber), Ffi.orNull(config));
            Ffi.checkError(err, "daqModuleManagerUtils_changeIpConfig");
        } catch (Throwable t) { throw Ffi.rethrow(t); }
    }

    /** Completes the DeviceInfo's ServerCapabilities of existing device with the information obtained from device discovery.  Calls {@code daqModuleManagerUtils_completeDeviceCapabilities()}. */
    public static void completeDeviceCapabilities(MemorySegment self, MemorySegment device) {
        try {
            int err = (int) Ffi.require(MH_completeDeviceCapabilities, "daqModuleManagerUtils_completeDeviceCapabilities").invokeExact(Ffi.orNull(self), Ffi.orNull(device));
            Ffi.checkError(err, "daqModuleManagerUtils_completeDeviceCapabilities");
        } catch (Throwable t) { throw Ffi.rethrow(t); }
    }

    /** Calls {@code daqModuleManagerUtils_createDefaultAddDeviceConfig()}. */
    public static MemorySegment createDefaultAddDeviceConfig(MemorySegment self) {
        try (Arena arena = Arena.ofConfined()) {
            MemorySegment defaultConfigSlot = arena.allocate(ADDRESS);
            int err = (int) Ffi.require(MH_createDefaultAddDeviceConfig, "daqModuleManagerUtils_createDefaultAddDeviceConfig").invokeExact(Ffi.orNull(self), defaultConfigSlot);
            Ffi.checkError(err, "daqModuleManagerUtils_createDefaultAddDeviceConfig");
            return defaultConfigSlot.get(ADDRESS, 0);
        } catch (Throwable t) { throw Ffi.rethrow(t); }
    }

    /** Creates a device object that can communicate with the device described in the specified connection string. The device object is not automatically added as a sub-device of the caller, but only returned by reference.  Calls {@code daqModuleManagerUtils_createDevice()}. */
    public static MemorySegment createDevice(MemorySegment self, MemorySegment connectionString, MemorySegment parent, MemorySegment config) {
        try (Arena arena = Arena.ofConfined()) {
            MemorySegment deviceSlot = arena.allocate(ADDRESS);
            int err = (int) Ffi.require(MH_createDevice, "daqModuleManagerUtils_createDevice").invokeExact(Ffi.orNull(self), deviceSlot, Ffi.orNull(connectionString), Ffi.orNull(parent), Ffi.orNull(config));
            Ffi.checkError(err, "daqModuleManagerUtils_createDevice");
            return deviceSlot.get(ADDRESS, 0);
        } catch (Throwable t) { throw Ffi.rethrow(t); }
    }

    /** Creates multiple device objects in parallel using the specified connection strings. Each device is created concurrently. None of the created device object are automatically added as a sub-device of the caller, but only returned by reference.  Calls {@code daqModuleManagerUtils_createDevices()}. */
    public static MemorySegment createDevices(MemorySegment self, MemorySegment connectionArgs, MemorySegment parent, MemorySegment errCodes, MemorySegment errorInfos) {
        try (Arena arena = Arena.ofConfined()) {
            MemorySegment devicesSlot = arena.allocate(ADDRESS);
            int err = (int) Ffi.require(MH_createDevices, "daqModuleManagerUtils_createDevices").invokeExact(Ffi.orNull(self), devicesSlot, Ffi.orNull(connectionArgs), Ffi.orNull(parent), Ffi.orNull(errCodes), Ffi.orNull(errorInfos));
            Ffi.checkError(err, "daqModuleManagerUtils_createDevices");
            return devicesSlot.get(ADDRESS, 0);
        } catch (Throwable t) { throw Ffi.rethrow(t); }
    }

    /** Creates and returns a function block with the specified id. The function block is not automatically added to the FB list of the caller.  Calls {@code daqModuleManagerUtils_createFunctionBlock()}. */
    public static MemorySegment createFunctionBlock(MemorySegment self, MemorySegment id, MemorySegment parent, MemorySegment config, MemorySegment localId) {
        try (Arena arena = Arena.ofConfined()) {
            MemorySegment functionBlockSlot = arena.allocate(ADDRESS);
            int err = (int) Ffi.require(MH_createFunctionBlock, "daqModuleManagerUtils_createFunctionBlock").invokeExact(Ffi.orNull(self), functionBlockSlot, Ffi.orNull(id), Ffi.orNull(parent), Ffi.orNull(config), Ffi.orNull(localId));
            Ffi.checkError(err, "daqModuleManagerUtils_createFunctionBlock");
            return functionBlockSlot.get(ADDRESS, 0);
        } catch (Throwable t) { throw Ffi.rethrow(t); }
    }

    /** Creates and returns a server with the provided serverType and configuration.  Calls {@code daqModuleManagerUtils_createServer()}. */
    public static MemorySegment createServer(MemorySegment self, MemorySegment serverTypeId, MemorySegment rootDevice, MemorySegment serverConfig) {
        try (Arena arena = Arena.ofConfined()) {
            MemorySegment serverSlot = arena.allocate(ADDRESS);
            int err = (int) Ffi.require(MH_createServer, "daqModuleManagerUtils_createServer").invokeExact(Ffi.orNull(self), serverSlot, Ffi.orNull(serverTypeId), Ffi.orNull(rootDevice), Ffi.orNull(serverConfig));
            Ffi.checkError(err, "daqModuleManagerUtils_createServer");
            return serverSlot.get(ADDRESS, 0);
        } catch (Throwable t) { throw Ffi.rethrow(t); }
    }

    /** Creates a streaming object using the specified connection string and config object.  Calls {@code daqModuleManagerUtils_createStreaming()}. */
    public static MemorySegment createStreaming(MemorySegment self, MemorySegment connectionString, MemorySegment config) {
        try (Arena arena = Arena.ofConfined()) {
            MemorySegment streamingSlot = arena.allocate(ADDRESS);
            int err = (int) Ffi.require(MH_createStreaming, "daqModuleManagerUtils_createStreaming").invokeExact(Ffi.orNull(self), streamingSlot, Ffi.orNull(connectionString), Ffi.orNull(config));
            Ffi.checkError(err, "daqModuleManagerUtils_createStreaming");
            return streamingSlot.get(ADDRESS, 0);
        } catch (Throwable t) { throw Ffi.rethrow(t); }
    }

    /** Returns a dictionary of known and available device types this module can create.  Calls {@code daqModuleManagerUtils_getAvailableDeviceTypes()}. */
    public static MemorySegment getAvailableDeviceTypes(MemorySegment self) {
        try (Arena arena = Arena.ofConfined()) {
            MemorySegment deviceTypesSlot = arena.allocate(ADDRESS);
            int err = (int) Ffi.require(MH_getAvailableDeviceTypes, "daqModuleManagerUtils_getAvailableDeviceTypes").invokeExact(Ffi.orNull(self), deviceTypesSlot);
            Ffi.checkError(err, "daqModuleManagerUtils_getAvailableDeviceTypes");
            return deviceTypesSlot.get(ADDRESS, 0);
        } catch (Throwable t) { throw Ffi.rethrow(t); }
    }

    /** Returns a list of known devices info. The implementation can start discovery in background and only return the results in this function.  Calls {@code daqModuleManagerUtils_getAvailableDevices()}. */
    public static MemorySegment getAvailableDevices(MemorySegment self) {
        try (Arena arena = Arena.ofConfined()) {
            MemorySegment availableDevicesSlot = arena.allocate(ADDRESS);
            int err = (int) Ffi.require(MH_getAvailableDevices, "daqModuleManagerUtils_getAvailableDevices").invokeExact(Ffi.orNull(self), availableDevicesSlot);
            Ffi.checkError(err, "daqModuleManagerUtils_getAvailableDevices");
            return availableDevicesSlot.get(ADDRESS, 0);
        } catch (Throwable t) { throw Ffi.rethrow(t); }
    }

    /** Returns a dictionary of known and available function block types this module can create.  Calls {@code daqModuleManagerUtils_getAvailableFunctionBlockTypes()}. */
    public static MemorySegment getAvailableFunctionBlockTypes(MemorySegment self) {
        try (Arena arena = Arena.ofConfined()) {
            MemorySegment functionBlockTypesSlot = arena.allocate(ADDRESS);
            int err = (int) Ffi.require(MH_getAvailableFunctionBlockTypes, "daqModuleManagerUtils_getAvailableFunctionBlockTypes").invokeExact(Ffi.orNull(self), functionBlockTypesSlot);
            Ffi.checkError(err, "daqModuleManagerUtils_getAvailableFunctionBlockTypes");
            return functionBlockTypesSlot.get(ADDRESS, 0);
        } catch (Throwable t) { throw Ffi.rethrow(t); }
    }

    /** Calls {@code daqModuleManagerUtils_getAvailableStreamingTypes()}. */
    public static MemorySegment getAvailableStreamingTypes(MemorySegment self) {
        try (Arena arena = Arena.ofConfined()) {
            MemorySegment streamingTypesSlot = arena.allocate(ADDRESS);
            int err = (int) Ffi.require(MH_getAvailableStreamingTypes, "daqModuleManagerUtils_getAvailableStreamingTypes").invokeExact(Ffi.orNull(self), streamingTypesSlot);
            Ffi.checkError(err, "daqModuleManagerUtils_getAvailableStreamingTypes");
            return streamingTypesSlot.get(ADDRESS, 0);
        } catch (Throwable t) { throw Ffi.rethrow(t); }
    }

    /** Retrieves discovery information for a device identified by manufacturer and serial number.  Calls {@code daqModuleManagerUtils_getDiscoveryInfo()}. */
    public static MemorySegment getDiscoveryInfo(MemorySegment self, MemorySegment manufacturer, MemorySegment serialNumber) {
        try (Arena arena = Arena.ofConfined()) {
            MemorySegment deviceInfoSlot = arena.allocate(ADDRESS);
            int err = (int) Ffi.require(MH_getDiscoveryInfo, "daqModuleManagerUtils_getDiscoveryInfo").invokeExact(Ffi.orNull(self), deviceInfoSlot, Ffi.orNull(manufacturer), Ffi.orNull(serialNumber));
            Ffi.checkError(err, "daqModuleManagerUtils_getDiscoveryInfo");
            return deviceInfoSlot.get(ADDRESS, 0);
        } catch (Throwable t) { throw Ffi.rethrow(t); }
    }

    /** Calls {@code daqModuleManagerUtils_getInterfaceId()} with caller-allocated out slots. */
    public static void getInterfaceId(MemorySegment intfId) {
        try {
            Ffi.require(MH_getInterfaceId, "daqModuleManagerUtils_getInterfaceId").invokeExact(Ffi.orNull(intfId));
        } catch (Throwable t) { throw Ffi.rethrow(t); }
    }

    /** Attempts to retrieve the current IP configuration parameters for a specified network interface associated with a target device.  Calls {@code daqModuleManagerUtils_requestIpConfig()}. */
    public static MemorySegment requestIpConfig(MemorySegment self, MemorySegment iface, MemorySegment manufacturer, MemorySegment serialNumber) {
        try (Arena arena = Arena.ofConfined()) {
            MemorySegment configSlot = arena.allocate(ADDRESS);
            int err = (int) Ffi.require(MH_requestIpConfig, "daqModuleManagerUtils_requestIpConfig").invokeExact(Ffi.orNull(self), Ffi.orNull(iface), Ffi.orNull(manufacturer), Ffi.orNull(serialNumber), configSlot);
            Ffi.checkError(err, "daqModuleManagerUtils_requestIpConfig");
            return configSlot.get(ADDRESS, 0);
        } catch (Throwable t) { throw Ffi.rethrow(t); }
    }

}
