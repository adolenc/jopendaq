// This file is autogenerated by tools/generate_low_level_bindings.py. Do not edit.

package org.opendaq.lowlevel;

import java.lang.foreign.Arena;
import java.lang.foreign.FunctionDescriptor;
import java.lang.foreign.MemorySegment;
import java.lang.invoke.MethodHandle;

import static java.lang.foreign.ValueLayout.*;

/** Raw FFI bindings for the daqConnection_* functions of the openDAQ C API. */
public final class DaqConnection {

    private DaqConnection() {}

    private static final MethodHandle MH_createConnection =
        Ffi.downcallOrNull("daqConnection_createConnection", FunctionDescriptor.of(JAVA_INT, ADDRESS, ADDRESS, ADDRESS, ADDRESS));
    private static final MethodHandle MH_dequeue =
        Ffi.downcallOrNull("daqConnection_dequeue", FunctionDescriptor.of(JAVA_INT, ADDRESS, ADDRESS));
    private static final MethodHandle MH_dequeueAll =
        Ffi.downcallOrNull("daqConnection_dequeueAll", FunctionDescriptor.of(JAVA_INT, ADDRESS, ADDRESS));
    private static final MethodHandle MH_enqueue =
        Ffi.downcallOrNull("daqConnection_enqueue", FunctionDescriptor.of(JAVA_INT, ADDRESS, ADDRESS));
    private static final MethodHandle MH_enqueueAndStealRef =
        Ffi.downcallOrNull("daqConnection_enqueueAndStealRef", FunctionDescriptor.of(JAVA_INT, ADDRESS, ADDRESS));
    private static final MethodHandle MH_enqueueMultiple =
        Ffi.downcallOrNull("daqConnection_enqueueMultiple", FunctionDescriptor.of(JAVA_INT, ADDRESS, ADDRESS));
    private static final MethodHandle MH_enqueueMultipleAndStealRef =
        Ffi.downcallOrNull("daqConnection_enqueueMultipleAndStealRef", FunctionDescriptor.of(JAVA_INT, ADDRESS, ADDRESS));
    private static final MethodHandle MH_enqueueOnThisThread =
        Ffi.downcallOrNull("daqConnection_enqueueOnThisThread", FunctionDescriptor.of(JAVA_INT, ADDRESS, ADDRESS));
    private static final MethodHandle MH_enqueueWithScheduler =
        Ffi.downcallOrNull("daqConnection_enqueueWithScheduler", FunctionDescriptor.of(JAVA_INT, ADDRESS, ADDRESS));
    private static final MethodHandle MH_getAvailableSamples =
        Ffi.downcallOrNull("daqConnection_getAvailableSamples", FunctionDescriptor.of(JAVA_INT, ADDRESS, ADDRESS));
    private static final MethodHandle MH_getInputPort =
        Ffi.downcallOrNull("daqConnection_getInputPort", FunctionDescriptor.of(JAVA_INT, ADDRESS, ADDRESS));
    private static final MethodHandle MH_getInterfaceId =
        Ffi.downcallOrNull("daqConnection_getInterfaceId", FunctionDescriptor.ofVoid(ADDRESS));
    private static final MethodHandle MH_getPacketCount =
        Ffi.downcallOrNull("daqConnection_getPacketCount", FunctionDescriptor.of(JAVA_INT, ADDRESS, ADDRESS));
    private static final MethodHandle MH_getSamplesUntilNextDescriptor =
        Ffi.downcallOrNull("daqConnection_getSamplesUntilNextDescriptor", FunctionDescriptor.of(JAVA_INT, ADDRESS, ADDRESS));
    private static final MethodHandle MH_getSamplesUntilNextEventPacket =
        Ffi.downcallOrNull("daqConnection_getSamplesUntilNextEventPacket", FunctionDescriptor.of(JAVA_INT, ADDRESS, ADDRESS));
    private static final MethodHandle MH_getSamplesUntilNextGapPacket =
        Ffi.downcallOrNull("daqConnection_getSamplesUntilNextGapPacket", FunctionDescriptor.of(JAVA_INT, ADDRESS, ADDRESS));
    private static final MethodHandle MH_getSignal =
        Ffi.downcallOrNull("daqConnection_getSignal", FunctionDescriptor.of(JAVA_INT, ADDRESS, ADDRESS));
    private static final MethodHandle MH_hasEventPacket =
        Ffi.downcallOrNull("daqConnection_hasEventPacket", FunctionDescriptor.of(JAVA_INT, ADDRESS, ADDRESS));
    private static final MethodHandle MH_hasGapPacket =
        Ffi.downcallOrNull("daqConnection_hasGapPacket", FunctionDescriptor.of(JAVA_INT, ADDRESS, ADDRESS));
    private static final MethodHandle MH_isRemote =
        Ffi.downcallOrNull("daqConnection_isRemote", FunctionDescriptor.of(JAVA_INT, ADDRESS, ADDRESS));
    private static final MethodHandle MH_peek =
        Ffi.downcallOrNull("daqConnection_peek", FunctionDescriptor.of(JAVA_INT, ADDRESS, ADDRESS));

    /** Calls {@code daqConnection_createConnection()}. */
    public static MemorySegment createConnection(MemorySegment inputPort, MemorySegment signal, MemorySegment context) {
        try (Arena arena = Arena.ofConfined()) {
            MemorySegment objSlot = arena.allocate(ADDRESS);
            int err = (int) Ffi.require(MH_createConnection, "daqConnection_createConnection").invokeExact(objSlot, Ffi.orNull(inputPort), Ffi.orNull(signal), Ffi.orNull(context));
            Ffi.checkError(err, "daqConnection_createConnection");
            return objSlot.get(ADDRESS, 0);
        } catch (Throwable t) { throw Ffi.rethrow(t); }
    }

    /** Removes the packet at the front of the queue and returns it.  Calls {@code daqConnection_dequeue()}. */
    public static MemorySegment dequeue(MemorySegment self) {
        try (Arena arena = Arena.ofConfined()) {
            MemorySegment packetSlot = arena.allocate(ADDRESS);
            int err = (int) Ffi.require(MH_dequeue, "daqConnection_dequeue").invokeExact(Ffi.orNull(self), packetSlot);
            Ffi.checkError(err, "daqConnection_dequeue");
            return packetSlot.get(ADDRESS, 0);
        } catch (Throwable t) { throw Ffi.rethrow(t); }
    }

    /** Removes all packets from the queue.  Calls {@code daqConnection_dequeueAll()}. */
    public static MemorySegment dequeueAll(MemorySegment self) {
        try (Arena arena = Arena.ofConfined()) {
            MemorySegment packetsSlot = arena.allocate(ADDRESS);
            int err = (int) Ffi.require(MH_dequeueAll, "daqConnection_dequeueAll").invokeExact(Ffi.orNull(self), packetsSlot);
            Ffi.checkError(err, "daqConnection_dequeueAll");
            return packetsSlot.get(ADDRESS, 0);
        } catch (Throwable t) { throw Ffi.rethrow(t); }
    }

    /** Places a packet at the back of the queue.  Calls {@code daqConnection_enqueue()}. */
    public static void enqueue(MemorySegment self, MemorySegment packet) {
        try {
            int err = (int) Ffi.require(MH_enqueue, "daqConnection_enqueue").invokeExact(Ffi.orNull(self), Ffi.orNull(packet));
            Ffi.checkError(err, "daqConnection_enqueue");
        } catch (Throwable t) { throw Ffi.rethrow(t); }
    }

    /** Places a packet at the back of the queue. The reference of the packet is stolen.  Calls {@code daqConnection_enqueueAndStealRef()}. */
    public static void enqueueAndStealRef(MemorySegment self, MemorySegment packet) {
        try {
            int err = (int) Ffi.require(MH_enqueueAndStealRef, "daqConnection_enqueueAndStealRef").invokeExact(Ffi.orNull(self), Ffi.orNull(packet));
            Ffi.checkError(err, "daqConnection_enqueueAndStealRef");
        } catch (Throwable t) { throw Ffi.rethrow(t); }
    }

    /** Places multiple packets at the back of the queue.  Calls {@code daqConnection_enqueueMultiple()}. */
    public static void enqueueMultiple(MemorySegment self, MemorySegment packets) {
        try {
            int err = (int) Ffi.require(MH_enqueueMultiple, "daqConnection_enqueueMultiple").invokeExact(Ffi.orNull(self), Ffi.orNull(packets));
            Ffi.checkError(err, "daqConnection_enqueueMultiple");
        } catch (Throwable t) { throw Ffi.rethrow(t); }
    }

    /** Places multiple packets at the back of the queue. The references of the packets are stolen.  Calls {@code daqConnection_enqueueMultipleAndStealRef()}. */
    public static void enqueueMultipleAndStealRef(MemorySegment self, MemorySegment packets) {
        try {
            int err = (int) Ffi.require(MH_enqueueMultipleAndStealRef, "daqConnection_enqueueMultipleAndStealRef").invokeExact(Ffi.orNull(self), Ffi.orNull(packets));
            Ffi.checkError(err, "daqConnection_enqueueMultipleAndStealRef");
        } catch (Throwable t) { throw Ffi.rethrow(t); }
    }

    /** Places a packet at the back of the queue.  Calls {@code daqConnection_enqueueOnThisThread()}. */
    public static void enqueueOnThisThread(MemorySegment self, MemorySegment packet) {
        try {
            int err = (int) Ffi.require(MH_enqueueOnThisThread, "daqConnection_enqueueOnThisThread").invokeExact(Ffi.orNull(self), Ffi.orNull(packet));
            Ffi.checkError(err, "daqConnection_enqueueOnThisThread");
        } catch (Throwable t) { throw Ffi.rethrow(t); }
    }

    /** Places a packet at the back of the queue.  Calls {@code daqConnection_enqueueWithScheduler()}. */
    public static void enqueueWithScheduler(MemorySegment self, MemorySegment packet) {
        try {
            int err = (int) Ffi.require(MH_enqueueWithScheduler, "daqConnection_enqueueWithScheduler").invokeExact(Ffi.orNull(self), Ffi.orNull(packet));
            Ffi.checkError(err, "daqConnection_enqueueWithScheduler");
        } catch (Throwable t) { throw Ffi.rethrow(t); }
    }

    /** Gets the number of samples available in the queued packets. The returned value ignores any Sample-Descriptor changes.  Calls {@code daqConnection_getAvailableSamples()}. */
    public static long getAvailableSamples(MemorySegment self) {
        try (Arena arena = Arena.ofConfined()) {
            MemorySegment samplesSlot = arena.allocate(JAVA_LONG);
            int err = (int) Ffi.require(MH_getAvailableSamples, "daqConnection_getAvailableSamples").invokeExact(Ffi.orNull(self), samplesSlot);
            Ffi.checkError(err, "daqConnection_getAvailableSamples");
            return samplesSlot.get(JAVA_LONG, 0);
        } catch (Throwable t) { throw Ffi.rethrow(t); }
    }

    /** Gets the Input port to which packets are being sent.  Calls {@code daqConnection_getInputPort()}. */
    public static MemorySegment getInputPort(MemorySegment self) {
        try (Arena arena = Arena.ofConfined()) {
            MemorySegment inputPortSlot = arena.allocate(ADDRESS);
            int err = (int) Ffi.require(MH_getInputPort, "daqConnection_getInputPort").invokeExact(Ffi.orNull(self), inputPortSlot);
            Ffi.checkError(err, "daqConnection_getInputPort");
            return inputPortSlot.get(ADDRESS, 0);
        } catch (Throwable t) { throw Ffi.rethrow(t); }
    }

    /** Calls {@code daqConnection_getInterfaceId()} with caller-allocated out slots. */
    public static void getInterfaceId(MemorySegment intfId) {
        try {
            Ffi.require(MH_getInterfaceId, "daqConnection_getInterfaceId").invokeExact(Ffi.orNull(intfId));
        } catch (Throwable t) { throw Ffi.rethrow(t); }
    }

    /** Gets the number of queued packets.  Calls {@code daqConnection_getPacketCount()}. */
    public static long getPacketCount(MemorySegment self) {
        try (Arena arena = Arena.ofConfined()) {
            MemorySegment packetCountSlot = arena.allocate(JAVA_LONG);
            int err = (int) Ffi.require(MH_getPacketCount, "daqConnection_getPacketCount").invokeExact(Ffi.orNull(self), packetCountSlot);
            Ffi.checkError(err, "daqConnection_getPacketCount");
            return packetCountSlot.get(JAVA_LONG, 0);
        } catch (Throwable t) { throw Ffi.rethrow(t); }
    }

    /** Gets the number of same-type samples available in the queued packets. The returned value is up-to the next Sample-Descriptor-Changed packet if any.  Calls {@code daqConnection_getSamplesUntilNextDescriptor()}. */
    public static long getSamplesUntilNextDescriptor(MemorySegment self) {
        try (Arena arena = Arena.ofConfined()) {
            MemorySegment samplesSlot = arena.allocate(JAVA_LONG);
            int err = (int) Ffi.require(MH_getSamplesUntilNextDescriptor, "daqConnection_getSamplesUntilNextDescriptor").invokeExact(Ffi.orNull(self), samplesSlot);
            Ffi.checkError(err, "daqConnection_getSamplesUntilNextDescriptor");
            return samplesSlot.get(JAVA_LONG, 0);
        } catch (Throwable t) { throw Ffi.rethrow(t); }
    }

    /** Gets the number of samples available in the queued packets until the next event packet. The returned value is up-to the next Event packet if any.  Calls {@code daqConnection_getSamplesUntilNextEventPacket()}. */
    public static long getSamplesUntilNextEventPacket(MemorySegment self) {
        try (Arena arena = Arena.ofConfined()) {
            MemorySegment samplesSlot = arena.allocate(JAVA_LONG);
            int err = (int) Ffi.require(MH_getSamplesUntilNextEventPacket, "daqConnection_getSamplesUntilNextEventPacket").invokeExact(Ffi.orNull(self), samplesSlot);
            Ffi.checkError(err, "daqConnection_getSamplesUntilNextEventPacket");
            return samplesSlot.get(JAVA_LONG, 0);
        } catch (Throwable t) { throw Ffi.rethrow(t); }
    }

    /** Gets the number of samples available in the queued packets until the next gap packet. The returned value is up-to the next Gap packet if any.  Calls {@code daqConnection_getSamplesUntilNextGapPacket()}. */
    public static long getSamplesUntilNextGapPacket(MemorySegment self) {
        try (Arena arena = Arena.ofConfined()) {
            MemorySegment samplesSlot = arena.allocate(JAVA_LONG);
            int err = (int) Ffi.require(MH_getSamplesUntilNextGapPacket, "daqConnection_getSamplesUntilNextGapPacket").invokeExact(Ffi.orNull(self), samplesSlot);
            Ffi.checkError(err, "daqConnection_getSamplesUntilNextGapPacket");
            return samplesSlot.get(JAVA_LONG, 0);
        } catch (Throwable t) { throw Ffi.rethrow(t); }
    }

    /** Gets the Signal that is sending packets through the Connection.  Calls {@code daqConnection_getSignal()}. */
    public static MemorySegment getSignal(MemorySegment self) {
        try (Arena arena = Arena.ofConfined()) {
            MemorySegment signalSlot = arena.allocate(ADDRESS);
            int err = (int) Ffi.require(MH_getSignal, "daqConnection_getSignal").invokeExact(Ffi.orNull(self), signalSlot);
            Ffi.checkError(err, "daqConnection_getSignal");
            return signalSlot.get(ADDRESS, 0);
        } catch (Throwable t) { throw Ffi.rethrow(t); }
    }

    /** Queries if the connection has an event packet.  Calls {@code daqConnection_hasEventPacket()}. */
    public static boolean hasEventPacket(MemorySegment self) {
        try (Arena arena = Arena.ofConfined()) {
            MemorySegment hasEventPacketSlot = arena.allocate(JAVA_BYTE);
            int err = (int) Ffi.require(MH_hasEventPacket, "daqConnection_hasEventPacket").invokeExact(Ffi.orNull(self), hasEventPacketSlot);
            Ffi.checkError(err, "daqConnection_hasEventPacket");
            return hasEventPacketSlot.get(JAVA_BYTE, 0) != 0;
        } catch (Throwable t) { throw Ffi.rethrow(t); }
    }

    /** Queries if the connection has a gap packet.  Calls {@code daqConnection_hasGapPacket()}. */
    public static boolean hasGapPacket(MemorySegment self) {
        try (Arena arena = Arena.ofConfined()) {
            MemorySegment hasGapPacketSlot = arena.allocate(JAVA_BYTE);
            int err = (int) Ffi.require(MH_hasGapPacket, "daqConnection_hasGapPacket").invokeExact(Ffi.orNull(self), hasGapPacketSlot);
            Ffi.checkError(err, "daqConnection_hasGapPacket");
            return hasGapPacketSlot.get(JAVA_BYTE, 0) != 0;
        } catch (Throwable t) { throw Ffi.rethrow(t); }
    }

    /** Returns true if the type of connection is remote.  Calls {@code daqConnection_isRemote()}. */
    public static boolean isRemote(MemorySegment self) {
        try (Arena arena = Arena.ofConfined()) {
            MemorySegment remoteSlot = arena.allocate(JAVA_BYTE);
            int err = (int) Ffi.require(MH_isRemote, "daqConnection_isRemote").invokeExact(Ffi.orNull(self), remoteSlot);
            Ffi.checkError(err, "daqConnection_isRemote");
            return remoteSlot.get(JAVA_BYTE, 0) != 0;
        } catch (Throwable t) { throw Ffi.rethrow(t); }
    }

    /** Returns the packet at the front of the queue without removing it.  Calls {@code daqConnection_peek()}. */
    public static MemorySegment peek(MemorySegment self) {
        try (Arena arena = Arena.ofConfined()) {
            MemorySegment packetSlot = arena.allocate(ADDRESS);
            int err = (int) Ffi.require(MH_peek, "daqConnection_peek").invokeExact(Ffi.orNull(self), packetSlot);
            Ffi.checkError(err, "daqConnection_peek");
            return packetSlot.get(ADDRESS, 0);
        } catch (Throwable t) { throw Ffi.rethrow(t); }
    }

}
