// This file is autogenerated by tools/generate_high_level_bindings.py. Do not edit.

package org.opendaq;

import java.lang.foreign.Arena;
import java.lang.foreign.MemorySegment;
import java.lang.foreign.ValueLayout;
import java.lang.ref.Reference;
import java.util.List;
import java.util.Map;

import org.opendaq.lowlevel.*;

/**
 * Used by openDAQ properties to coerce a value to match the restrictions imposed by the Property.
 * Whenever a value is set to on a Property object, if the corresponding Property has a coercer configured, the value will
 * be evaluated and modified to fit the restrictions imposed by the coercer. For example, a coercer can enforce lower-than,
 * greater-than, equality, or other number relations on written values.
 * The coercion conditions are configured with an evaluation string when the coercer is constructed. The string constructs an
 * Eval value that replaces any instance of the keyword "value" or "val" with the value being set. The result of the Eval
 * value evaluation is the output of the `coerce` function call. For example, coercers created with the string
 * "if(value &gt; 5, 5, value)" would enforce that the property value is always equal to or lower than 5.
 *
 * &lt;p&gt;Wraps the openDAQ {&#64;code daqCoercer} interface.
 */
public class Coercer extends BaseObject {

    /** Adopt an owned reference (see {@link DaqObject#DaqObject}). */
    public Coercer(MemorySegment pointer) {
        super(pointer);
    }

    /**
     * Creates a Coercer with the given evaluation expression.
     * &#64;param eval The evaluation expression used for coercion.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqCoercer_createCoercer()}.
     */
    public Coercer(String eval) {
        super(create$createCoercer(eval));
    }

    private static MemorySegment create$createCoercer(String eval) {
        MemorySegment eval$s = Wrap.daqStringOrNull(eval);
        try {
            return DaqCoercer.createCoercer(eval$s);
        } finally { Ffi.releaseRef(eval$s); }
    }

    /**
     * Coerces `value` to match the coercion restrictions and outputs the result.
     * &#64;param propObj Optional property object parameter required if the coercion depends on other properties of the Property object.
     * &#64;param value The value to be coerced to fit the restrictions.
     * &#64;param[out] result The coercer output containing the modified `value`.
     * &#64;retval OPENDAQ_ERR_COERCE_FAILED if `value` cannot be coerced to fit the restrictions.
     * &#64;retval OPENDAQ_SUCCESS If the value either already fits the restrictions, or was successfully modified to do so.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqCoercer_coerce()}.
     */
    public BaseObject coerce(Object propObj, Object value) {
        try {
            Box.Boxed propObj$b = Box.box(propObj);
            try {
                Box.Boxed value$b = Box.box(value);
                try {
                    return DaqObject.wrap(DaqCoercer.coerce(requireLivePointer(), propObj$b.segment(), value$b.segment()), BaseObject.class);
                } finally { Box.releaseBoxed(value$b); }
            } finally { Box.releaseBoxed(propObj$b); }
        } finally {
            Reference.reachabilityFence(this);
        }
    }

    /**
     * Calls the openDAQ C function {&#64;code daqCoercer_coerceNoLock()}.
     */
    public BaseObject coerceNoLock(Object propObj, Object value) {
        try {
            Box.Boxed propObj$b = Box.box(propObj);
            try {
                Box.Boxed value$b = Box.box(value);
                try {
                    return DaqObject.wrap(DaqCoercer.coerceNoLock(requireLivePointer(), propObj$b.segment(), value$b.segment()), BaseObject.class);
                } finally { Box.releaseBoxed(value$b); }
            } finally { Box.releaseBoxed(propObj$b); }
        } finally {
            Reference.reachabilityFence(this);
        }
    }

    /**
     * Gets the string expression used when creating the coercer.
     * &#64;param[out] eval The coercion expression.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqCoercer_getEval()}.
     */
    public String getEval() {
        try {
            return Ffi.readDaqStringAndRelease(DaqCoercer.getEval(requireLivePointer()));
        } finally {
            Reference.reachabilityFence(this);
        }
    }

}
