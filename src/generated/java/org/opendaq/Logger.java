// This file is autogenerated by tools/generate_high_level_bindings.py. Do not edit.

package org.opendaq;

import java.lang.foreign.Arena;
import java.lang.foreign.MemorySegment;
import java.lang.foreign.ValueLayout;
import java.lang.ref.Reference;
import java.util.List;
import java.util.Map;

import org.opendaq.lowlevel.*;

/**
 * Represents a collection of &#64;ref ILoggerComponent "Logger Components" with multiple
 * &#64;ref ILoggerSink "Logger Sinks" and a single &#64;ref ILoggerThreadPool "Logger Thread Pool"
 * shared between components.
 * Logger is used to create, manage and maintain Logger Components associated with different parts of
 * the openDAQ SDK. The Logger provides methods, allowing for components to be added and removed dynamically.
 * The components added within the same Logger object should have unique names. Each newly added
 * component inherits threshold log severity level from the Logger. Then this level can be changed
 * independently per component. The set of sinks is initialized on the Logger object creation
 * and cannot be changed after.
 * Additionally, Logger provides the ability to manage flushing policies for the added components,
 * see `flushOnLevel` method.
 *
 * &lt;p&gt;Wraps the openDAQ {&#64;code daqLogger} interface.
 */
public class Logger extends BaseObject {

    /** Adopt an owned reference (see {@link DaqObject#DaqObject}). */
    public Logger(MemorySegment pointer) {
        super(pointer);
    }

    /**
     * Calls the openDAQ C function {&#64;code daqLogger_createLogger()}.
     */
    public Logger(java.util.List<?> sinks, LogLevel level) {
        super(create$createLogger(sinks, level));
    }

    private static MemorySegment create$createLogger(java.util.List<?> sinks, LogLevel level) {
        MemorySegment sinks$l = (sinks == null ? MemorySegment.NULL : Box.boxListOwned(sinks));
        try {
            return DaqLogger.createLogger(sinks$l, level);
        } finally { Ffi.releaseRef(sinks$l); }
    }

    /**
     * Creates a component with a given name and adds it to the Logger object.
     * &#64;param name The component's name.
     * &#64;param[out] component Added component.
     * &#64;retval OPENDAQ_ERR_INVALIDPARAMETER if &#64;p name is empty string.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqLogger_addComponent()}.
     */
    public LoggerComponent addComponent(String name) {
        try {
            MemorySegment name$s = Wrap.daqStringOrNull(name);
            try {
                return DaqObject.wrap(DaqLogger.addComponent(requireLivePointer(), name$s), LoggerComponent.class);
            } finally { Ffi.releaseRef(name$s); }
        } finally {
            Reference.reachabilityFence(this);
        }
    }

    /**
     * Triggers writing out the messages stored in temporary buffers for added components and sinks associated with the Logger object.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqLogger_flush()}.
     */
    public void flush() {
        try {
            DaqLogger.flush(requireLivePointer());
        } finally {
            Reference.reachabilityFence(this);
        }
    }

    /**
     * Sets the minimum severity level of messages to be automatically flushed by components of Logger object.
     * &#64;param level The log severity level.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqLogger_flushOnLevel()}.
     */
    public void flushOnLevel(LogLevel level) {
        try {
            DaqLogger.flushOnLevel(requireLivePointer(), level);
        } finally {
            Reference.reachabilityFence(this);
        }
    }

    /**
     * Gets an added component by name.
     * &#64;param name The component's name.
     * &#64;param[out] component The logger component with the name equal to &#64;p `name`.
     * &#64;retval OPENDAQ_ERR_NOTFOUND if a component with the specified &#64;p name was not added.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqLogger_getComponent()}.
     */
    public LoggerComponent getComponent(String name) {
        try {
            MemorySegment name$s = Wrap.daqStringOrNull(name);
            try {
                return DaqObject.wrap(DaqLogger.getComponent(requireLivePointer(), name$s), LoggerComponent.class);
            } finally { Ffi.releaseRef(name$s); }
        } finally {
            Reference.reachabilityFence(this);
        }
    }

    /**
     * Gets a list of added components.
     * &#64;param[out] components The list of added components.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqLogger_getComponents()}.
     */
    public java.util.List<LoggerComponent> getComponents() {
        try {
            return Wrap.list(DaqLogger.getComponents(requireLivePointer()), p -> Wrap.castOwned(p, LoggerComponent.class));
        } finally {
            Reference.reachabilityFence(this);
        }
    }

    /**
     * Gets the default log severity level.
     * &#64;param[out] level The log severity level.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqLogger_getLevel()}.
     */
    public LogLevel getLevel() {
        try {
            return DaqLogger.getLevel(requireLivePointer());
        } finally {
            Reference.reachabilityFence(this);
        }
    }

    /**
     * Gets an added component by name or creates a new one with a given name and adds it to the Logger object.
     * &#64;param name The component's name.
     * &#64;param[out] component The logger component with the name equal to &#64;p `name`.
     * &#64;retval OPENDAQ_ERR_INVALIDPARAMETER if &#64;p name is empty string.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqLogger_getOrAddComponent()}.
     */
    public LoggerComponent getOrAddComponent(String name) {
        try {
            MemorySegment name$s = Wrap.daqStringOrNull(name);
            try {
                return DaqObject.wrap(DaqLogger.getOrAddComponent(requireLivePointer(), name$s), LoggerComponent.class);
            } finally { Ffi.releaseRef(name$s); }
        } finally {
            Reference.reachabilityFence(this);
        }
    }

    /**
     * Removes the component with a given name from the Logger object.
     * &#64;param name The component's name.
     * &#64;retval OPENDAQ_ERR_NOTFOUND if a component with the specified &#64;p name was not added.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqLogger_removeComponent()}.
     */
    public void removeComponent(String name) {
        try {
            MemorySegment name$s = Wrap.daqStringOrNull(name);
            try {
                DaqLogger.removeComponent(requireLivePointer(), name$s);
            } finally { Ffi.releaseRef(name$s); }
        } finally {
            Reference.reachabilityFence(this);
        }
    }

    /**
     * Sets the default log severity level.
     * &#64;param level The log severity level.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqLogger_setLevel()}.
     */
    public void setLevel(LogLevel level) {
        try {
            DaqLogger.setLevel(requireLivePointer(), level);
        } finally {
            Reference.reachabilityFence(this);
        }
    }

}
