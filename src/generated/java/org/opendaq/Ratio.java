// This file is autogenerated by tools/generate_high_level_bindings.py. Do not edit.

package org.opendaq;

import java.lang.foreign.Arena;
import java.lang.foreign.MemorySegment;
import java.lang.foreign.ValueLayout;
import java.lang.ref.Reference;
import java.util.List;
import java.util.Map;

import org.opendaq.lowlevel.*;

/**
 * Represents rational number as `IRatio` interface. Use this interface to wrap
 * rational number when you need to add the number to lists, dictionaries and
 * other containers which accept `IBaseObject` and derived interfaces.
 * Rational numbers are defined as numerator / denominator.
 * Available factories:
 * &#64;code
 * // Creates a new Ratio object. Throws exception if not successful.
 * IRatio* Ratio_Create(Int numerator, Int denominator)
 * // Creates a new Ratio object. Returns error code if not successful.
 * ErrCode createRatio(IRatio** obj, Int numerator, Int denominator)
 * &#64;endcode
 *
 * &lt;p&gt;Wraps the openDAQ {&#64;code daqRatio} interface.
 */
public class Ratio extends BaseObject {

    /** Adopt an owned reference (see {@link DaqObject#DaqObject}). */
    public Ratio(MemorySegment pointer) {
        super(pointer);
    }

    /**
     * Calls the openDAQ C function {&#64;code daqRatio_createRatio()}.
     */
    public Ratio(long numerator, long denominator) {
        super(create$createRatio(numerator, denominator));
    }

    private static MemorySegment create$createRatio(long numerator, long denominator) {
        return DaqRatio.createRatio(numerator, denominator);
    }

    /**
     * Gets denominator part.
     * &#64;param[out] denominator Denominator value.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqRatio_getDenominator()}.
     */
    public long getDenominator() {
        try {
            return DaqRatio.getDenominator(requireLivePointer());
        } finally {
            Reference.reachabilityFence(this);
        }
    }

    /**
     * Gets numerator part.
     * &#64;param[out] numerator Numerator value.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqRatio_getNumerator()}.
     */
    public long getNumerator() {
        try {
            return DaqRatio.getNumerator(requireLivePointer());
        } finally {
            Reference.reachabilityFence(this);
        }
    }

    /**
     * Simplifies rational number if possible and returns the simplified ratio as a new object.
     * &#64;param[out] simplifiedRatio the simplified ratio.
     * Call this method to reduce stored rational number to the lowest terms possible.
     * Example: 10/100 is reduced to 1/10.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqRatio_simplify()}.
     */
    public Ratio simplify() {
        try {
            return DaqObject.wrap(DaqRatio.simplify(requireLivePointer()), Ratio.class);
        } finally {
            Reference.reachabilityFence(this);
        }
    }

}
