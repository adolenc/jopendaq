// This file is autogenerated by tools/generate_high_level_bindings.py. Do not edit.

package org.opendaq;

import java.lang.foreign.Arena;
import java.lang.foreign.MemorySegment;
import java.lang.foreign.ValueLayout;
import java.lang.ref.Reference;
import java.util.List;
import java.util.Map;

import org.opendaq.lowlevel.*;

/**
 * A module is an object that provides device and function block factories. The object is usually implemented in an external dynamic link / shared library. IModuleManager is responsible for loading all modules.
 *
 * &lt;p&gt;Wraps the openDAQ {&#64;code daqModule} interface.
 */
public class Module extends BaseObject {

    /** Adopt an owned reference (see {@link DaqObject#DaqObject}). */
    public Module(MemorySegment pointer) {
        super(pointer);
    }

    /**
     * Calls the openDAQ C function {&#64;code daqModule_completeServerCapability()}.
     */
    public boolean completeServerCapability(ServerCapability source, ServerCapabilityConfig target) {
        try {
            return DaqModule.completeServerCapability(requireLivePointer(), (source == null ? MemorySegment.NULL : source.requireLivePointer()), (target == null ? MemorySegment.NULL : target.requireLivePointer()));
        } finally {
            Reference.reachabilityFence(this);
            Reference.reachabilityFence(source);
            Reference.reachabilityFence(target);
        }
    }

    /**
     * Creates a device object that can communicate with the device described in the specified connection string. The device object is not automatically added as a sub-device of the caller, but only returned by reference.
     * &#64;param[out] device The device object created to communicate with and control the device.
     * &#64;param connectionString Describes the connection info of the device to connect to.
     * &#64;param parent The parent component/device to which the device attaches.
     * &#64;param config A configuration object that contains parameters used to configure a device in the form of key-value pairs.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqModule_createDevice()}.
     */
    public Device createDevice(String connectionString, Component parent, PropertyObject config) {
        try {
            MemorySegment connectionString$s = Wrap.daqStringOrNull(connectionString);
            try {
                return DaqObject.wrap(DaqModule.createDevice(requireLivePointer(), connectionString$s, (parent == null ? MemorySegment.NULL : parent.requireLivePointer()), (config == null ? MemorySegment.NULL : config.requireLivePointer())), Device.class);
            } finally { Ffi.releaseRef(connectionString$s); }
        } finally {
            Reference.reachabilityFence(this);
            Reference.reachabilityFence(parent);
            Reference.reachabilityFence(config);
        }
    }

    /** createDevice with trailing arguments at their openDAQ defaults. */
    public Device createDevice(String connectionString, Component parent) {
        return createDevice(connectionString, parent, (PropertyObject) null);
    }

    /**
     * Creates and returns a function block with the specified id. The function block is not automatically added to the FB list of the caller.
     * &#64;param id The id of the function block to create. Ids can be retrieved by calling `getAvailableFunctionBlockTypes()`.
     * &#64;param parent The parent component/device to which the device attaches.
     * &#64;param localId The local id of the function block.
     * &#64;param config Function block configuration. In case of a null value, implementation should use default configuration.
     * &#64;param[out] functionBlock The created function block.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqModule_createFunctionBlock()}.
     */
    public FunctionBlock createFunctionBlock(String id, Component parent, String localId, PropertyObject config) {
        try {
            MemorySegment id$s = Wrap.daqStringOrNull(id);
            try {
                MemorySegment localId$s = Wrap.daqStringOrNull(localId);
                try {
                    return DaqObject.wrap(DaqModule.createFunctionBlock(requireLivePointer(), id$s, (parent == null ? MemorySegment.NULL : parent.requireLivePointer()), localId$s, (config == null ? MemorySegment.NULL : config.requireLivePointer())), FunctionBlock.class);
                } finally { Ffi.releaseRef(localId$s); }
            } finally { Ffi.releaseRef(id$s); }
        } finally {
            Reference.reachabilityFence(this);
            Reference.reachabilityFence(parent);
            Reference.reachabilityFence(config);
        }
    }

    /** createFunctionBlock with trailing arguments at their openDAQ defaults. */
    public FunctionBlock createFunctionBlock(String id, Component parent, String localId) {
        return createFunctionBlock(id, parent, localId, (PropertyObject) null);
    }

    /**
     * Creates and returns a server with the specified server type.
     * &#64;param serverTypeId The id of the server type to create. ServerType can be retrieved by calling `getAvailableServerTypes()`.
     * &#64;param config Server configuration. In case of a null value, implementation should use default configuration.
     * &#64;param rootDevice Root device.
     * &#64;param[out] server The created server.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqModule_createServer()}.
     */
    public Server createServer(String serverTypeId, Device rootDevice, PropertyObject config) {
        try {
            MemorySegment serverTypeId$s = Wrap.daqStringOrNull(serverTypeId);
            try {
                return DaqObject.wrap(DaqModule.createServer(requireLivePointer(), serverTypeId$s, (rootDevice == null ? MemorySegment.NULL : rootDevice.requireLivePointer()), (config == null ? MemorySegment.NULL : config.requireLivePointer())), Server.class);
            } finally { Ffi.releaseRef(serverTypeId$s); }
        } finally {
            Reference.reachabilityFence(this);
            Reference.reachabilityFence(rootDevice);
            Reference.reachabilityFence(config);
        }
    }

    /** createServer with trailing arguments at their openDAQ defaults. */
    public Server createServer(String serverTypeId, Device rootDevice) {
        return createServer(serverTypeId, rootDevice, (PropertyObject) null);
    }

    /**
     * Creates and returns a streaming object using the specified connection string and config object.
     * &#64;param connectionString Typically a connection string usually has a well known prefix, such as `daq.lt//`.
     * &#64;param config A config object that contains parameters used to configure a streaming connection. In case of a null value, implementation should use default configuration.
     * &#64;param[out] streaming The created streaming object.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqModule_createStreaming()}.
     */
    public Streaming createStreaming(String connectionString, PropertyObject config) {
        try {
            MemorySegment connectionString$s = Wrap.daqStringOrNull(connectionString);
            try {
                return DaqObject.wrap(DaqModule.createStreaming(requireLivePointer(), connectionString$s, (config == null ? MemorySegment.NULL : config.requireLivePointer())), Streaming.class);
            } finally { Ffi.releaseRef(connectionString$s); }
        } finally {
            Reference.reachabilityFence(this);
            Reference.reachabilityFence(config);
        }
    }

    /** createStreaming with trailing arguments at their openDAQ defaults. */
    public Streaming createStreaming(String connectionString) {
        return createStreaming(connectionString, (PropertyObject) null);
    }

    /**
     * Returns a dictionary of known and available device types this module can create.
     * &#64;param[out] deviceTypes The dictionary of known device types.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqModule_getAvailableDeviceTypes()}.
     */
    public java.util.Map<String, DeviceType> getAvailableDeviceTypes() {
        try {
            return Wrap.map(DaqModule.getAvailableDeviceTypes(requireLivePointer()), p -> (String) Wrap.unboxOwned(p), p -> Wrap.castOwned(p, DeviceType.class));
        } finally {
            Reference.reachabilityFence(this);
        }
    }

    /**
     * Returns a list of known devices info. The implementation can start discovery in background and only return the results in this function.
     * &#64;param[out] availableDevices The list of known devices information.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqModule_getAvailableDevices()}.
     */
    public java.util.List<DeviceInfo> getAvailableDevices() {
        try {
            return Wrap.list(DaqModule.getAvailableDevices(requireLivePointer()), p -> Wrap.castOwned(p, DeviceInfo.class));
        } finally {
            Reference.reachabilityFence(this);
        }
    }

    /**
     * Returns a dictionary of known and available function block types this module can create.
     * &#64;param[out] functionBlockTypes The dictionary of known function block types.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqModule_getAvailableFunctionBlockTypes()}.
     */
    public java.util.Map<String, FunctionBlockType> getAvailableFunctionBlockTypes() {
        try {
            return Wrap.map(DaqModule.getAvailableFunctionBlockTypes(requireLivePointer()), p -> (String) Wrap.unboxOwned(p), p -> Wrap.castOwned(p, FunctionBlockType.class));
        } finally {
            Reference.reachabilityFence(this);
        }
    }

    /**
     * Returns a dictionary of known and available servers types that this module can create.
     * &#64;param[out] serverTypes The dictionary of known server types.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqModule_getAvailableServerTypes()}.
     */
    public java.util.Map<String, ServerType> getAvailableServerTypes() {
        try {
            return Wrap.map(DaqModule.getAvailableServerTypes(requireLivePointer()), p -> (String) Wrap.unboxOwned(p), p -> Wrap.castOwned(p, ServerType.class));
        } finally {
            Reference.reachabilityFence(this);
        }
    }

    /**
     * Returns a dictionary of known and available streaming types that this module (client) can create.
     * &#64;param[out] streamingTypes The dictionary of known streaming types.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqModule_getAvailableStreamingTypes()}.
     */
    public java.util.Map<String, StreamingType> getAvailableStreamingTypes() {
        try {
            return Wrap.map(DaqModule.getAvailableStreamingTypes(requireLivePointer()), p -> (String) Wrap.unboxOwned(p), p -> Wrap.castOwned(p, StreamingType.class));
        } finally {
            Reference.reachabilityFence(this);
        }
    }

    /**
     * Used to retrieve the license config template.
     * &#64;param[out] licenseConfig Previously used config.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqModule_getLicenseConfig()}.
     */
    public java.util.Map<String, String> getLicenseConfig() {
        try {
            return Wrap.map(DaqModule.getLicenseConfig(requireLivePointer()), p -> (String) Wrap.unboxOwned(p), p -> (String) Wrap.unboxOwned(p));
        } finally {
            Reference.reachabilityFence(this);
        }
    }

    /**
     * Retrieves the module information.
     * &#64;param[out] info The module information.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqModule_getModuleInfo()}.
     */
    public ModuleInfo getModuleInfo() {
        try {
            return DaqObject.wrap(DaqModule.getModuleInfo(requireLivePointer()), ModuleInfo.class);
        } finally {
            Reference.reachabilityFence(this);
        }
    }

    /**
     * Check whether the module license is loaded.
     * &#64;param[out] loaded True, if the module license is loaded.
     * Always return True if no license is required by the module.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqModule_licenseLoaded()}.
     */
    public boolean licenseLoaded() {
        try {
            return DaqModule.licenseLoaded(requireLivePointer());
        } finally {
            Reference.reachabilityFence(this);
        }
    }

    /**
     * Used for loading a license, when the module requires one. Licenses can specify the degree to which the module is unlocked to the user (i.e. which and/or how many concurrent function blocks from this modules are accessible with the license).
     * &#64;param[out] succeeded License was accepted.
     * &#64;param licenseConfig Any information relevant to load the license (i.e. a path to the license file).
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqModule_loadLicense()}.
     */
    public boolean loadLicense(java.util.Map<?, ?> licenseConfig) {
        try {
            MemorySegment licenseConfig$d = (licenseConfig == null ? MemorySegment.NULL : Box.boxDictOwned(licenseConfig));
            try {
                return DaqModule.loadLicense(requireLivePointer(), licenseConfig$d);
            } finally { Ffi.releaseRef(licenseConfig$d); }
        } finally {
            Reference.reachabilityFence(this);
        }
    }

}
