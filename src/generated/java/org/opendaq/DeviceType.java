// This file is autogenerated by tools/generate_high_level_bindings.py. Do not edit.

package org.opendaq;

import java.lang.foreign.Arena;
import java.lang.foreign.MemorySegment;
import java.lang.foreign.ValueLayout;
import java.lang.ref.Reference;
import java.util.List;
import java.util.Map;

import org.opendaq.lowlevel.*;

/**
 * Provides information on what device type can be created by a given module. Can be used to obtain the default configuration used when either adding/creating a new device.
 *
 * &lt;p&gt;Wraps the openDAQ {&#64;code daqDeviceType} interface.
 */
public class DeviceType extends ComponentType {

    /** Adopt an owned reference (see {@link DaqObject#DaqObject}). */
    public DeviceType(MemorySegment pointer) {
        super(pointer);
    }

    /**
     * Creates a Device type object, with the id, name, description and optional defaultConfig.
     * &#64;param id The unique type ID of the device.
     * &#64;param name The name of the device type.
     * &#64;param description A short description of the device type.
     * &#64;param defaultConfig The property object, to be cloned and returned, each time user creates default configuration object. This way each instance of the device has its own configuration object.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqDeviceType_createDeviceType()}.
     */
    public DeviceType(String id, String name, String description, PropertyObject defaultConfig, String prefix) {
        super(create$createDeviceType(id, name, description, defaultConfig, prefix));
    }

    private static MemorySegment create$createDeviceType(String id, String name, String description, PropertyObject defaultConfig, String prefix) {
        try {
            MemorySegment id$s = Wrap.daqStringOrNull(id);
            try {
                MemorySegment name$s = Wrap.daqStringOrNull(name);
                try {
                    MemorySegment description$s = Wrap.daqStringOrNull(description);
                    try {
                        MemorySegment prefix$s = Wrap.daqStringOrNull(prefix);
                        try {
                            return DaqDeviceType.createDeviceType(id$s, name$s, description$s, (defaultConfig == null ? MemorySegment.NULL : defaultConfig.requireLivePointer()), prefix$s);
                        } finally { Ffi.releaseRef(prefix$s); }
                    } finally { Ffi.releaseRef(description$s); }
                } finally { Ffi.releaseRef(name$s); }
            } finally { Ffi.releaseRef(id$s); }
        } finally {
            Reference.reachabilityFence(defaultConfig);
        }
    }

    /**
     * Calls the openDAQ C function {&#64;code daqDeviceType_getConnectionStringPrefix()}.
     */
    public String getConnectionStringPrefix() {
        try {
            return Ffi.readDaqStringAndRelease(DaqDeviceType.getConnectionStringPrefix(requireLivePointer()));
        } finally {
            Reference.reachabilityFence(this);
        }
    }

}
