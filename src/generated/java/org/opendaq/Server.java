// This file is autogenerated by tools/generate_high_level_bindings.py. Do not edit.

package org.opendaq;

import java.lang.foreign.Arena;
import java.lang.foreign.MemorySegment;
import java.lang.foreign.ValueLayout;
import java.lang.ref.Reference;
import java.util.List;
import java.util.Map;

import org.opendaq.lowlevel.*;

/**
 * Represents a server. The server provides access to the openDAQ device. Depend of the implementation, it can support configuring the device, reading configuration, and data streaming.
 * We do not make the server directly. But through the instance and module manager. For that reason, the server must be uniquely
 * defined with "ServerType". The server is then created with the current root device, context and configuration object.
 * Configuration of the server can be done with custom property object.
 * The configuration object is created with the corresponding ServerType object (IServerType::createDefaultConfig method).
 * For example, with a configuration object, we can define connection timeout.
 *
 * &lt;p&gt;Wraps the openDAQ {&#64;code daqServer} interface.
 */
public class Server extends Folder {

    /** Adopt an owned reference (see {@link DaqObject#DaqObject}). */
    public Server(MemorySegment pointer) {
        super(pointer);
    }

    /**
     * Disables the server to be discovered by the clients.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqServer_disableDiscovery()}.
     */
    public void disableDiscovery() {
        try {
            DaqServer.disableDiscovery(requireLivePointer());
        } finally {
            Reference.reachabilityFence(this);
        }
    }

    /**
     * Enables the server to be discovered by the clients.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqServer_enableDiscovery()}.
     */
    public void enableDiscovery() {
        try {
            DaqServer.enableDiscovery(requireLivePointer());
        } finally {
            Reference.reachabilityFence(this);
        }
    }

    /**
     * Gets the server id.
     * &#64;param[out] serverId The server id.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqServer_getId()}.
     */
    public String getId() {
        try {
            return Ffi.readDaqStringAndRelease(DaqServer.getId(requireLivePointer()));
        } finally {
            Reference.reachabilityFence(this);
        }
    }

    /**
     * Gets a list of the server's signals.
     * &#64;param[out] signals The flat list of signals.
     * Server signals are most often the mirrored representations of signals that belong to the client connected to the instance via this server.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqServer_getSignals()}.
     */
    public java.util.List<Signal> getSignals(SearchFilter searchFilter) {
        try {
            return Wrap.list(DaqServer.getSignals(requireLivePointer(), (searchFilter == null ? MemorySegment.NULL : searchFilter.requireLivePointer())), p -> Wrap.castOwned(p, Signal.class));
        } finally {
            Reference.reachabilityFence(this);
            Reference.reachabilityFence(searchFilter);
        }
    }

    /** getSignals with trailing arguments at their openDAQ defaults. */
    public java.util.List<Signal> getSignals() {
        return getSignals((SearchFilter) null);
    }

    /**
     * Gets a streaming source associated with server.
     * &#64;param[out] streaming The streaming object that represents the server’s client-to-device streaming source.
     * The streaming object is assigned on servers that support the generalized client-to-device streaming mechanism.
     * The object is expected to be associated with mirrored server-side copies of client signals and is responsible for receiving
     * their data sent from the client to the device.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqServer_getStreaming()}.
     */
    public Streaming getStreaming() {
        try {
            return DaqObject.wrap(DaqServer.getStreaming(requireLivePointer()), Streaming.class);
        } finally {
            Reference.reachabilityFence(this);
        }
    }

    /**
     * Stops the server. This is called when we remove the server from the Instance or Instance is closing.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqServer_stop()}.
     */
    public void stop() {
        try {
            DaqServer.stop(requireLivePointer());
        } finally {
            Reference.reachabilityFence(this);
        }
    }

}
