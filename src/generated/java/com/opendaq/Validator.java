// This file is autogenerated by tools/generate_high_level_bindings.py. Do not edit.

package com.opendaq;

import java.lang.foreign.Arena;
import java.lang.foreign.MemorySegment;
import java.lang.foreign.ValueLayout;
import java.lang.ref.Reference;
import java.util.List;
import java.util.Map;

import com.opendaq.lowlevel.*;

/**
 * Used by openDAQ properties to validate whether a value fits the value restrictions of the Property.
 * Whenever a value is set to on a Property object, if the corresponding Property has a validator configured, the value will
 * be validated, throwing a validation error, if the value is not compliant with the validation restrictions. For example,
 * a validator can check the written value for lower-than, greater-than, equality, or other number relations.
 * The validation conditions are configured with an evaluation string when the validator is constructed. The string constructs an
 * Eval value that replaces any instance of the keyword "value" or "val" with the value being set. The result of the Eval
 * value evaluation is the output of the `validate` function call. For example, validators created with the string
 * "value == 5" would reject any value that is not equal to 5.
 *
 * &lt;p&gt;Wraps the openDAQ {&#64;code daqValidator} interface.
 */
public class Validator extends BaseObject {

    /** Adopt an owned reference (see {@link DaqObject#DaqObject}). */
    public Validator(MemorySegment pointer) {
        super(pointer);
    }

    /**
     * Creates a Validator with the given evaluation expression.
     * &#64;param eval The evaluation expression used for validation.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqValidator_createValidator()}.
     */
    public Validator(String eval) {
        super(create$createValidator(eval));
    }

    private static MemorySegment create$createValidator(String eval) {
        MemorySegment eval$s = Wrap.daqStringOrNull(eval);
        try {
            return DaqValidator.createValidator(eval$s);
        } finally { Ffi.releaseRef(eval$s); }
    }

    /**
     * Gets the string expression used when creating the validator.
     * &#64;param[out] eval The validation expression.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqValidator_getEval()}.
     */
    public String getEval() {
        try {
            return Ffi.readDaqStringAndRelease(DaqValidator.getEval(requireLivePointer()));
        } finally {
            Reference.reachabilityFence(this);
        }
    }

    /**
     * Checks whether `value` adheres to the validity conditions of the validator.
     * &#64;param propObj Optional property object parameter required if the validation depends on other properties of the Property object.
     * &#64;param value The value to be checked for whether it is valid or not.
     * &#64;retval OPENDAQ_ERR_VALIDATE_FAILED if `value` is invalid.
     * &#64;retval OPENDAQ_SUCCESS if `value` is valid.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqValidator_validate()}.
     */
    public void validate(Object propObj, Object value) {
        try {
            Box.Boxed propObj$b = Box.box(propObj);
            try {
                Box.Boxed value$b = Box.box(value);
                try {
                    DaqValidator.validate(requireLivePointer(), propObj$b.segment(), value$b.segment());
                } finally { Box.releaseBoxed(value$b); }
            } finally { Box.releaseBoxed(propObj$b); }
        } finally {
            Reference.reachabilityFence(this);
        }
    }

    /**
     * Calls the openDAQ C function {&#64;code daqValidator_validateNoLock()}.
     */
    public void validateNoLock(Object propObj, Object value) {
        try {
            Box.Boxed propObj$b = Box.box(propObj);
            try {
                Box.Boxed value$b = Box.box(value);
                try {
                    DaqValidator.validateNoLock(requireLivePointer(), propObj$b.segment(), value$b.segment());
                } finally { Box.releaseBoxed(value$b); }
            } finally { Box.releaseBoxed(propObj$b); }
        } finally {
            Reference.reachabilityFence(this);
        }
    }

}
