// This file is autogenerated by tools/generate_high_level_bindings.py. Do not edit.

package com.opendaq;

import java.lang.foreign.Arena;
import java.lang.foreign.MemorySegment;
import java.lang.foreign.ValueLayout;
import java.lang.ref.Reference;
import java.util.List;
import java.util.Map;

import com.opendaq.lowlevel.*;

/**
 * Describes a dimension of the signal's data. Eg. a column/row in a matrix.
 * Dimension objects define the size and labels of a single data dimension. Labels, in concert
 * with the unit, provide information about the position of data in a structure. For example, for vector
 * rank data in a frequency domain, the unit would be Hz, and the labels would range from the minimum to the maximum
 * frequency of the spectrum.
 * The number of dimensions a sample descriptor defines the rank of the signals data. When no dimensions are
 * present, one sample is a single value. When there's one dimension, a sample contains a vector of values,
 * when there are three a sample contains a matrix. Higher ranks of data can be represented by adding more
 * dimension objects to a sample descriptor.
 * The labels can be defined with a Rule (in example a linear rule with a coefficient and offset), where the
 * the data index is the input to the rule. In example
 * A Linear rule with coefficient = 5, offset = 10, size = 5 provides the following list of labels:
 * [10, 15, 20, 25, 30]
 * To specify the labels can explicitly, the List dimension rule can be used. The list rule allows for a list of
 * numbers, strings, or ranges to be used as the dimension labels.
 * Dimension objects implement the Struct methods internally and are Core type `ctStruct`.
 *
 * &lt;p&gt;Wraps the openDAQ {&#64;code daqDimension} interface.
 */
public class Dimension extends BaseObject {

    /** Adopt an owned reference (see {@link DaqObject#DaqObject}). */
    public Dimension(MemorySegment pointer) {
        super(pointer);
    }

    /**
     * Creates a dimension object of which labels and size are defined via rule.
     * &#64;param rule The rule via which labels are defined.
     * &#64;param unit The unit of the dimension's labels.
     * &#64;param name The name the dimension.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqDimension_createDimension()}.
     */
    public Dimension(DimensionRule rule, Unit unit, String name) {
        super(create$createDimension(rule, unit, name));
    }

    private static MemorySegment create$createDimension(DimensionRule rule, Unit unit, String name) {
        try {
            MemorySegment name$s = Wrap.daqStringOrNull(name);
            try {
                return DaqDimension.createDimension((rule == null ? MemorySegment.NULL : rule.requireLivePointer()), (unit == null ? MemorySegment.NULL : unit.requireLivePointer()), name$s);
            } finally { Ffi.releaseRef(name$s); }
        } finally {
            Reference.reachabilityFence(rule);
            Reference.reachabilityFence(unit);
        }
    }

    /**
     * Creates a Dimension using Builder
     * &#64;param builder Dimension Builder
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqDimension_createDimensionFromBuilder()}.
     */
    public Dimension(DimensionBuilder builder) {
        super(create$createDimensionFromBuilder(builder));
    }

    private static MemorySegment create$createDimensionFromBuilder(DimensionBuilder builder) {
        try {
            return DaqDimension.createDimensionFromBuilder((builder == null ? MemorySegment.NULL : builder.requireLivePointer()));
        } finally {
            Reference.reachabilityFence(builder);
        }
    }

    /**
     * Gets a list of labels that defines the dimension.
     * &#64;param[out] labels The list of labels.
     * The list is obtained from the dimension rule parameters by parsing and evaluating the parameters
     * in conjunction with the rule type.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqDimension_getLabels()}.
     */
    public java.util.List<Object> getLabels() {
        try {
            return Wrap.list(DaqDimension.getLabels(requireLivePointer()), Wrap::unboxOwned);
        } finally {
            Reference.reachabilityFence(this);
        }
    }

    /**
     * Gets the name of the dimension.
     * &#64;param[out] name The name of the dimension.
     * The name that best describes the dimension, in example "Frequency" for spectrum data.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqDimension_getName()}.
     */
    public String getName() {
        try {
            return Ffi.readDaqStringAndRelease(DaqDimension.getName(requireLivePointer()));
        } finally {
            Reference.reachabilityFence(this);
        }
    }

    /**
     * Gets the rule that defines the labels and size of the dimension.
     * &#64;param[out] rule The dimension rule.
     * The rule takes as input the index of data value in a sample and produces a label associated
     * with that index.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqDimension_getRule()}.
     */
    public DimensionRule getRule() {
        try {
            return DaqObject.wrap(DaqDimension.getRule(requireLivePointer()), DimensionRule.class);
        } finally {
            Reference.reachabilityFence(this);
        }
    }

    /**
     * Gets the size of the dimension.
     * &#64;param[out] size The size of the dimension.
     * The size is obtained from the dimension rule parameters - either from the `size` parameter, or the
     * count of elements in the `list` parameter.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqDimension_getSize()}.
     */
    public long getSize() {
        try {
            return DaqDimension.getSize(requireLivePointer());
        } finally {
            Reference.reachabilityFence(this);
        }
    }

    /**
     * Gets the unit of the dimension's labels.
     * &#64;param[out] unit The unit of the dimension.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqDimension_getUnit()}.
     */
    public Unit getUnit() {
        try {
            return DaqObject.wrap(DaqDimension.getUnit(requireLivePointer()), Unit.class);
        } finally {
            Reference.reachabilityFence(this);
        }
    }

}
