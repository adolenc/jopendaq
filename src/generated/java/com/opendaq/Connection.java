// This file is autogenerated by tools/generate_high_level_bindings.py. Do not edit.

package com.opendaq;

import java.lang.foreign.Arena;
import java.lang.foreign.MemorySegment;
import java.lang.foreign.ValueLayout;
import java.lang.ref.Reference;
import java.util.List;
import java.util.Map;

import com.opendaq.lowlevel.*;

/**
 * Represents a connection between an Input port and Signal. Acts as a queue for packets sent by the signal, which can be read by the input port and the input port's owner.
 * The Connection provides standard queue methods, allowing for packets to be put at the back
 * of the queue, and popped from the front. Additionally, the front packet can be inspected via
 * `peek`, and the number of queued packets can be obtained through `getPacketCount`.
 * The Connection has a reference to the connected Signal and Input port.
 *
 * &lt;p&gt;Wraps the openDAQ {&#64;code daqConnection} interface.
 */
public class Connection extends BaseObject {

    /** Adopt an owned reference (see {@link DaqObject#DaqObject}). */
    public Connection(MemorySegment pointer) {
        super(pointer);
    }

    /**
     * Calls the openDAQ C function {&#64;code daqConnection_createConnection()}.
     */
    public Connection(InputPort inputPort, Signal signal, Context context) {
        super(create$createConnection(inputPort, signal, context));
    }

    private static MemorySegment create$createConnection(InputPort inputPort, Signal signal, Context context) {
        try {
            return DaqConnection.createConnection((inputPort == null ? MemorySegment.NULL : inputPort.requireLivePointer()), (signal == null ? MemorySegment.NULL : signal.requireLivePointer()), (context == null ? MemorySegment.NULL : context.requireLivePointer()));
        } finally {
            Reference.reachabilityFence(inputPort);
            Reference.reachabilityFence(signal);
            Reference.reachabilityFence(context);
        }
    }

    /**
     * Removes the packet at the front of the queue and returns it.
     * &#64;param[out] packet The removed packet or &#64;c nullptr if the connection has no packets.
     * &#64;retval OPENDAQ_NO_MORE_ITEMS When the connection does not hold any packets.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqConnection_dequeue()}.
     */
    public Packet dequeue() {
        try {
            return DaqObject.wrap(DaqConnection.dequeue(requireLivePointer()), Packet.class);
        } finally {
            Reference.reachabilityFence(this);
        }
    }

    /**
     * Removes all packets from the queue.
     * &#64;param[out] packets The removed packets.
     * Removing all packets can be more efficient than dequeuing packet by packet in heavily loaded systems.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqConnection_dequeueAll()}.
     */
    public java.util.List<Packet> dequeueAll() {
        try {
            return Wrap.list(DaqConnection.dequeueAll(requireLivePointer()), p -> Wrap.castOwned(p, Packet.class));
        } finally {
            Reference.reachabilityFence(this);
        }
    }

    /**
     * Places a packet at the back of the queue.
     * &#64;param packet The packet to be enqueued.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqConnection_enqueue()}.
     */
    public void enqueue(Packet packet) {
        try {
            DaqConnection.enqueue(requireLivePointer(), (packet == null ? MemorySegment.NULL : packet.requireLivePointer()));
        } finally {
            Reference.reachabilityFence(this);
            Reference.reachabilityFence(packet);
        }
    }

    /**
     * Places a packet at the back of the queue. The reference of the packet is stolen.
     * &#64;param packet The packet to be enqueued.
     * After calling the method, the packet should not be touched again. The ownership of the packet
     * is taken by underlying connections and it could be destroyed before the function returns.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqConnection_enqueueAndStealRef()}.
     */
    public void enqueueAndStealRef(Packet packet) {
        try {
            DaqConnection.enqueueAndStealRef(requireLivePointer(), (packet == null ? MemorySegment.NULL : packet.requireLivePointer()));
        } finally {
            Reference.reachabilityFence(this);
            Reference.reachabilityFence(packet);
        }
    }

    /**
     * Places multiple packets at the back of the queue.
     * &#64;param packets The packets to be enqueued.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqConnection_enqueueMultiple()}.
     */
    public void enqueueMultiple(java.util.List<?> packets) {
        try {
            MemorySegment packets$l = (packets == null ? MemorySegment.NULL : Box.boxListOwned(packets));
            try {
                DaqConnection.enqueueMultiple(requireLivePointer(), packets$l);
            } finally { Ffi.releaseRef(packets$l); }
        } finally {
            Reference.reachabilityFence(this);
        }
    }

    /**
     * Places multiple packets at the back of the queue. The references of the packets are stolen.
     * &#64;param packets The packets to be enqueued.
     * After calling the method, the packets should not be touched again. The ownership of the packets
     * is taken by underlying connections and it could be destroyed before the function returns.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqConnection_enqueueMultipleAndStealRef()}.
     */
    public void enqueueMultipleAndStealRef(java.util.List<?> packets) {
        try {
            MemorySegment packets$l = (packets == null ? MemorySegment.NULL : Box.boxListOwned(packets));
            try {
                DaqConnection.enqueueMultipleAndStealRef(requireLivePointer(), packets$l);
            } finally { Ffi.releaseRef(packets$l); }
        } finally {
            Reference.reachabilityFence(this);
        }
    }

    /**
     * Places a packet at the back of the queue.
     * &#64;param packet The packet to be enqueued.
     * The connection notifies the listener on the same thread that this method was called.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqConnection_enqueueOnThisThread()}.
     */
    public void enqueueOnThisThread(Packet packet) {
        try {
            DaqConnection.enqueueOnThisThread(requireLivePointer(), (packet == null ? MemorySegment.NULL : packet.requireLivePointer()));
        } finally {
            Reference.reachabilityFence(this);
            Reference.reachabilityFence(packet);
        }
    }

    /**
     * Places a packet at the back of the queue.
     * &#64;param packet The packet to be enqueued.
     * The connection schedules the `onPacketReceived` notification.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqConnection_enqueueWithScheduler()}.
     */
    public void enqueueWithScheduler(Packet packet) {
        try {
            DaqConnection.enqueueWithScheduler(requireLivePointer(), (packet == null ? MemorySegment.NULL : packet.requireLivePointer()));
        } finally {
            Reference.reachabilityFence(this);
            Reference.reachabilityFence(packet);
        }
    }

    /**
     * Gets the number of samples available in the queued packets. The returned value ignores any Sample-Descriptor changes.
     * &#64;param[out] samples The total amount of samples currently available in the stored packets.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqConnection_getAvailableSamples()}.
     */
    public long getAvailableSamples() {
        try {
            return DaqConnection.getAvailableSamples(requireLivePointer());
        } finally {
            Reference.reachabilityFence(this);
        }
    }

    /**
     * Gets the Input port to which packets are being sent.
     * &#64;param[out] inputPort The input port.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqConnection_getInputPort()}.
     */
    public InputPort getInputPort() {
        try {
            return DaqObject.wrap(DaqConnection.getInputPort(requireLivePointer()), InputPort.class);
        } finally {
            Reference.reachabilityFence(this);
        }
    }

    /**
     * Gets the number of queued packets.
     * &#64;param[out] packetCount The number of queued packets.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqConnection_getPacketCount()}.
     */
    public long getPacketCount() {
        try {
            return DaqConnection.getPacketCount(requireLivePointer());
        } finally {
            Reference.reachabilityFence(this);
        }
    }

    /**
     * Gets the number of same-type samples available in the queued packets. The returned value is up-to the next Sample-Descriptor-Changed packet if any.
     * &#64;param[out] samples The total amount of same-type samples currently available in the stored packets.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqConnection_getSamplesUntilNextDescriptor()}.
     */
    public long getSamplesUntilNextDescriptor() {
        try {
            return DaqConnection.getSamplesUntilNextDescriptor(requireLivePointer());
        } finally {
            Reference.reachabilityFence(this);
        }
    }

    /**
     * Gets the number of samples available in the queued packets until the next event packet. The returned value is up-to the next Event packet if any.
     * &#64;param[out] samples The total amount of samples currently available in the stored packets until the next event packet.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqConnection_getSamplesUntilNextEventPacket()}.
     */
    public long getSamplesUntilNextEventPacket() {
        try {
            return DaqConnection.getSamplesUntilNextEventPacket(requireLivePointer());
        } finally {
            Reference.reachabilityFence(this);
        }
    }

    /**
     * Gets the number of samples available in the queued packets until the next gap packet. The returned value is up-to the next Gap packet if any.
     * &#64;param[out] samples The total amount of samples currently available in the stored packets until the next gap packet.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqConnection_getSamplesUntilNextGapPacket()}.
     */
    public long getSamplesUntilNextGapPacket() {
        try {
            return DaqConnection.getSamplesUntilNextGapPacket(requireLivePointer());
        } finally {
            Reference.reachabilityFence(this);
        }
    }

    /**
     * Gets the Signal that is sending packets through the Connection.
     * &#64;param[out] signal The Signal.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqConnection_getSignal()}.
     */
    public Signal getSignal() {
        try {
            return DaqObject.wrap(DaqConnection.getSignal(requireLivePointer()), Signal.class);
        } finally {
            Reference.reachabilityFence(this);
        }
    }

    /**
     * Queries if the connection has an event packet.
     * &#64;param[out] hasEventPacket True if the connection has an event packet.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqConnection_hasEventPacket()}.
     */
    public boolean hasEventPacket() {
        try {
            return DaqConnection.hasEventPacket(requireLivePointer());
        } finally {
            Reference.reachabilityFence(this);
        }
    }

    /**
     * Queries if the connection has a gap packet.
     * &#64;param[out] hasGapPacket True if the connection has a gap packet.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqConnection_hasGapPacket()}.
     */
    public boolean hasGapPacket() {
        try {
            return DaqConnection.hasGapPacket(requireLivePointer());
        } finally {
            Reference.reachabilityFence(this);
        }
    }

    /**
     * Returns true if the type of connection is remote.
     * &#64;param[out] remote True if connection is remote.
     * Remote connections do not pass any packets. They represent the connection between input ports and signals
     * on remote devices.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqConnection_isRemote()}.
     */
    public boolean isRemote() {
        try {
            return DaqConnection.isRemote(requireLivePointer());
        } finally {
            Reference.reachabilityFence(this);
        }
    }

    /**
     * Returns the packet at the front of the queue without removing it.
     * &#64;param[out] packet The packet at the front of the queue or &#64;c nullptr if the connection has no packets.
     * &#64;retval OPENDAQ_NO_MORE_ITEMS When the connection does not hold any packets.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqConnection_peek()}.
     */
    public Packet peek() {
        try {
            return DaqObject.wrap(DaqConnection.peek(requireLivePointer()), Packet.class);
        } finally {
            Reference.reachabilityFence(this);
        }
    }

}
