// This file is autogenerated by tools/generate_high_level_bindings.py. Do not edit.

package com.opendaq;

import java.lang.foreign.Arena;
import java.lang.foreign.MemorySegment;
import java.lang.foreign.ValueLayout;
import java.lang.ref.Reference;
import java.util.List;
import java.util.Map;

import com.opendaq.lowlevel.*;

/**
 * List of string tags. Provides helpers to get and search the list of tags.
 * Tags provide a view into an underlying list of tags. The list can be retrieved via
 * `getList`, and inspected through `contains` and `query`.
 * To manipulate the list of tags, the add/remove tag functions can be used. The Tags
 * object can only be modified if the object is not locked by the owning Component.
 *
 * &lt;p&gt;Wraps the openDAQ {&#64;code daqTags} interface.
 */
public class Tags extends BaseObject {

    /** Adopt an owned reference (see {@link DaqObject#DaqObject}). */
    public Tags(MemorySegment pointer) {
        super(pointer);
    }

    /**
     * Calls the openDAQ C function {&#64;code daqTags_createTags()}.
     */
    public Tags() {
        super(create$createTags());
    }

    private static MemorySegment create$createTags() {
        return DaqTags.createTags();
    }

    /**
     * Checks whether a tag is present in the list of tags.
     * &#64;param name The name of the tag being checked.
     * &#64;param[out] value True if a tag is found; false otherwise.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqTags_contains()}.
     */
    public boolean contains(String name) {
        try {
            MemorySegment name$s = Wrap.daqStringOrNull(name);
            try {
                return DaqTags.contains(requireLivePointer(), name$s);
            } finally { Ffi.releaseRef(name$s); }
        } finally {
            Reference.reachabilityFence(this);
        }
    }

    /**
     * Gets the list of all tags in the list.
     * &#64;param[out] value The list of tag strings.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqTags_getList()}.
     */
    public java.util.List<String> getList() {
        try {
            return Wrap.list(DaqTags.getList(requireLivePointer()), p -> (String) Wrap.unboxOwned(p));
        } finally {
            Reference.reachabilityFence(this);
        }
    }

    /**
     * Queries the list of tags, creating an EvalValue expression from the `query` string. Returns true if the list of tags matches the query, false otherwise.
     * &#64;param query The query string. I.e. "tag1 || (tag2 &amp;&amp; tag3)"
     * &#64;param[out] value The result of the query
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqTags_query()}.
     */
    public boolean query(String query) {
        try {
            MemorySegment query$s = Wrap.daqStringOrNull(query);
            try {
                return DaqTags.query(requireLivePointer(), query$s);
            } finally { Ffi.releaseRef(query$s); }
        } finally {
            Reference.reachabilityFence(this);
        }
    }

}
