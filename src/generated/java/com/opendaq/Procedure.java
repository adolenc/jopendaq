// This file is autogenerated by tools/generate_high_level_bindings.py. Do not edit.

package com.opendaq;

import java.lang.foreign.Arena;
import java.lang.foreign.MemorySegment;
import java.lang.foreign.ValueLayout;
import java.lang.ref.Reference;
import java.util.List;
import java.util.Map;

import com.opendaq.lowlevel.*;

/**
 * Wraps the openDAQ {&#64;code daqProcedure} interface.
 */
public class Procedure extends BaseObject {

    /** Adopt an owned reference (see {@link DaqObject#DaqObject}). */
    public Procedure(MemorySegment pointer) {
        super(pointer);
    }

    /**
     * Calls the openDAQ C function {&#64;code daqProcedure_createProcedure()}.
     */
    public static Procedure createProcedure(MemorySegment value) {
        return DaqObject.wrap(create$createProcedure(value), Procedure.class);
    }

    private static MemorySegment create$createProcedure(MemorySegment value) {
        return DaqProcedure.createProcedure(Ffi.orNull(value));
    }

    /**
     * Calls the openDAQ C function {&#64;code daqProcedure_dispatch()}.
     */
    public void dispatch(Object params) {
        try {
            Box.Boxed params$b = Box.box(params);
            try {
                DaqProcedure.dispatch(requireLivePointer(), params$b.segment());
            } finally { Box.releaseBoxed(params$b); }
        } finally {
            Reference.reachabilityFence(this);
        }
    }

    // ------------------------------------------------------------------
    // Hand-written part (tools/manual/Procedure.java.inc)
    // ------------------------------------------------------------------

    /**
     * Wrap a Java callback as an openDAQ Procedure, invoked with the decoded
     * params each time the procedure is dispatched — from Java or from native
     * code (a proc property's value, ...).
     */
    public Procedure(ProcedureCallback callback) {
        super(DaqProcedure.createProcedure(Callbacks.procedureStub(callback)));
    }

    // Declared argument count from the owning property's callable info (set
    // by PropertyObject.getPropertyValue); -1 when unknown.
    int declaredArity = -1;

    /**
     * Dispatch this procedure with natural Java arguments (boxed per the
     * openDAQ params convention: none, a single value, or a list).  A
     * procedure obtained from a property checks the argument count against
     * the property's declared callable info.
     */
    public void invoke(Object... params) {
        if (declaredArity >= 0 && params.length != declaredArity) {
            throw new IllegalArgumentException(
                "openDAQ callable expects " + declaredArity + " argument(s) but was called with "
                + params.length + ".");
        }
        MemorySegment encoded = Callbacks.encodeParamsOwned(params);
        try {
            DaqProcedure.dispatch(requireLivePointer(), encoded);
        } finally {
            Ffi.releaseRef(encoded);
            Reference.reachabilityFence(this);
        }
    }

}
