// This file is autogenerated by tools/generate_high_level_bindings.py. Do not edit.

package com.opendaq;

import java.lang.foreign.Arena;
import java.lang.foreign.MemorySegment;
import java.lang.foreign.ValueLayout;
import java.lang.ref.Reference;
import java.util.List;
import java.util.Map;

import com.opendaq.lowlevel.*;

/**
 * The top-level openDAQ object. It acts as container for the openDAQ context and the base module manager.
 * It forwards all Device and PropertyObject calls to the current root device, making the calls on the Instance
 * and root device equivalent.
 * On creation, it creates a Client device - a default device implementation that can load any function blocks
 * present in the module manager search path. If the native openDAQ client-module is loaded, the Client device
 * can connect to any TMS enabled device by using the `addDevice` function. The Client is set as the root device
 * when the instance is created.
 *
 * &lt;p&gt;Wraps the openDAQ {&#64;code daqInstance} interface.
 */
public class Instance extends Device {

    /** Adopt an owned reference (see {@link DaqObject#DaqObject}). */
    public Instance(MemorySegment pointer) {
        super(pointer);
    }

    /**
     * Creates a Instance with Builder
     * &#64;param builder Instance Builder
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqInstance_createInstanceFromBuilder()}.
     */
    public Instance(InstanceBuilder builder) {
        super(create$createInstanceFromBuilder(builder));
    }

    private static MemorySegment create$createInstanceFromBuilder(InstanceBuilder builder) {
        try {
            return DaqInstance.createInstanceFromBuilder((builder == null ? MemorySegment.NULL : builder.requireLivePointer()));
        } finally {
            Reference.reachabilityFence(builder);
        }
    }

    /**
     * Creates an openDAQ instance.
     * &#64;param context The context object.
     * &#64;param localId The localID of the instance.
     * openDAQ application uses instance as an entry point and a root component. The instance is the first openDAQ object
     * that is created in the application.
     * The caller should provide a localID that is a string that should be unique across multiple instances. If the
     * instance sets a root device, the localID of the root device is automatically used as localID of the instance.
     * The caller should provide configured module manager and context.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqInstance_createInstance()}.
     */
    public static Instance createInstance(Context context, String localId) {
        return DaqObject.wrap(create$createInstance(context, localId), Instance.class);
    }

    private static MemorySegment create$createInstance(Context context, String localId) {
        try {
            MemorySegment localId$s = Wrap.daqStringOrNull(localId);
            try {
                return DaqInstance.createInstance((context == null ? MemorySegment.NULL : context.requireLivePointer()), localId$s);
            } finally { Ffi.releaseRef(localId$s); }
        } finally {
            Reference.reachabilityFence(context);
        }
    }

    /**
     * Creates and adds streaming and "OpenDAQOPCUA" servers with default configurations.
     * &#64;param[out] servers List of added created servers.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqInstance_addStandardServers()}.
     */
    public java.util.List<Server> addStandardServers() {
        try {
            return Wrap.list(DaqInstance.addStandardServers(requireLivePointer()), p -> Wrap.castOwned(p, Server.class));
        } finally {
            Reference.reachabilityFence(this);
        }
    }

    /**
     * Get a dictionary of available server types as &lt;IString, IServerType&gt; pairs
     * &#64;param[out] serverTypes The dictionary of available server types.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqInstance_getAvailableServerTypes()}.
     */
    public java.util.Map<String, ServerType> getAvailableServerTypes() {
        try {
            return Wrap.map(DaqInstance.getAvailableServerTypes(requireLivePointer()), p -> (String) Wrap.unboxOwned(p), p -> Wrap.castOwned(p, ServerType.class));
        } finally {
            Reference.reachabilityFence(this);
        }
    }

    /**
     * Gets the Module manager.
     * &#64;param[out] manager The module manager.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqInstance_getModuleManager()}.
     */
    public ModuleManager getModuleManager() {
        try {
            return DaqObject.wrap(DaqInstance.getModuleManager(requireLivePointer()), ModuleManager.class);
        } finally {
            Reference.reachabilityFence(this);
        }
    }

    /**
     * Gets the current root device.
     * &#64;param[out] rootDevice The current root device.
     * All Device calls invoked on the Instance are forwarded to the current root device.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqInstance_getRootDevice()}.
     */
    public Device getRootDevice() {
        try {
            return DaqObject.wrap(DaqInstance.getRootDevice(requireLivePointer()), Device.class);
        } finally {
            Reference.reachabilityFence(this);
        }
    }

    /**
     * Adds a device with the connection string as root device.
     * &#64;param connectionString The connection string containing the address of the device.
     * &#64;param config A config object to configure a client device. This object can contain properties like max sample rate, port to use for 3rd party communication, number of channels to generate, or other device specific settings. In case of nullptr, a default configuration is used.
     * All Device calls invoked on the Instance are forwarded to the root device. The root device can only be set once.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqInstance_setRootDevice()}.
     */
    public void setRootDevice(String connectionString, PropertyObject config) {
        try {
            MemorySegment connectionString$s = Wrap.daqStringOrNull(connectionString);
            try {
                DaqInstance.setRootDevice(requireLivePointer(), connectionString$s, (config == null ? MemorySegment.NULL : config.requireLivePointer()));
            } finally { Ffi.releaseRef(connectionString$s); }
        } finally {
            Reference.reachabilityFence(this);
            Reference.reachabilityFence(config);
        }
    }

    /** setRootDevice with trailing arguments at their openDAQ defaults. */
    public void setRootDevice(String connectionString) {
        setRootDevice(connectionString, (PropertyObject) null);
    }

    // ------------------------------------------------------------------
    // Hand-written part (tools/manual/Instance.java.inc)
    // ------------------------------------------------------------------

    /**
     * Create a ready-to-use openDAQ instance: built from a default
     * {@code InstanceBuilder} whose module path points at the bundled native
     * openDAQ modules, so no arguments are needed.
     */
    public Instance() {
        super(createDefaultInstance());
    }

    private static MemorySegment createDefaultInstance() {
        InstanceBuilder builder = new InstanceBuilder();
        try {
            builder.setModulePath(NativeLoader.nativeLibraryDirectory().toString());
            return DaqInstance.createInstanceFromBuilder(builder.requireLivePointer());
        } finally {
            builder.release();
        }
    }

}
