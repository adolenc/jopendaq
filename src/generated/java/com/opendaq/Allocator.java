// This file is autogenerated by tools/generate_high_level_bindings.py. Do not edit.

package com.opendaq;

import java.lang.foreign.Arena;
import java.lang.foreign.MemorySegment;
import java.lang.foreign.ValueLayout;
import java.lang.ref.Reference;
import java.util.List;
import java.util.Map;

import com.opendaq.lowlevel.*;

/**
 * An allocator used to allocate memory.
 * The default BB allocator simply uses `malloc`, but the user can implement a custom allocator to
 * override this behavior (perhaps using a memory pool or different allocation strategy). An
 * example/reference implementation is provided which uses Microsoft `mimalloc`.
 *
 * &lt;p&gt;Wraps the openDAQ {&#64;code daqAllocator} interface.
 */
public class Allocator extends BaseObject {

    /** Adopt an owned reference (see {@link DaqObject#DaqObject}). */
    public Allocator(MemorySegment pointer) {
        super(pointer);
    }

    /**
     * Calls the openDAQ C function {&#64;code daqAllocator_createExternalAllocator()}.
     */
    public Allocator(MemorySegment data, Deleter deleter) {
        super(create$createExternalAllocator(data, deleter));
    }

    private static MemorySegment create$createExternalAllocator(MemorySegment data, Deleter deleter) {
        try {
            return DaqAllocator.createExternalAllocator(Ffi.orNull(data), (deleter == null ? MemorySegment.NULL : deleter.requireLivePointer()));
        } finally {
            Reference.reachabilityFence(deleter);
        }
    }

    /**
     * Calls the openDAQ C function {&#64;code daqAllocator_createMallocAllocator()}.
     */
    public static Allocator createMallocAllocator() {
        return DaqObject.wrap(create$createMallocAllocator(), Allocator.class);
    }

    private static MemorySegment create$createMallocAllocator() {
        return DaqAllocator.createMallocAllocator();
    }

    /**
     * Allocates a chunk of memory for a packet.
     * &#64;param descriptor The OPTIONAL data descriptor of the signal for which memory is to be allocated. This can provide hints to the allocator. However, allocator implementations MUST accept null values.
     * &#64;param bytes The number of bytes to allocate.
     * &#64;param align The alignment requirement of the caller (typically the element size). This value may be zero if the caller does not need to specify an alignment requirement.
     * &#64;param[out] address The address of the allocated memory.
     * The implementation MAY set address value to `nullptr` without returning an error code,
     * if the allocator is out of memory. Alternatively, the implementation MAY return an error
     * code in this case.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqAllocator_allocate()}.
     */
    public void allocate(DataDescriptor descriptor, long bytes, long align, MemorySegment address) {
        try {
            DaqAllocator.allocate(requireLivePointer(), (descriptor == null ? MemorySegment.NULL : descriptor.requireLivePointer()), bytes, align, Ffi.orNull(address));
        } finally {
            Reference.reachabilityFence(this);
            Reference.reachabilityFence(descriptor);
        }
    }

    /**
     * Releases a chunk of memory allocated by allocate().
     * &#64;param address The address of the allocated memory to release.
     * The implementation MUST ignore calls where &#64;p address is null.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqAllocator_free()}.
     */
    public void free(MemorySegment address) {
        try {
            DaqAllocator.free(requireLivePointer(), Ffi.orNull(address));
        } finally {
            Reference.reachabilityFence(this);
        }
    }

}
