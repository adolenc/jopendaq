// This file is autogenerated by tools/generate_high_level_bindings.py. Do not edit.

package com.opendaq;

import java.lang.foreign.Arena;
import java.lang.foreign.MemorySegment;
import java.lang.foreign.ValueLayout;
import java.lang.ref.Reference;
import java.util.List;
import java.util.Map;

import com.opendaq.lowlevel.*;

/**
 * Represents a heterogeneous collection of objects that can be individually accessed by index.
 *
 * &lt;p&gt;Wraps the openDAQ {&#64;code daqList} interface.
 */
public class ObjectList extends BaseObject {

    /** Adopt an owned reference (see {@link DaqObject#DaqObject}). */
    public ObjectList(MemorySegment pointer) {
        super(pointer);
    }

    /**
     * Calls the openDAQ C function {&#64;code daqList_createList()}.
     */
    public ObjectList() {
        super(create$createList());
    }

    private static MemorySegment create$createList() {
        return DaqList.createList();
    }

    /**
     * Removes all elements from the list.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqList_clear()}.
     */
    public void clear() {
        try {
            DaqList.clear(requireLivePointer());
        } finally {
            Reference.reachabilityFence(this);
        }
    }

    /**
     * Creates and returns the stop iterator of the list.
     * &#64;param[out] iterator The stop iterator.
     * Use iterators to iterate through the elements.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqList_createEndIterator()}.
     */
    public Iterator createEndIterator() {
        try {
            return DaqObject.wrap(DaqList.createEndIterator(requireLivePointer()), Iterator.class);
        } finally {
            Reference.reachabilityFence(this);
        }
    }

    /**
     * Creates and returns the start iterator of the list.
     * &#64;param[out] iterator The start iterator.
     * Use iterators to iterate through the elements.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqList_createStartIterator()}.
     */
    public Iterator createStartIterator() {
        try {
            return DaqObject.wrap(DaqList.createStartIterator(requireLivePointer()), Iterator.class);
        } finally {
            Reference.reachabilityFence(this);
        }
    }

    /**
     * Deletes the element at a specific position.
     * &#64;param index The zero-based index of the element to remove.
     * If the client needs the element deleted, it should use `removeAt` method.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqList_deleteAt()}.
     */
    public void deleteAt(long index) {
        try {
            DaqList.deleteAt(requireLivePointer(), index);
        } finally {
            Reference.reachabilityFence(this);
        }
    }

    /**
     * Gets the number of elements contained in the list.
     * &#64;param[out] size The number of elements contained in the list.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqList_getCount()}.
     */
    public long getCount() {
        try {
            return DaqList.getCount(requireLivePointer());
        } finally {
            Reference.reachabilityFence(this);
        }
    }

    /**
     * Gets the element at a specific position.
     * &#64;param index The zero-based index of the element to get.
     * &#64;param[out] obj The element at the specified index.
     * The reference count of the element that is retrieved is incremented. The client is
     * responsible for calling `releaseRef` when the element is no longer needed.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqList_getItemAt()}.
     */
    public BaseObject getItemAt(long index) {
        try {
            return DaqObject.wrap(DaqList.getItemAt(requireLivePointer(), index), BaseObject.class);
        } finally {
            Reference.reachabilityFence(this);
        }
    }

    /**
     * Inserts the element at a specific position.
     * &#64;param index The zero-based index of the element to insert.
     * &#64;param obj The element to insert at the specified index.
     * The reference count of the element is incremented.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqList_insertAt()}.
     */
    public void insertAt(long index, Object obj) {
        try {
            Box.Boxed obj$b = Box.box(obj);
            try {
                DaqList.insertAt(requireLivePointer(), index, obj$b.segment());
            } finally { Box.releaseBoxed(obj$b); }
        } finally {
            Reference.reachabilityFence(this);
        }
    }

    /**
     * Inserts the element at the end of the list without incrementing the reference count.
     * &#64;param obj The element to insert.
     * The reference count of the element is not incremented. The client can use this method when it no
     * longer needs to access the element after calling the method.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqList_moveBack()}.
     */
    public void moveBack(Object obj) {
        try {
            Box.Boxed obj$b = Box.box(obj);
            try {
                DaqList.moveBack(requireLivePointer(), obj$b.segment());
            } finally { Box.releaseBoxed(obj$b); }
        } finally {
            Reference.reachabilityFence(this);
        }
    }

    /**
     * Inserts the element at the start of the list without incrementing the reference count.
     * &#64;param obj The element to insert.
     * The reference count of the element is not incremented. The client can use this method when it no
     * longer needs to access the element after calling the method.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqList_moveFront()}.
     */
    public void moveFront(Object obj) {
        try {
            Box.Boxed obj$b = Box.box(obj);
            try {
                DaqList.moveFront(requireLivePointer(), obj$b.segment());
            } finally { Box.releaseBoxed(obj$b); }
        } finally {
            Reference.reachabilityFence(this);
        }
    }

    /**
     * Gets the element from the end of the list.
     * &#64;param[out] obj The extracted element.
     * The reference count of the element that is retrieved is incremented. The client is
     * responsible for calling `releaseRef` when the element is no longer needed.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqList_popBack()}.
     */
    public BaseObject popBack() {
        try {
            return DaqObject.wrap(DaqList.popBack(requireLivePointer()), BaseObject.class);
        } finally {
            Reference.reachabilityFence(this);
        }
    }

    /**
     * Gets the element from the start of the list.
     * &#64;param[out] obj The extracted element.
     * The reference count of the element that is retrieved is incremented. The client is
     * responsible for calling `releaseRef` when the element is no longer needed.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqList_popFront()}.
     */
    public BaseObject popFront() {
        try {
            return DaqObject.wrap(DaqList.popFront(requireLivePointer()), BaseObject.class);
        } finally {
            Reference.reachabilityFence(this);
        }
    }

    /**
     * Inserts the element at the end of the list.
     * &#64;param obj The element to insert.
     * The reference count of the element is incremented.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqList_pushBack()}.
     */
    public void pushBack(Object obj) {
        try {
            Box.Boxed obj$b = Box.box(obj);
            try {
                DaqList.pushBack(requireLivePointer(), obj$b.segment());
            } finally { Box.releaseBoxed(obj$b); }
        } finally {
            Reference.reachabilityFence(this);
        }
    }

    /**
     * Inserts the element at the start of the list.
     * &#64;param obj The element to insert.
     * The reference count of the element is incremented.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqList_pushFront()}.
     */
    public void pushFront(Object obj) {
        try {
            Box.Boxed obj$b = Box.box(obj);
            try {
                DaqList.pushFront(requireLivePointer(), obj$b.segment());
            } finally { Box.releaseBoxed(obj$b); }
        } finally {
            Reference.reachabilityFence(this);
        }
    }

    /**
     * Removes the element at a specific position.
     * &#64;param index The zero-based index of the element to remove.
     * &#64;param[out] obj The removed element.
     * The client is responsible for calling `releaseRef` when the element is no longer needed.
     * If the client does not need the element after it is removed, it should call `delete` method.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqList_removeAt()}.
     */
    public BaseObject removeAt(long index) {
        try {
            return DaqObject.wrap(DaqList.removeAt(requireLivePointer(), index), BaseObject.class);
        } finally {
            Reference.reachabilityFence(this);
        }
    }

    /**
     * Sets the element at a specific position.
     * &#64;param index The zero-based index of the element to set.
     * &#64;param obj The element to set at the specified index.
     * The reference count of the element is incremented.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqList_setItemAt()}.
     */
    public void setItemAt(long index, Object obj) {
        try {
            Box.Boxed obj$b = Box.box(obj);
            try {
                DaqList.setItemAt(requireLivePointer(), index, obj$b.segment());
            } finally { Box.releaseBoxed(obj$b); }
        } finally {
            Reference.reachabilityFence(this);
        }
    }

}
