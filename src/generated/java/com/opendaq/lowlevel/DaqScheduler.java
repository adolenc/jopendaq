// This file is autogenerated by tools/generate_low_level_bindings.py. Do not edit.

package com.opendaq.lowlevel;

import java.lang.foreign.Arena;
import java.lang.foreign.FunctionDescriptor;
import java.lang.foreign.MemorySegment;
import java.lang.invoke.MethodHandle;

import static java.lang.foreign.ValueLayout.*;

/** Raw FFI bindings for the daqScheduler_* functions of the openDAQ C API. */
public final class DaqScheduler {

    private DaqScheduler() {}

    private static final MethodHandle MH_createScheduler =
        Ffi.downcallOrNull("daqScheduler_createScheduler", FunctionDescriptor.of(JAVA_INT, ADDRESS, ADDRESS, JAVA_LONG));
    private static final MethodHandle MH_createSchedulerWithMainLoop =
        Ffi.downcallOrNull("daqScheduler_createSchedulerWithMainLoop", FunctionDescriptor.of(JAVA_INT, ADDRESS, ADDRESS, JAVA_LONG, JAVA_BYTE));
    private static final MethodHandle MH_getInterfaceId =
        Ffi.downcallOrNull("daqScheduler_getInterfaceId", FunctionDescriptor.ofVoid(ADDRESS));
    private static final MethodHandle MH_isMainLoopSet =
        Ffi.downcallOrNull("daqScheduler_isMainLoopSet", FunctionDescriptor.of(JAVA_INT, ADDRESS, ADDRESS));
    private static final MethodHandle MH_isMultiThreaded =
        Ffi.downcallOrNull("daqScheduler_isMultiThreaded", FunctionDescriptor.of(JAVA_INT, ADDRESS, ADDRESS));
    private static final MethodHandle MH_runMainLoop =
        Ffi.downcallOrNull("daqScheduler_runMainLoop", FunctionDescriptor.of(JAVA_INT, ADDRESS, JAVA_LONG));
    private static final MethodHandle MH_runMainLoopIteration =
        Ffi.downcallOrNull("daqScheduler_runMainLoopIteration", FunctionDescriptor.of(JAVA_INT, ADDRESS));
    private static final MethodHandle MH_scheduleFunction =
        Ffi.downcallOrNull("daqScheduler_scheduleFunction", FunctionDescriptor.of(JAVA_INT, ADDRESS, ADDRESS, ADDRESS));
    private static final MethodHandle MH_scheduleGraph =
        Ffi.downcallOrNull("daqScheduler_scheduleGraph", FunctionDescriptor.of(JAVA_INT, ADDRESS, ADDRESS, ADDRESS));
    private static final MethodHandle MH_scheduleWork =
        Ffi.downcallOrNull("daqScheduler_scheduleWork", FunctionDescriptor.of(JAVA_INT, ADDRESS, ADDRESS));
    private static final MethodHandle MH_scheduleWorkOnMainLoop =
        Ffi.downcallOrNull("daqScheduler_scheduleWorkOnMainLoop", FunctionDescriptor.of(JAVA_INT, ADDRESS, ADDRESS));
    private static final MethodHandle MH_stop =
        Ffi.downcallOrNull("daqScheduler_stop", FunctionDescriptor.of(JAVA_INT, ADDRESS));
    private static final MethodHandle MH_stopMainLoop =
        Ffi.downcallOrNull("daqScheduler_stopMainLoop", FunctionDescriptor.of(JAVA_INT, ADDRESS));
    private static final MethodHandle MH_waitAll =
        Ffi.downcallOrNull("daqScheduler_waitAll", FunctionDescriptor.of(JAVA_INT, ADDRESS));

    /** Calls {@code daqScheduler_createScheduler()}. */
    public static MemorySegment createScheduler(MemorySegment logger, long numWorkers) {
        try (Arena arena = Arena.ofConfined()) {
            MemorySegment objSlot = arena.allocate(ADDRESS);
            int err = (int) Ffi.require(MH_createScheduler, "daqScheduler_createScheduler").invokeExact(objSlot, Ffi.orNull(logger), numWorkers);
            Ffi.checkError(err, "daqScheduler_createScheduler");
            return objSlot.get(ADDRESS, 0);
        } catch (Throwable t) { throw Ffi.rethrow(t); }
    }

    /** Calls {@code daqScheduler_createSchedulerWithMainLoop()}. */
    public static MemorySegment createSchedulerWithMainLoop(MemorySegment logger, long numWorkers, boolean useMainLoop) {
        try (Arena arena = Arena.ofConfined()) {
            MemorySegment objSlot = arena.allocate(ADDRESS);
            int err = (int) Ffi.require(MH_createSchedulerWithMainLoop, "daqScheduler_createSchedulerWithMainLoop").invokeExact(objSlot, Ffi.orNull(logger), numWorkers, (byte) (useMainLoop ? 1 : 0));
            Ffi.checkError(err, "daqScheduler_createSchedulerWithMainLoop");
            return objSlot.get(ADDRESS, 0);
        } catch (Throwable t) { throw Ffi.rethrow(t); }
    }

    /** Calls {@code daqScheduler_getInterfaceId()} with caller-allocated out slots. */
    public static void getInterfaceId(MemorySegment intfId) {
        try {
            Ffi.require(MH_getInterfaceId, "daqScheduler_getInterfaceId").invokeExact(Ffi.orNull(intfId));
        } catch (Throwable t) { throw Ffi.rethrow(t); }
    }

    /** Checks if the main loop is currently set.  Calls {@code daqScheduler_isMainLoopSet()}. */
    public static boolean isMainLoopSet(MemorySegment self) {
        try (Arena arena = Arena.ofConfined()) {
            MemorySegment isSetSlot = arena.allocate(JAVA_BYTE);
            int err = (int) Ffi.require(MH_isMainLoopSet, "daqScheduler_isMainLoopSet").invokeExact(Ffi.orNull(self), isSetSlot);
            Ffi.checkError(err, "daqScheduler_isMainLoopSet");
            return isSetSlot.get(JAVA_BYTE, 0) != 0;
        } catch (Throwable t) { throw Ffi.rethrow(t); }
    }

    /** Returns whether more than one worker thread is used.  Calls {@code daqScheduler_isMultiThreaded()}. */
    public static boolean isMultiThreaded(MemorySegment self) {
        try (Arena arena = Arena.ofConfined()) {
            MemorySegment multiThreadedSlot = arena.allocate(JAVA_BYTE);
            int err = (int) Ffi.require(MH_isMultiThreaded, "daqScheduler_isMultiThreaded").invokeExact(Ffi.orNull(self), multiThreadedSlot);
            Ffi.checkError(err, "daqScheduler_isMultiThreaded");
            return multiThreadedSlot.get(JAVA_BYTE, 0) != 0;
        } catch (Throwable t) { throw Ffi.rethrow(t); }
    }

    /** Starts and blocks the main event loop, executing scheduled tasks.  Calls {@code daqScheduler_runMainLoop()}. */
    public static void runMainLoop(MemorySegment self, long loopTime) {
        try {
            int err = (int) Ffi.require(MH_runMainLoop, "daqScheduler_runMainLoop").invokeExact(Ffi.orNull(self), loopTime);
            Ffi.checkError(err, "daqScheduler_runMainLoop");
        } catch (Throwable t) { throw Ffi.rethrow(t); }
    }

    /** Executes a single iteration of the main loop, processing scheduled tasks.  Calls {@code daqScheduler_runMainLoopIteration()}. */
    public static void runMainLoopIteration(MemorySegment self) {
        try {
            int err = (int) Ffi.require(MH_runMainLoopIteration, "daqScheduler_runMainLoopIteration").invokeExact(Ffi.orNull(self));
            Ffi.checkError(err, "daqScheduler_runMainLoopIteration");
        } catch (Throwable t) { throw Ffi.rethrow(t); }
    }

    /** Schedules the specified &#64;p work function to run on the thread-pool. The call does not block but immediately returns an &#64;p awaitable that represents the asynchronous execution. It can be waited upon and queried for status and result.  Calls {@code daqScheduler_scheduleFunction()}. */
    public static MemorySegment scheduleFunction(MemorySegment self, MemorySegment function) {
        try (Arena arena = Arena.ofConfined()) {
            MemorySegment awaitableSlot = arena.allocate(ADDRESS);
            int err = (int) Ffi.require(MH_scheduleFunction, "daqScheduler_scheduleFunction").invokeExact(Ffi.orNull(self), Ffi.orNull(function), awaitableSlot);
            Ffi.checkError(err, "daqScheduler_scheduleFunction");
            return awaitableSlot.get(ADDRESS, 0);
        } catch (Throwable t) { throw Ffi.rethrow(t); }
    }

    /** Schedules the specified dependency &#64;p graph to run on the thread-pool. The call does not block but immediately returns an &#64;p awaitable that represents the asynchronous execution. It can be waited upon and queried for status and result. &lt;b&gt;Any exceptions that occur during the graph execution are silently ignored.&lt;/b&gt;  Calls {@code daqScheduler_scheduleGraph()}. */
    public static MemorySegment scheduleGraph(MemorySegment self, MemorySegment graph) {
        try (Arena arena = Arena.ofConfined()) {
            MemorySegment awaitableSlot = arena.allocate(ADDRESS);
            int err = (int) Ffi.require(MH_scheduleGraph, "daqScheduler_scheduleGraph").invokeExact(Ffi.orNull(self), Ffi.orNull(graph), awaitableSlot);
            Ffi.checkError(err, "daqScheduler_scheduleGraph");
            return awaitableSlot.get(ADDRESS, 0);
        } catch (Throwable t) { throw Ffi.rethrow(t); }
    }

    /** Schedules the specified work callback to run on the thread-pool. The call does not block.  Calls {@code daqScheduler_scheduleWork()}. */
    public static void scheduleWork(MemorySegment self, MemorySegment work) {
        try {
            int err = (int) Ffi.require(MH_scheduleWork, "daqScheduler_scheduleWork").invokeExact(Ffi.orNull(self), Ffi.orNull(work));
            Ffi.checkError(err, "daqScheduler_scheduleWork");
        } catch (Throwable t) { throw Ffi.rethrow(t); }
    }

    /** Schedules a task to be executed by the main loop.  Calls {@code daqScheduler_scheduleWorkOnMainLoop()}. */
    public static void scheduleWorkOnMainLoop(MemorySegment self, MemorySegment work) {
        try {
            int err = (int) Ffi.require(MH_scheduleWorkOnMainLoop, "daqScheduler_scheduleWorkOnMainLoop").invokeExact(Ffi.orNull(self), Ffi.orNull(work));
            Ffi.checkError(err, "daqScheduler_scheduleWorkOnMainLoop");
        } catch (Throwable t) { throw Ffi.rethrow(t); }
    }

    /** Cancels all outstanding work and waits for the remaining to complete. After this point the scheduler does not allow any new work or graphs for scheduling.  Calls {@code daqScheduler_stop()}. */
    public static void stop(MemorySegment self) {
        try {
            int err = (int) Ffi.require(MH_stop, "daqScheduler_stop").invokeExact(Ffi.orNull(self));
            Ffi.checkError(err, "daqScheduler_stop");
        } catch (Throwable t) { throw Ffi.rethrow(t); }
    }

    /** Signals the main loop to stop processing and return from &#64;ref runMainLoop.  Calls {@code daqScheduler_stopMainLoop()}. */
    public static void stopMainLoop(MemorySegment self) {
        try {
            int err = (int) Ffi.require(MH_stopMainLoop, "daqScheduler_stopMainLoop").invokeExact(Ffi.orNull(self));
            Ffi.checkError(err, "daqScheduler_stopMainLoop");
        } catch (Throwable t) { throw Ffi.rethrow(t); }
    }

    /** Waits fo all current scheduled work and tasks to complete.  Calls {@code daqScheduler_waitAll()}. */
    public static void waitAll(MemorySegment self) {
        try {
            int err = (int) Ffi.require(MH_waitAll, "daqScheduler_waitAll").invokeExact(Ffi.orNull(self));
            Ffi.checkError(err, "daqScheduler_waitAll");
        } catch (Throwable t) { throw Ffi.rethrow(t); }
    }

}
