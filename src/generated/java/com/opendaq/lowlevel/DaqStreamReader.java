// This file is autogenerated by tools/generate_low_level_bindings.py. Do not edit.

package com.opendaq.lowlevel;

import java.lang.foreign.Arena;
import java.lang.foreign.FunctionDescriptor;
import java.lang.foreign.MemorySegment;
import java.lang.invoke.MethodHandle;

import static java.lang.foreign.ValueLayout.*;
import com.opendaq.ReadMode;
import com.opendaq.ReadTimeoutType;
import com.opendaq.SampleType;

/** Raw FFI bindings for the daqStreamReader_* functions of the openDAQ C API. */
public final class DaqStreamReader {

    private DaqStreamReader() {}

    private static final MethodHandle MH_createStreamReader =
        Ffi.downcallOrNull("daqStreamReader_createStreamReader", FunctionDescriptor.of(JAVA_INT, ADDRESS, ADDRESS, JAVA_INT, JAVA_INT, JAVA_INT, JAVA_INT));
    private static final MethodHandle MH_createStreamReaderFromExisting =
        Ffi.downcallOrNull("daqStreamReader_createStreamReaderFromExisting", FunctionDescriptor.of(JAVA_INT, ADDRESS, ADDRESS, JAVA_INT, JAVA_INT));
    private static final MethodHandle MH_createStreamReaderFromPort =
        Ffi.downcallOrNull("daqStreamReader_createStreamReaderFromPort", FunctionDescriptor.of(JAVA_INT, ADDRESS, ADDRESS, JAVA_INT, JAVA_INT, JAVA_INT, JAVA_INT));
    private static final MethodHandle MH_getInterfaceId =
        Ffi.downcallOrNull("daqStreamReader_getInterfaceId", FunctionDescriptor.ofVoid(ADDRESS));
    private static final MethodHandle MH_read =
        Ffi.downcallOrNull("daqStreamReader_read", FunctionDescriptor.of(JAVA_INT, ADDRESS, ADDRESS, ADDRESS, JAVA_LONG, ADDRESS));
    private static final MethodHandle MH_readWithDomain =
        Ffi.downcallOrNull("daqStreamReader_readWithDomain", FunctionDescriptor.of(JAVA_INT, ADDRESS, ADDRESS, ADDRESS, ADDRESS, JAVA_LONG, ADDRESS));
    private static final MethodHandle MH_skipSamples =
        Ffi.downcallOrNull("daqStreamReader_skipSamples", FunctionDescriptor.of(JAVA_INT, ADDRESS, ADDRESS, ADDRESS));

    /** Calls {@code daqStreamReader_createStreamReader()}. */
    public static MemorySegment createStreamReader(MemorySegment signal, SampleType valueReadType, SampleType domainReadType, ReadMode mode, ReadTimeoutType timeoutType) {
        try (Arena arena = Arena.ofConfined()) {
            MemorySegment objSlot = arena.allocate(ADDRESS);
            int err = (int) Ffi.require(MH_createStreamReader, "daqStreamReader_createStreamReader").invokeExact(objSlot, Ffi.orNull(signal), valueReadType.value(), domainReadType.value(), mode.value(), timeoutType.value());
            Ffi.checkError(err, "daqStreamReader_createStreamReader");
            return objSlot.get(ADDRESS, 0);
        } catch (Throwable t) { throw Ffi.rethrow(t); }
    }

    /** Calls {@code daqStreamReader_createStreamReaderFromExisting()}. */
    public static MemorySegment createStreamReaderFromExisting(MemorySegment invalidatedReader, SampleType valueReadType, SampleType domainReadType) {
        try (Arena arena = Arena.ofConfined()) {
            MemorySegment objSlot = arena.allocate(ADDRESS);
            int err = (int) Ffi.require(MH_createStreamReaderFromExisting, "daqStreamReader_createStreamReaderFromExisting").invokeExact(objSlot, Ffi.orNull(invalidatedReader), valueReadType.value(), domainReadType.value());
            Ffi.checkError(err, "daqStreamReader_createStreamReaderFromExisting");
            return objSlot.get(ADDRESS, 0);
        } catch (Throwable t) { throw Ffi.rethrow(t); }
    }

    /** Calls {@code daqStreamReader_createStreamReaderFromPort()}. */
    public static MemorySegment createStreamReaderFromPort(MemorySegment port, SampleType valueReadType, SampleType domainReadType, ReadMode readMode, ReadTimeoutType timeoutType) {
        try (Arena arena = Arena.ofConfined()) {
            MemorySegment objSlot = arena.allocate(ADDRESS);
            int err = (int) Ffi.require(MH_createStreamReaderFromPort, "daqStreamReader_createStreamReaderFromPort").invokeExact(objSlot, Ffi.orNull(port), valueReadType.value(), domainReadType.value(), readMode.value(), timeoutType.value());
            Ffi.checkError(err, "daqStreamReader_createStreamReaderFromPort");
            return objSlot.get(ADDRESS, 0);
        } catch (Throwable t) { throw Ffi.rethrow(t); }
    }

    /** Calls {@code daqStreamReader_getInterfaceId()} with caller-allocated out slots. */
    public static void getInterfaceId(MemorySegment intfId) {
        try {
            Ffi.require(MH_getInterfaceId, "daqStreamReader_getInterfaceId").invokeExact(Ffi.orNull(intfId));
        } catch (Throwable t) { throw Ffi.rethrow(t); }
    }

    /** Result of {@link #read}. */
    public record ReadResult(long count, MemorySegment status) {}

    /** Copies at maximum the next `count` unread samples to the values buffer. The amount actually read is returned through the `count` parameter.  Calls {@code daqStreamReader_read()}. */
    public static ReadResult read(MemorySegment self, MemorySegment samples, long count, long timeoutMs) {
        try (Arena arena = Arena.ofConfined()) {
            MemorySegment countSlot = arena.allocate(JAVA_LONG);
            countSlot.set(JAVA_LONG, 0, count);
            MemorySegment statusSlot = arena.allocate(ADDRESS);
            int err = (int) Ffi.require(MH_read, "daqStreamReader_read").invokeExact(Ffi.orNull(self), Ffi.orNull(samples), countSlot, timeoutMs, statusSlot);
            Ffi.checkError(err, "daqStreamReader_read");
            return new ReadResult(countSlot.get(JAVA_LONG, 0), statusSlot.get(ADDRESS, 0));
        } catch (Throwable t) { throw Ffi.rethrow(t); }
    }

    /** Result of {@link #readWithDomain}. */
    public record ReadWithDomainResult(long count, MemorySegment status) {}

    /** Copies at maximum the next `count` unread samples and clock-stamps to the `samples` and `domain` buffers. The amount actually read is returned through the `count` parameter.  Calls {@code daqStreamReader_readWithDomain()}. */
    public static ReadWithDomainResult readWithDomain(MemorySegment self, MemorySegment samples, MemorySegment domain, long count, long timeoutMs) {
        try (Arena arena = Arena.ofConfined()) {
            MemorySegment countSlot = arena.allocate(JAVA_LONG);
            countSlot.set(JAVA_LONG, 0, count);
            MemorySegment statusSlot = arena.allocate(ADDRESS);
            int err = (int) Ffi.require(MH_readWithDomain, "daqStreamReader_readWithDomain").invokeExact(Ffi.orNull(self), Ffi.orNull(samples), Ffi.orNull(domain), countSlot, timeoutMs, statusSlot);
            Ffi.checkError(err, "daqStreamReader_readWithDomain");
            return new ReadWithDomainResult(countSlot.get(JAVA_LONG, 0), statusSlot.get(ADDRESS, 0));
        } catch (Throwable t) { throw Ffi.rethrow(t); }
    }

    /** Result of {@link #skipSamples}. */
    public record SkipSamplesResult(long count, MemorySegment status) {}

    /** Skips the specified amount of samples.  Calls {@code daqStreamReader_skipSamples()}. */
    public static SkipSamplesResult skipSamples(MemorySegment self, long count) {
        try (Arena arena = Arena.ofConfined()) {
            MemorySegment countSlot = arena.allocate(JAVA_LONG);
            countSlot.set(JAVA_LONG, 0, count);
            MemorySegment statusSlot = arena.allocate(ADDRESS);
            int err = (int) Ffi.require(MH_skipSamples, "daqStreamReader_skipSamples").invokeExact(Ffi.orNull(self), countSlot, statusSlot);
            Ffi.checkError(err, "daqStreamReader_skipSamples");
            return new SkipSamplesResult(countSlot.get(JAVA_LONG, 0), statusSlot.get(ADDRESS, 0));
        } catch (Throwable t) { throw Ffi.rethrow(t); }
    }

}
