// This file is autogenerated by tools/generate_low_level_bindings.py. Do not edit.

package com.opendaq.lowlevel;

import java.lang.foreign.Arena;
import java.lang.foreign.FunctionDescriptor;
import java.lang.foreign.MemorySegment;
import java.lang.invoke.MethodHandle;

import static java.lang.foreign.ValueLayout.*;

/** Raw FFI bindings for the daqCoercer_* functions of the openDAQ C API. */
public final class DaqCoercer {

    private DaqCoercer() {}

    private static final MethodHandle MH_coerce =
        Ffi.downcallOrNull("daqCoercer_coerce", FunctionDescriptor.of(JAVA_INT, ADDRESS, ADDRESS, ADDRESS, ADDRESS));
    private static final MethodHandle MH_coerceNoLock =
        Ffi.downcallOrNull("daqCoercer_coerceNoLock", FunctionDescriptor.of(JAVA_INT, ADDRESS, ADDRESS, ADDRESS, ADDRESS));
    private static final MethodHandle MH_createCoercer =
        Ffi.downcallOrNull("daqCoercer_createCoercer", FunctionDescriptor.of(JAVA_INT, ADDRESS, ADDRESS));
    private static final MethodHandle MH_getEval =
        Ffi.downcallOrNull("daqCoercer_getEval", FunctionDescriptor.of(JAVA_INT, ADDRESS, ADDRESS));
    private static final MethodHandle MH_getInterfaceId =
        Ffi.downcallOrNull("daqCoercer_getInterfaceId", FunctionDescriptor.ofVoid(ADDRESS));

    /** Coerces `value` to match the coercion restrictions and outputs the result.  Calls {@code daqCoercer_coerce()}. */
    public static MemorySegment coerce(MemorySegment self, MemorySegment propObj, MemorySegment value) {
        try (Arena arena = Arena.ofConfined()) {
            MemorySegment resultSlot = arena.allocate(ADDRESS);
            int err = (int) Ffi.require(MH_coerce, "daqCoercer_coerce").invokeExact(Ffi.orNull(self), Ffi.orNull(propObj), Ffi.orNull(value), resultSlot);
            Ffi.checkError(err, "daqCoercer_coerce");
            return resultSlot.get(ADDRESS, 0);
        } catch (Throwable t) { throw Ffi.rethrow(t); }
    }

    /** Calls {@code daqCoercer_coerceNoLock()}. */
    public static MemorySegment coerceNoLock(MemorySegment self, MemorySegment propObj, MemorySegment value) {
        try (Arena arena = Arena.ofConfined()) {
            MemorySegment resultSlot = arena.allocate(ADDRESS);
            int err = (int) Ffi.require(MH_coerceNoLock, "daqCoercer_coerceNoLock").invokeExact(Ffi.orNull(self), Ffi.orNull(propObj), Ffi.orNull(value), resultSlot);
            Ffi.checkError(err, "daqCoercer_coerceNoLock");
            return resultSlot.get(ADDRESS, 0);
        } catch (Throwable t) { throw Ffi.rethrow(t); }
    }

    /** Creates a Coercer with the given evaluation expression.  Calls {@code daqCoercer_createCoercer()}. */
    public static MemorySegment createCoercer(MemorySegment eval) {
        try (Arena arena = Arena.ofConfined()) {
            MemorySegment objSlot = arena.allocate(ADDRESS);
            int err = (int) Ffi.require(MH_createCoercer, "daqCoercer_createCoercer").invokeExact(objSlot, Ffi.orNull(eval));
            Ffi.checkError(err, "daqCoercer_createCoercer");
            return objSlot.get(ADDRESS, 0);
        } catch (Throwable t) { throw Ffi.rethrow(t); }
    }

    /** Gets the string expression used when creating the coercer.  Calls {@code daqCoercer_getEval()}. */
    public static MemorySegment getEval(MemorySegment self) {
        try (Arena arena = Arena.ofConfined()) {
            MemorySegment evalSlot = arena.allocate(ADDRESS);
            int err = (int) Ffi.require(MH_getEval, "daqCoercer_getEval").invokeExact(Ffi.orNull(self), evalSlot);
            Ffi.checkError(err, "daqCoercer_getEval");
            return evalSlot.get(ADDRESS, 0);
        } catch (Throwable t) { throw Ffi.rethrow(t); }
    }

    /** Calls {@code daqCoercer_getInterfaceId()} with caller-allocated out slots. */
    public static void getInterfaceId(MemorySegment intfId) {
        try {
            Ffi.require(MH_getInterfaceId, "daqCoercer_getInterfaceId").invokeExact(Ffi.orNull(intfId));
        } catch (Throwable t) { throw Ffi.rethrow(t); }
    }

}
