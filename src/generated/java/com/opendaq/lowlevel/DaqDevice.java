// This file is autogenerated by tools/generate_low_level_bindings.py. Do not edit.

package com.opendaq.lowlevel;

import java.lang.foreign.Arena;
import java.lang.foreign.FunctionDescriptor;
import java.lang.foreign.MemorySegment;
import java.lang.invoke.MethodHandle;

import static java.lang.foreign.ValueLayout.*;
import com.opendaq.OperationModeType;

/** Raw FFI bindings for the daqDevice_* functions of the openDAQ C API. */
public final class DaqDevice {

    private DaqDevice() {}

    private static final MethodHandle MH_addDevice =
        Ffi.downcallOrNull("daqDevice_addDevice", FunctionDescriptor.of(JAVA_INT, ADDRESS, ADDRESS, ADDRESS, ADDRESS));
    private static final MethodHandle MH_addDevices =
        Ffi.downcallOrNull("daqDevice_addDevices", FunctionDescriptor.of(JAVA_INT, ADDRESS, ADDRESS, ADDRESS, ADDRESS, ADDRESS));
    private static final MethodHandle MH_addFunctionBlock =
        Ffi.downcallOrNull("daqDevice_addFunctionBlock", FunctionDescriptor.of(JAVA_INT, ADDRESS, ADDRESS, ADDRESS, ADDRESS));
    private static final MethodHandle MH_addServer =
        Ffi.downcallOrNull("daqDevice_addServer", FunctionDescriptor.of(JAVA_INT, ADDRESS, ADDRESS, ADDRESS, ADDRESS));
    private static final MethodHandle MH_addStreaming =
        Ffi.downcallOrNull("daqDevice_addStreaming", FunctionDescriptor.of(JAVA_INT, ADDRESS, ADDRESS, ADDRESS, ADDRESS));
    private static final MethodHandle MH_createClient =
        Ffi.downcallOrNull("daqDevice_createClient", FunctionDescriptor.of(JAVA_INT, ADDRESS, ADDRESS, ADDRESS, ADDRESS, ADDRESS));
    private static final MethodHandle MH_createDefaultAddDeviceConfig =
        Ffi.downcallOrNull("daqDevice_createDefaultAddDeviceConfig", FunctionDescriptor.of(JAVA_INT, ADDRESS, ADDRESS));
    private static final MethodHandle MH_getAvailableDeviceTypes =
        Ffi.downcallOrNull("daqDevice_getAvailableDeviceTypes", FunctionDescriptor.of(JAVA_INT, ADDRESS, ADDRESS));
    private static final MethodHandle MH_getAvailableDevices =
        Ffi.downcallOrNull("daqDevice_getAvailableDevices", FunctionDescriptor.of(JAVA_INT, ADDRESS, ADDRESS));
    private static final MethodHandle MH_getAvailableFunctionBlockTypes =
        Ffi.downcallOrNull("daqDevice_getAvailableFunctionBlockTypes", FunctionDescriptor.of(JAVA_INT, ADDRESS, ADDRESS));
    private static final MethodHandle MH_getAvailableOperationModes =
        Ffi.downcallOrNull("daqDevice_getAvailableOperationModes", FunctionDescriptor.of(JAVA_INT, ADDRESS, ADDRESS));
    private static final MethodHandle MH_getChannels =
        Ffi.downcallOrNull("daqDevice_getChannels", FunctionDescriptor.of(JAVA_INT, ADDRESS, ADDRESS, ADDRESS));
    private static final MethodHandle MH_getChannelsRecursive =
        Ffi.downcallOrNull("daqDevice_getChannelsRecursive", FunctionDescriptor.of(JAVA_INT, ADDRESS, ADDRESS, ADDRESS));
    private static final MethodHandle MH_getConnectionStatusContainer =
        Ffi.downcallOrNull("daqDevice_getConnectionStatusContainer", FunctionDescriptor.of(JAVA_INT, ADDRESS, ADDRESS));
    private static final MethodHandle MH_getCustomComponents =
        Ffi.downcallOrNull("daqDevice_getCustomComponents", FunctionDescriptor.of(JAVA_INT, ADDRESS, ADDRESS));
    private static final MethodHandle MH_getDevices =
        Ffi.downcallOrNull("daqDevice_getDevices", FunctionDescriptor.of(JAVA_INT, ADDRESS, ADDRESS, ADDRESS));
    private static final MethodHandle MH_getDomain =
        Ffi.downcallOrNull("daqDevice_getDomain", FunctionDescriptor.of(JAVA_INT, ADDRESS, ADDRESS));
    private static final MethodHandle MH_getFunctionBlocks =
        Ffi.downcallOrNull("daqDevice_getFunctionBlocks", FunctionDescriptor.of(JAVA_INT, ADDRESS, ADDRESS, ADDRESS));
    private static final MethodHandle MH_getInfo =
        Ffi.downcallOrNull("daqDevice_getInfo", FunctionDescriptor.of(JAVA_INT, ADDRESS, ADDRESS));
    private static final MethodHandle MH_getInputsOutputsFolder =
        Ffi.downcallOrNull("daqDevice_getInputsOutputsFolder", FunctionDescriptor.of(JAVA_INT, ADDRESS, ADDRESS));
    private static final MethodHandle MH_getInterfaceId =
        Ffi.downcallOrNull("daqDevice_getInterfaceId", FunctionDescriptor.ofVoid(ADDRESS));
    private static final MethodHandle MH_getLog =
        Ffi.downcallOrNull("daqDevice_getLog", FunctionDescriptor.of(JAVA_INT, ADDRESS, ADDRESS, ADDRESS, JAVA_LONG, JAVA_LONG));
    private static final MethodHandle MH_getLogFileInfos =
        Ffi.downcallOrNull("daqDevice_getLogFileInfos", FunctionDescriptor.of(JAVA_INT, ADDRESS, ADDRESS));
    private static final MethodHandle MH_getServers =
        Ffi.downcallOrNull("daqDevice_getServers", FunctionDescriptor.of(JAVA_INT, ADDRESS, ADDRESS));
    private static final MethodHandle MH_getSignals =
        Ffi.downcallOrNull("daqDevice_getSignals", FunctionDescriptor.of(JAVA_INT, ADDRESS, ADDRESS, ADDRESS));
    private static final MethodHandle MH_getSignalsRecursive =
        Ffi.downcallOrNull("daqDevice_getSignalsRecursive", FunctionDescriptor.of(JAVA_INT, ADDRESS, ADDRESS, ADDRESS));
    private static final MethodHandle MH_getSyncComponent =
        Ffi.downcallOrNull("daqDevice_getSyncComponent", FunctionDescriptor.of(JAVA_INT, ADDRESS, ADDRESS));
    private static final MethodHandle MH_getTicksSinceOrigin =
        Ffi.downcallOrNull("daqDevice_getTicksSinceOrigin", FunctionDescriptor.of(JAVA_INT, ADDRESS, ADDRESS));
    private static final MethodHandle MH_isLocked =
        Ffi.downcallOrNull("daqDevice_isLocked", FunctionDescriptor.of(JAVA_INT, ADDRESS, ADDRESS));
    private static final MethodHandle MH_loadConfiguration =
        Ffi.downcallOrNull("daqDevice_loadConfiguration", FunctionDescriptor.of(JAVA_INT, ADDRESS, ADDRESS, ADDRESS));
    private static final MethodHandle MH_lock =
        Ffi.downcallOrNull("daqDevice_lock", FunctionDescriptor.of(JAVA_INT, ADDRESS));
    private static final MethodHandle MH_removeDevice =
        Ffi.downcallOrNull("daqDevice_removeDevice", FunctionDescriptor.of(JAVA_INT, ADDRESS, ADDRESS));
    private static final MethodHandle MH_removeFunctionBlock =
        Ffi.downcallOrNull("daqDevice_removeFunctionBlock", FunctionDescriptor.of(JAVA_INT, ADDRESS, ADDRESS));
    private static final MethodHandle MH_removeServer =
        Ffi.downcallOrNull("daqDevice_removeServer", FunctionDescriptor.of(JAVA_INT, ADDRESS, ADDRESS));
    private static final MethodHandle MH_saveConfiguration =
        Ffi.downcallOrNull("daqDevice_saveConfiguration", FunctionDescriptor.of(JAVA_INT, ADDRESS, ADDRESS));
    private static final MethodHandle MH_setOperationMode =
        Ffi.downcallOrNull("daqDevice_setOperationMode", FunctionDescriptor.of(JAVA_INT, ADDRESS, JAVA_INT));
    private static final MethodHandle MH_setOperationModeRecursive =
        Ffi.downcallOrNull("daqDevice_setOperationModeRecursive", FunctionDescriptor.of(JAVA_INT, ADDRESS, JAVA_INT));
    private static final MethodHandle MH_unlock =
        Ffi.downcallOrNull("daqDevice_unlock", FunctionDescriptor.of(JAVA_INT, ADDRESS));

    /** Connects to a device at the given connection string and returns it.  Calls {@code daqDevice_addDevice()}. */
    public static MemorySegment addDevice(MemorySegment self, MemorySegment connectionString, MemorySegment config) {
        try (Arena arena = Arena.ofConfined()) {
            MemorySegment deviceSlot = arena.allocate(ADDRESS);
            int err = (int) Ffi.require(MH_addDevice, "daqDevice_addDevice").invokeExact(Ffi.orNull(self), deviceSlot, Ffi.orNull(connectionString), Ffi.orNull(config));
            Ffi.checkError(err, "daqDevice_addDevice");
            return deviceSlot.get(ADDRESS, 0);
        } catch (Throwable t) { throw Ffi.rethrow(t); }
    }

    /** Connects to multiple devices in parallel using the provided connection strings and returns the connected devices. Each connection is established concurrently to improve performance when handling multiple devices. The additions, in turn, are performed sequentially in the order specified by connectionArgs.  Calls {@code daqDevice_addDevices()}. */
    public static MemorySegment addDevices(MemorySegment self, MemorySegment connectionArgs, MemorySegment errCodes, MemorySegment errorInfos) {
        try (Arena arena = Arena.ofConfined()) {
            MemorySegment devicesSlot = arena.allocate(ADDRESS);
            int err = (int) Ffi.require(MH_addDevices, "daqDevice_addDevices").invokeExact(Ffi.orNull(self), devicesSlot, Ffi.orNull(connectionArgs), Ffi.orNull(errCodes), Ffi.orNull(errorInfos));
            Ffi.checkError(err, "daqDevice_addDevices");
            return devicesSlot.get(ADDRESS, 0);
        } catch (Throwable t) { throw Ffi.rethrow(t); }
    }

    /** Creates and adds a function block to the device with the provided unique ID and returns it.  Calls {@code daqDevice_addFunctionBlock()}. */
    public static MemorySegment addFunctionBlock(MemorySegment self, MemorySegment typeId, MemorySegment config) {
        try (Arena arena = Arena.ofConfined()) {
            MemorySegment functionBlockSlot = arena.allocate(ADDRESS);
            int err = (int) Ffi.require(MH_addFunctionBlock, "daqDevice_addFunctionBlock").invokeExact(Ffi.orNull(self), functionBlockSlot, Ffi.orNull(typeId), Ffi.orNull(config));
            Ffi.checkError(err, "daqDevice_addFunctionBlock");
            return functionBlockSlot.get(ADDRESS, 0);
        } catch (Throwable t) { throw Ffi.rethrow(t); }
    }

    /** Creates and adds to the device a server with the provided unique type ID and returns it.  Calls {@code daqDevice_addServer()}. */
    public static MemorySegment addServer(MemorySegment self, MemorySegment typeId, MemorySegment config) {
        try (Arena arena = Arena.ofConfined()) {
            MemorySegment serverSlot = arena.allocate(ADDRESS);
            int err = (int) Ffi.require(MH_addServer, "daqDevice_addServer").invokeExact(Ffi.orNull(self), Ffi.orNull(typeId), Ffi.orNull(config), serverSlot);
            Ffi.checkError(err, "daqDevice_addServer");
            return serverSlot.get(ADDRESS, 0);
        } catch (Throwable t) { throw Ffi.rethrow(t); }
    }

    /** Connects to a streaming at the given connection string, adds it as a streaming source of device and returns created streaming object.  Calls {@code daqDevice_addStreaming()}. */
    public static MemorySegment addStreaming(MemorySegment self, MemorySegment connectionString, MemorySegment config) {
        try (Arena arena = Arena.ofConfined()) {
            MemorySegment streamingSlot = arena.allocate(ADDRESS);
            int err = (int) Ffi.require(MH_addStreaming, "daqDevice_addStreaming").invokeExact(Ffi.orNull(self), streamingSlot, Ffi.orNull(connectionString), Ffi.orNull(config));
            Ffi.checkError(err, "daqDevice_addStreaming");
            return streamingSlot.get(ADDRESS, 0);
        } catch (Throwable t) { throw Ffi.rethrow(t); }
    }

    /** Creates an openDAQ client.  Calls {@code daqDevice_createClient()}. */
    public static MemorySegment createClient(MemorySegment ctx, MemorySegment localId, MemorySegment defaultDeviceInfo, MemorySegment parent) {
        try (Arena arena = Arena.ofConfined()) {
            MemorySegment objSlot = arena.allocate(ADDRESS);
            int err = (int) Ffi.require(MH_createClient, "daqDevice_createClient").invokeExact(objSlot, Ffi.orNull(ctx), Ffi.orNull(localId), Ffi.orNull(defaultDeviceInfo), Ffi.orNull(parent));
            Ffi.checkError(err, "daqDevice_createClient");
            return objSlot.get(ADDRESS, 0);
        } catch (Throwable t) { throw Ffi.rethrow(t); }
    }

    /** Creates config object that can be used when adding a device. Contains Device and Streaming default configuration for all available Device/Streaming types. Also contains general add-device configuration settings.  Calls {@code daqDevice_createDefaultAddDeviceConfig()}. */
    public static MemorySegment createDefaultAddDeviceConfig(MemorySegment self) {
        try (Arena arena = Arena.ofConfined()) {
            MemorySegment defaultConfigSlot = arena.allocate(ADDRESS);
            int err = (int) Ffi.require(MH_createDefaultAddDeviceConfig, "daqDevice_createDefaultAddDeviceConfig").invokeExact(Ffi.orNull(self), defaultConfigSlot);
            Ffi.checkError(err, "daqDevice_createDefaultAddDeviceConfig");
            return defaultConfigSlot.get(ADDRESS, 0);
        } catch (Throwable t) { throw Ffi.rethrow(t); }
    }

    /** Get a dictionary of available device types as &lt;IString, IDeviceType&gt; pairs  Calls {@code daqDevice_getAvailableDeviceTypes()}. */
    public static MemorySegment getAvailableDeviceTypes(MemorySegment self) {
        try (Arena arena = Arena.ofConfined()) {
            MemorySegment deviceTypesSlot = arena.allocate(ADDRESS);
            int err = (int) Ffi.require(MH_getAvailableDeviceTypes, "daqDevice_getAvailableDeviceTypes").invokeExact(Ffi.orNull(self), deviceTypesSlot);
            Ffi.checkError(err, "daqDevice_getAvailableDeviceTypes");
            return deviceTypesSlot.get(ADDRESS, 0);
        } catch (Throwable t) { throw Ffi.rethrow(t); }
    }

    /** Gets a list of available devices, containing their Device Info.  Calls {@code daqDevice_getAvailableDevices()}. */
    public static MemorySegment getAvailableDevices(MemorySegment self) {
        try (Arena arena = Arena.ofConfined()) {
            MemorySegment availableDevicesSlot = arena.allocate(ADDRESS);
            int err = (int) Ffi.require(MH_getAvailableDevices, "daqDevice_getAvailableDevices").invokeExact(Ffi.orNull(self), availableDevicesSlot);
            Ffi.checkError(err, "daqDevice_getAvailableDevices");
            return availableDevicesSlot.get(ADDRESS, 0);
        } catch (Throwable t) { throw Ffi.rethrow(t); }
    }

    /** Gets all function block types that are supported by the device, containing their description.  Calls {@code daqDevice_getAvailableFunctionBlockTypes()}. */
    public static MemorySegment getAvailableFunctionBlockTypes(MemorySegment self) {
        try (Arena arena = Arena.ofConfined()) {
            MemorySegment functionBlockTypesSlot = arena.allocate(ADDRESS);
            int err = (int) Ffi.require(MH_getAvailableFunctionBlockTypes, "daqDevice_getAvailableFunctionBlockTypes").invokeExact(Ffi.orNull(self), functionBlockTypesSlot);
            Ffi.checkError(err, "daqDevice_getAvailableFunctionBlockTypes");
            return functionBlockTypesSlot.get(ADDRESS, 0);
        } catch (Throwable t) { throw Ffi.rethrow(t); }
    }

    /** Gets a list of available operation modes for the device.  Calls {@code daqDevice_getAvailableOperationModes()}. */
    public static MemorySegment getAvailableOperationModes(MemorySegment self) {
        try (Arena arena = Arena.ofConfined()) {
            MemorySegment availableOpModesSlot = arena.allocate(ADDRESS);
            int err = (int) Ffi.require(MH_getAvailableOperationModes, "daqDevice_getAvailableOperationModes").invokeExact(Ffi.orNull(self), availableOpModesSlot);
            Ffi.checkError(err, "daqDevice_getAvailableOperationModes");
            return availableOpModesSlot.get(ADDRESS, 0);
        } catch (Throwable t) { throw Ffi.rethrow(t); }
    }

    /** Gets a flat list of the device's physical channels.  Calls {@code daqDevice_getChannels()}. */
    public static MemorySegment getChannels(MemorySegment self, MemorySegment searchFilter) {
        try (Arena arena = Arena.ofConfined()) {
            MemorySegment channelsSlot = arena.allocate(ADDRESS);
            int err = (int) Ffi.require(MH_getChannels, "daqDevice_getChannels").invokeExact(Ffi.orNull(self), channelsSlot, Ffi.orNull(searchFilter));
            Ffi.checkError(err, "daqDevice_getChannels");
            return channelsSlot.get(ADDRESS, 0);
        } catch (Throwable t) { throw Ffi.rethrow(t); }
    }

    /** Gets a flat list of the device's physical channels. Also finds all visible channels of visible child devices  Calls {@code daqDevice_getChannelsRecursive()}. */
    public static MemorySegment getChannelsRecursive(MemorySegment self, MemorySegment searchFilter) {
        try (Arena arena = Arena.ofConfined()) {
            MemorySegment channelsSlot = arena.allocate(ADDRESS);
            int err = (int) Ffi.require(MH_getChannelsRecursive, "daqDevice_getChannelsRecursive").invokeExact(Ffi.orNull(self), channelsSlot, Ffi.orNull(searchFilter));
            Ffi.checkError(err, "daqDevice_getChannelsRecursive");
            return channelsSlot.get(ADDRESS, 0);
        } catch (Throwable t) { throw Ffi.rethrow(t); }
    }

    /** Gets the container holding the statuses of device configuration and streaming connections.  Calls {@code daqDevice_getConnectionStatusContainer()}. */
    public static MemorySegment getConnectionStatusContainer(MemorySegment self) {
        try (Arena arena = Arena.ofConfined()) {
            MemorySegment statusContainerSlot = arena.allocate(ADDRESS);
            int err = (int) Ffi.require(MH_getConnectionStatusContainer, "daqDevice_getConnectionStatusContainer").invokeExact(Ffi.orNull(self), statusContainerSlot);
            Ffi.checkError(err, "daqDevice_getConnectionStatusContainer");
            return statusContainerSlot.get(ADDRESS, 0);
        } catch (Throwable t) { throw Ffi.rethrow(t); }
    }

    /** Gets a list of all components/folders in a device that are not titled 'IO', 'Sig', 'Dev', 'Synchronization' or 'FB'  Calls {@code daqDevice_getCustomComponents()}. */
    public static MemorySegment getCustomComponents(MemorySegment self) {
        try (Arena arena = Arena.ofConfined()) {
            MemorySegment customComponentsSlot = arena.allocate(ADDRESS);
            int err = (int) Ffi.require(MH_getCustomComponents, "daqDevice_getCustomComponents").invokeExact(Ffi.orNull(self), customComponentsSlot);
            Ffi.checkError(err, "daqDevice_getCustomComponents");
            return customComponentsSlot.get(ADDRESS, 0);
        } catch (Throwable t) { throw Ffi.rethrow(t); }
    }

    /** Gets a list of child devices that the device is connected to.  Calls {@code daqDevice_getDevices()}. */
    public static MemorySegment getDevices(MemorySegment self, MemorySegment searchFilter) {
        try (Arena arena = Arena.ofConfined()) {
            MemorySegment devicesSlot = arena.allocate(ADDRESS);
            int err = (int) Ffi.require(MH_getDevices, "daqDevice_getDevices").invokeExact(Ffi.orNull(self), devicesSlot, Ffi.orNull(searchFilter));
            Ffi.checkError(err, "daqDevice_getDevices");
            return devicesSlot.get(ADDRESS, 0);
        } catch (Throwable t) { throw Ffi.rethrow(t); }
    }

    /** Gets the device's domain data. It allows for querying the device for its domain (time) values.  Calls {@code daqDevice_getDomain()}. */
    public static MemorySegment getDomain(MemorySegment self) {
        try (Arena arena = Arena.ofConfined()) {
            MemorySegment domainSlot = arena.allocate(ADDRESS);
            int err = (int) Ffi.require(MH_getDomain, "daqDevice_getDomain").invokeExact(Ffi.orNull(self), domainSlot);
            Ffi.checkError(err, "daqDevice_getDomain");
            return domainSlot.get(ADDRESS, 0);
        } catch (Throwable t) { throw Ffi.rethrow(t); }
    }

    /** Gets the list of added function blocks.  Calls {@code daqDevice_getFunctionBlocks()}. */
    public static MemorySegment getFunctionBlocks(MemorySegment self, MemorySegment searchFilter) {
        try (Arena arena = Arena.ofConfined()) {
            MemorySegment functionBlocksSlot = arena.allocate(ADDRESS);
            int err = (int) Ffi.require(MH_getFunctionBlocks, "daqDevice_getFunctionBlocks").invokeExact(Ffi.orNull(self), functionBlocksSlot, Ffi.orNull(searchFilter));
            Ffi.checkError(err, "daqDevice_getFunctionBlocks");
            return functionBlocksSlot.get(ADDRESS, 0);
        } catch (Throwable t) { throw Ffi.rethrow(t); }
    }

    /** Gets the device info. It contains data about the device such as the device's serial number, location, and connection string.  Calls {@code daqDevice_getInfo()}. */
    public static MemorySegment getInfo(MemorySegment self) {
        try (Arena arena = Arena.ofConfined()) {
            MemorySegment infoSlot = arena.allocate(ADDRESS);
            int err = (int) Ffi.require(MH_getInfo, "daqDevice_getInfo").invokeExact(Ffi.orNull(self), infoSlot);
            Ffi.checkError(err, "daqDevice_getInfo");
            return infoSlot.get(ADDRESS, 0);
        } catch (Throwable t) { throw Ffi.rethrow(t); }
    }

    /** Gets a folder containing channels.  Calls {@code daqDevice_getInputsOutputsFolder()}. */
    public static MemorySegment getInputsOutputsFolder(MemorySegment self) {
        try (Arena arena = Arena.ofConfined()) {
            MemorySegment inputsOutputsFolderSlot = arena.allocate(ADDRESS);
            int err = (int) Ffi.require(MH_getInputsOutputsFolder, "daqDevice_getInputsOutputsFolder").invokeExact(Ffi.orNull(self), inputsOutputsFolderSlot);
            Ffi.checkError(err, "daqDevice_getInputsOutputsFolder");
            return inputsOutputsFolderSlot.get(ADDRESS, 0);
        } catch (Throwable t) { throw Ffi.rethrow(t); }
    }

    /** Calls {@code daqDevice_getInterfaceId()} with caller-allocated out slots. */
    public static void getInterfaceId(MemorySegment intfId) {
        try {
            Ffi.require(MH_getInterfaceId, "daqDevice_getInterfaceId").invokeExact(Ffi.orNull(intfId));
        } catch (Throwable t) { throw Ffi.rethrow(t); }
    }

    /** Retrieves a chunk of the log file with the provided ID.  Calls {@code daqDevice_getLog()}. */
    public static MemorySegment getLog(MemorySegment self, MemorySegment id, long size, long offset) {
        try (Arena arena = Arena.ofConfined()) {
            MemorySegment logSlot = arena.allocate(ADDRESS);
            int err = (int) Ffi.require(MH_getLog, "daqDevice_getLog").invokeExact(Ffi.orNull(self), logSlot, Ffi.orNull(id), size, offset);
            Ffi.checkError(err, "daqDevice_getLog");
            return logSlot.get(ADDRESS, 0);
        } catch (Throwable t) { throw Ffi.rethrow(t); }
    }

    /** Gets a list of available log files.  Calls {@code daqDevice_getLogFileInfos()}. */
    public static MemorySegment getLogFileInfos(MemorySegment self) {
        try (Arena arena = Arena.ofConfined()) {
            MemorySegment logFileInfosSlot = arena.allocate(ADDRESS);
            int err = (int) Ffi.require(MH_getLogFileInfos, "daqDevice_getLogFileInfos").invokeExact(Ffi.orNull(self), logFileInfosSlot);
            Ffi.checkError(err, "daqDevice_getLogFileInfos");
            return logFileInfosSlot.get(ADDRESS, 0);
        } catch (Throwable t) { throw Ffi.rethrow(t); }
    }

    /** Get list of added servers.  Calls {@code daqDevice_getServers()}. */
    public static MemorySegment getServers(MemorySegment self) {
        try (Arena arena = Arena.ofConfined()) {
            MemorySegment serversSlot = arena.allocate(ADDRESS);
            int err = (int) Ffi.require(MH_getServers, "daqDevice_getServers").invokeExact(Ffi.orNull(self), serversSlot);
            Ffi.checkError(err, "daqDevice_getServers");
            return serversSlot.get(ADDRESS, 0);
        } catch (Throwable t) { throw Ffi.rethrow(t); }
    }

    /** Gets a list of the device's signals.  Calls {@code daqDevice_getSignals()}. */
    public static MemorySegment getSignals(MemorySegment self, MemorySegment searchFilter) {
        try (Arena arena = Arena.ofConfined()) {
            MemorySegment signalsSlot = arena.allocate(ADDRESS);
            int err = (int) Ffi.require(MH_getSignals, "daqDevice_getSignals").invokeExact(Ffi.orNull(self), signalsSlot, Ffi.orNull(searchFilter));
            Ffi.checkError(err, "daqDevice_getSignals");
            return signalsSlot.get(ADDRESS, 0);
        } catch (Throwable t) { throw Ffi.rethrow(t); }
    }

    /** Gets a list of the signals that belong to the device.  Calls {@code daqDevice_getSignalsRecursive()}. */
    public static MemorySegment getSignalsRecursive(MemorySegment self, MemorySegment searchFilter) {
        try (Arena arena = Arena.ofConfined()) {
            MemorySegment signalsSlot = arena.allocate(ADDRESS);
            int err = (int) Ffi.require(MH_getSignalsRecursive, "daqDevice_getSignalsRecursive").invokeExact(Ffi.orNull(self), signalsSlot, Ffi.orNull(searchFilter));
            Ffi.checkError(err, "daqDevice_getSignalsRecursive");
            return signalsSlot.get(ADDRESS, 0);
        } catch (Throwable t) { throw Ffi.rethrow(t); }
    }

    /** Gets the sync component of the device.  Calls {@code daqDevice_getSyncComponent()}. */
    public static MemorySegment getSyncComponent(MemorySegment self) {
        try (Arena arena = Arena.ofConfined()) {
            MemorySegment syncSlot = arena.allocate(ADDRESS);
            int err = (int) Ffi.require(MH_getSyncComponent, "daqDevice_getSyncComponent").invokeExact(Ffi.orNull(self), syncSlot);
            Ffi.checkError(err, "daqDevice_getSyncComponent");
            return syncSlot.get(ADDRESS, 0);
        } catch (Throwable t) { throw Ffi.rethrow(t); }
    }

    /** Gets the number of ticks passed since the device's absolute origin.  Calls {@code daqDevice_getTicksSinceOrigin()}. */
    public static long getTicksSinceOrigin(MemorySegment self) {
        try (Arena arena = Arena.ofConfined()) {
            MemorySegment ticksSlot = arena.allocate(JAVA_LONG);
            int err = (int) Ffi.require(MH_getTicksSinceOrigin, "daqDevice_getTicksSinceOrigin").invokeExact(Ffi.orNull(self), ticksSlot);
            Ffi.checkError(err, "daqDevice_getTicksSinceOrigin");
            return ticksSlot.get(JAVA_LONG, 0);
        } catch (Throwable t) { throw Ffi.rethrow(t); }
    }

    /** Returns true if device is locked. Once locked, no properties of the device can be changed via the protocol layer.  Calls {@code daqDevice_isLocked()}. */
    public static boolean isLocked(MemorySegment self) {
        try (Arena arena = Arena.ofConfined()) {
            MemorySegment lockedSlot = arena.allocate(JAVA_BYTE);
            int err = (int) Ffi.require(MH_isLocked, "daqDevice_isLocked").invokeExact(Ffi.orNull(self), lockedSlot);
            Ffi.checkError(err, "daqDevice_isLocked");
            return lockedSlot.get(JAVA_BYTE, 0) != 0;
        } catch (Throwable t) { throw Ffi.rethrow(t); }
    }

    /** Loads the configuration of the device from string.  Calls {@code daqDevice_loadConfiguration()}. */
    public static void loadConfiguration(MemorySegment self, MemorySegment configuration, MemorySegment config) {
        try {
            int err = (int) Ffi.require(MH_loadConfiguration, "daqDevice_loadConfiguration").invokeExact(Ffi.orNull(self), Ffi.orNull(configuration), Ffi.orNull(config));
            Ffi.checkError(err, "daqDevice_loadConfiguration");
        } catch (Throwable t) { throw Ffi.rethrow(t); }
    }

    /** Lock a device with a session user. Once locked, no properties of the device can be changed via the protocol layer. Only the same user who locked the device can unlock it. If no user was specified when the device was locked, any user will be able to unlock it.  Calls {@code daqDevice_lock()}. */
    public static void lock(MemorySegment self) {
        try {
            int err = (int) Ffi.require(MH_lock, "daqDevice_lock").invokeExact(Ffi.orNull(self));
            Ffi.checkError(err, "daqDevice_lock");
        } catch (Throwable t) { throw Ffi.rethrow(t); }
    }

    /** Disconnects from the device provided as argument and removes it from the internal list of devices.  Calls {@code daqDevice_removeDevice()}. */
    public static void removeDevice(MemorySegment self, MemorySegment device) {
        try {
            int err = (int) Ffi.require(MH_removeDevice, "daqDevice_removeDevice").invokeExact(Ffi.orNull(self), Ffi.orNull(device));
            Ffi.checkError(err, "daqDevice_removeDevice");
        } catch (Throwable t) { throw Ffi.rethrow(t); }
    }

    /** Removes the function block provided as argument, disconnecting its signals and input ports.  Calls {@code daqDevice_removeFunctionBlock()}. */
    public static void removeFunctionBlock(MemorySegment self, MemorySegment functionBlock) {
        try {
            int err = (int) Ffi.require(MH_removeFunctionBlock, "daqDevice_removeFunctionBlock").invokeExact(Ffi.orNull(self), Ffi.orNull(functionBlock));
            Ffi.checkError(err, "daqDevice_removeFunctionBlock");
        } catch (Throwable t) { throw Ffi.rethrow(t); }
    }

    /** Removes the server provided as argument.  Calls {@code daqDevice_removeServer()}. */
    public static void removeServer(MemorySegment self, MemorySegment server) {
        try {
            int err = (int) Ffi.require(MH_removeServer, "daqDevice_removeServer").invokeExact(Ffi.orNull(self), Ffi.orNull(server));
            Ffi.checkError(err, "daqDevice_removeServer");
        } catch (Throwable t) { throw Ffi.rethrow(t); }
    }

    /** Saves the configuration of the device to string.  Calls {@code daqDevice_saveConfiguration()}. */
    public static MemorySegment saveConfiguration(MemorySegment self) {
        try (Arena arena = Arena.ofConfined()) {
            MemorySegment configurationSlot = arena.allocate(ADDRESS);
            int err = (int) Ffi.require(MH_saveConfiguration, "daqDevice_saveConfiguration").invokeExact(Ffi.orNull(self), configurationSlot);
            Ffi.checkError(err, "daqDevice_saveConfiguration");
            return configurationSlot.get(ADDRESS, 0);
        } catch (Throwable t) { throw Ffi.rethrow(t); }
    }

    /** Sets the operation mode of the device subtree excluding the sub-devices.  Calls {@code daqDevice_setOperationMode()}. */
    public static void setOperationMode(MemorySegment self, OperationModeType modeType) {
        try {
            int err = (int) Ffi.require(MH_setOperationMode, "daqDevice_setOperationMode").invokeExact(Ffi.orNull(self), modeType.value());
            Ffi.checkError(err, "daqDevice_setOperationMode");
        } catch (Throwable t) { throw Ffi.rethrow(t); }
    }

    /** Sets the operation mode of the device subtree including the sub-devices.  Calls {@code daqDevice_setOperationModeRecursive()}. */
    public static void setOperationModeRecursive(MemorySegment self, OperationModeType modeType) {
        try {
            int err = (int) Ffi.require(MH_setOperationModeRecursive, "daqDevice_setOperationModeRecursive").invokeExact(Ffi.orNull(self), modeType.value());
            Ffi.checkError(err, "daqDevice_setOperationModeRecursive");
        } catch (Throwable t) { throw Ffi.rethrow(t); }
    }

    /** Unlock a device with a session user. A device can only be unlocked by the same user who locked it. If no user was specified when the device was locked, any user will be able to unlock it.  Calls {@code daqDevice_unlock()}. */
    public static void unlock(MemorySegment self) {
        try {
            int err = (int) Ffi.require(MH_unlock, "daqDevice_unlock").invokeExact(Ffi.orNull(self));
            Ffi.checkError(err, "daqDevice_unlock");
        } catch (Throwable t) { throw Ffi.rethrow(t); }
    }

}
