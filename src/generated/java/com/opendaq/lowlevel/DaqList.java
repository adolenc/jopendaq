// This file is autogenerated by tools/generate_low_level_bindings.py. Do not edit.

package com.opendaq.lowlevel;

import java.lang.foreign.Arena;
import java.lang.foreign.FunctionDescriptor;
import java.lang.foreign.MemorySegment;
import java.lang.invoke.MethodHandle;

import static java.lang.foreign.ValueLayout.*;

/** Raw FFI bindings for the daqList_* functions of the openDAQ C API. */
public final class DaqList {

    private DaqList() {}

    private static final MethodHandle MH_clear =
        Ffi.downcallOrNull("daqList_clear", FunctionDescriptor.of(JAVA_INT, ADDRESS));
    private static final MethodHandle MH_createEndIterator =
        Ffi.downcallOrNull("daqList_createEndIterator", FunctionDescriptor.of(JAVA_INT, ADDRESS, ADDRESS));
    private static final MethodHandle MH_createList =
        Ffi.downcallOrNull("daqList_createList", FunctionDescriptor.of(JAVA_INT, ADDRESS));
    private static final MethodHandle MH_createListWithElementType =
        Ffi.downcallOrNull("daqList_createListWithElementType", FunctionDescriptor.of(JAVA_INT, ADDRESS, Ffi.INTF_ID));
    private static final MethodHandle MH_createStartIterator =
        Ffi.downcallOrNull("daqList_createStartIterator", FunctionDescriptor.of(JAVA_INT, ADDRESS, ADDRESS));
    private static final MethodHandle MH_deleteAt =
        Ffi.downcallOrNull("daqList_deleteAt", FunctionDescriptor.of(JAVA_INT, ADDRESS, JAVA_LONG));
    private static final MethodHandle MH_getCount =
        Ffi.downcallOrNull("daqList_getCount", FunctionDescriptor.of(JAVA_INT, ADDRESS, ADDRESS));
    private static final MethodHandle MH_getInterfaceId =
        Ffi.downcallOrNull("daqList_getInterfaceId", FunctionDescriptor.ofVoid(ADDRESS));
    private static final MethodHandle MH_getItemAt =
        Ffi.downcallOrNull("daqList_getItemAt", FunctionDescriptor.of(JAVA_INT, ADDRESS, JAVA_LONG, ADDRESS));
    private static final MethodHandle MH_insertAt =
        Ffi.downcallOrNull("daqList_insertAt", FunctionDescriptor.of(JAVA_INT, ADDRESS, JAVA_LONG, ADDRESS));
    private static final MethodHandle MH_moveBack =
        Ffi.downcallOrNull("daqList_moveBack", FunctionDescriptor.of(JAVA_INT, ADDRESS, ADDRESS));
    private static final MethodHandle MH_moveFront =
        Ffi.downcallOrNull("daqList_moveFront", FunctionDescriptor.of(JAVA_INT, ADDRESS, ADDRESS));
    private static final MethodHandle MH_popBack =
        Ffi.downcallOrNull("daqList_popBack", FunctionDescriptor.of(JAVA_INT, ADDRESS, ADDRESS));
    private static final MethodHandle MH_popFront =
        Ffi.downcallOrNull("daqList_popFront", FunctionDescriptor.of(JAVA_INT, ADDRESS, ADDRESS));
    private static final MethodHandle MH_pushBack =
        Ffi.downcallOrNull("daqList_pushBack", FunctionDescriptor.of(JAVA_INT, ADDRESS, ADDRESS));
    private static final MethodHandle MH_pushFront =
        Ffi.downcallOrNull("daqList_pushFront", FunctionDescriptor.of(JAVA_INT, ADDRESS, ADDRESS));
    private static final MethodHandle MH_removeAt =
        Ffi.downcallOrNull("daqList_removeAt", FunctionDescriptor.of(JAVA_INT, ADDRESS, JAVA_LONG, ADDRESS));
    private static final MethodHandle MH_setItemAt =
        Ffi.downcallOrNull("daqList_setItemAt", FunctionDescriptor.of(JAVA_INT, ADDRESS, JAVA_LONG, ADDRESS));

    /** Removes all elements from the list.  Calls {@code daqList_clear()}. */
    public static void clear(MemorySegment self) {
        try {
            int err = (int) Ffi.require(MH_clear, "daqList_clear").invokeExact(Ffi.orNull(self));
            Ffi.checkError(err, "daqList_clear");
        } catch (Throwable t) { throw Ffi.rethrow(t); }
    }

    /** Creates and returns the stop iterator of the list.  Calls {@code daqList_createEndIterator()}. */
    public static MemorySegment createEndIterator(MemorySegment self) {
        try (Arena arena = Arena.ofConfined()) {
            MemorySegment iteratorSlot = arena.allocate(ADDRESS);
            int err = (int) Ffi.require(MH_createEndIterator, "daqList_createEndIterator").invokeExact(Ffi.orNull(self), iteratorSlot);
            Ffi.checkError(err, "daqList_createEndIterator");
            return iteratorSlot.get(ADDRESS, 0);
        } catch (Throwable t) { throw Ffi.rethrow(t); }
    }

    /** Calls {@code daqList_createList()}. */
    public static MemorySegment createList() {
        try (Arena arena = Arena.ofConfined()) {
            MemorySegment objSlot = arena.allocate(ADDRESS);
            int err = (int) Ffi.require(MH_createList, "daqList_createList").invokeExact(objSlot);
            Ffi.checkError(err, "daqList_createList");
            return objSlot.get(ADDRESS, 0);
        } catch (Throwable t) { throw Ffi.rethrow(t); }
    }

    /** Calls {@code daqList_createListWithElementType()}. */
    public static MemorySegment createListWithElementType(MemorySegment id) {
        try (Arena arena = Arena.ofConfined()) {
            MemorySegment objSlot = arena.allocate(ADDRESS);
            int err = (int) Ffi.require(MH_createListWithElementType, "daqList_createListWithElementType").invokeExact(objSlot, Ffi.orNull(id));
            Ffi.checkError(err, "daqList_createListWithElementType");
            return objSlot.get(ADDRESS, 0);
        } catch (Throwable t) { throw Ffi.rethrow(t); }
    }

    /** Creates and returns the start iterator of the list.  Calls {@code daqList_createStartIterator()}. */
    public static MemorySegment createStartIterator(MemorySegment self) {
        try (Arena arena = Arena.ofConfined()) {
            MemorySegment iteratorSlot = arena.allocate(ADDRESS);
            int err = (int) Ffi.require(MH_createStartIterator, "daqList_createStartIterator").invokeExact(Ffi.orNull(self), iteratorSlot);
            Ffi.checkError(err, "daqList_createStartIterator");
            return iteratorSlot.get(ADDRESS, 0);
        } catch (Throwable t) { throw Ffi.rethrow(t); }
    }

    /** Deletes the element at a specific position.  Calls {@code daqList_deleteAt()}. */
    public static void deleteAt(MemorySegment self, long index) {
        try {
            int err = (int) Ffi.require(MH_deleteAt, "daqList_deleteAt").invokeExact(Ffi.orNull(self), index);
            Ffi.checkError(err, "daqList_deleteAt");
        } catch (Throwable t) { throw Ffi.rethrow(t); }
    }

    /** Gets the number of elements contained in the list.  Calls {@code daqList_getCount()}. */
    public static long getCount(MemorySegment self) {
        try (Arena arena = Arena.ofConfined()) {
            MemorySegment sizeSlot = arena.allocate(JAVA_LONG);
            int err = (int) Ffi.require(MH_getCount, "daqList_getCount").invokeExact(Ffi.orNull(self), sizeSlot);
            Ffi.checkError(err, "daqList_getCount");
            return sizeSlot.get(JAVA_LONG, 0);
        } catch (Throwable t) { throw Ffi.rethrow(t); }
    }

    /** Calls {@code daqList_getInterfaceId()} with caller-allocated out slots. */
    public static void getInterfaceId(MemorySegment intfId) {
        try {
            Ffi.require(MH_getInterfaceId, "daqList_getInterfaceId").invokeExact(Ffi.orNull(intfId));
        } catch (Throwable t) { throw Ffi.rethrow(t); }
    }

    /** Gets the element at a specific position.  Calls {@code daqList_getItemAt()}. */
    public static MemorySegment getItemAt(MemorySegment self, long index) {
        try (Arena arena = Arena.ofConfined()) {
            MemorySegment objSlot = arena.allocate(ADDRESS);
            int err = (int) Ffi.require(MH_getItemAt, "daqList_getItemAt").invokeExact(Ffi.orNull(self), index, objSlot);
            Ffi.checkError(err, "daqList_getItemAt");
            return objSlot.get(ADDRESS, 0);
        } catch (Throwable t) { throw Ffi.rethrow(t); }
    }

    /** Inserts the element at a specific position.  Calls {@code daqList_insertAt()}. */
    public static void insertAt(MemorySegment self, long index, MemorySegment obj) {
        try {
            int err = (int) Ffi.require(MH_insertAt, "daqList_insertAt").invokeExact(Ffi.orNull(self), index, Ffi.orNull(obj));
            Ffi.checkError(err, "daqList_insertAt");
        } catch (Throwable t) { throw Ffi.rethrow(t); }
    }

    /** Inserts the element at the end of the list without incrementing the reference count.  Calls {@code daqList_moveBack()}. */
    public static void moveBack(MemorySegment self, MemorySegment obj) {
        try {
            int err = (int) Ffi.require(MH_moveBack, "daqList_moveBack").invokeExact(Ffi.orNull(self), Ffi.orNull(obj));
            Ffi.checkError(err, "daqList_moveBack");
        } catch (Throwable t) { throw Ffi.rethrow(t); }
    }

    /** Inserts the element at the start of the list without incrementing the reference count.  Calls {@code daqList_moveFront()}. */
    public static void moveFront(MemorySegment self, MemorySegment obj) {
        try {
            int err = (int) Ffi.require(MH_moveFront, "daqList_moveFront").invokeExact(Ffi.orNull(self), Ffi.orNull(obj));
            Ffi.checkError(err, "daqList_moveFront");
        } catch (Throwable t) { throw Ffi.rethrow(t); }
    }

    /** Gets the element from the end of the list.  Calls {@code daqList_popBack()}. */
    public static MemorySegment popBack(MemorySegment self) {
        try (Arena arena = Arena.ofConfined()) {
            MemorySegment objSlot = arena.allocate(ADDRESS);
            int err = (int) Ffi.require(MH_popBack, "daqList_popBack").invokeExact(Ffi.orNull(self), objSlot);
            Ffi.checkError(err, "daqList_popBack");
            return objSlot.get(ADDRESS, 0);
        } catch (Throwable t) { throw Ffi.rethrow(t); }
    }

    /** Gets the element from the start of the list.  Calls {@code daqList_popFront()}. */
    public static MemorySegment popFront(MemorySegment self) {
        try (Arena arena = Arena.ofConfined()) {
            MemorySegment objSlot = arena.allocate(ADDRESS);
            int err = (int) Ffi.require(MH_popFront, "daqList_popFront").invokeExact(Ffi.orNull(self), objSlot);
            Ffi.checkError(err, "daqList_popFront");
            return objSlot.get(ADDRESS, 0);
        } catch (Throwable t) { throw Ffi.rethrow(t); }
    }

    /** Inserts the element at the end of the list.  Calls {@code daqList_pushBack()}. */
    public static void pushBack(MemorySegment self, MemorySegment obj) {
        try {
            int err = (int) Ffi.require(MH_pushBack, "daqList_pushBack").invokeExact(Ffi.orNull(self), Ffi.orNull(obj));
            Ffi.checkError(err, "daqList_pushBack");
        } catch (Throwable t) { throw Ffi.rethrow(t); }
    }

    /** Inserts the element at the start of the list.  Calls {@code daqList_pushFront()}. */
    public static void pushFront(MemorySegment self, MemorySegment obj) {
        try {
            int err = (int) Ffi.require(MH_pushFront, "daqList_pushFront").invokeExact(Ffi.orNull(self), Ffi.orNull(obj));
            Ffi.checkError(err, "daqList_pushFront");
        } catch (Throwable t) { throw Ffi.rethrow(t); }
    }

    /** Removes the element at a specific position.  Calls {@code daqList_removeAt()}. */
    public static MemorySegment removeAt(MemorySegment self, long index) {
        try (Arena arena = Arena.ofConfined()) {
            MemorySegment objSlot = arena.allocate(ADDRESS);
            int err = (int) Ffi.require(MH_removeAt, "daqList_removeAt").invokeExact(Ffi.orNull(self), index, objSlot);
            Ffi.checkError(err, "daqList_removeAt");
            return objSlot.get(ADDRESS, 0);
        } catch (Throwable t) { throw Ffi.rethrow(t); }
    }

    /** Sets the element at a specific position.  Calls {@code daqList_setItemAt()}. */
    public static void setItemAt(MemorySegment self, long index, MemorySegment obj) {
        try {
            int err = (int) Ffi.require(MH_setItemAt, "daqList_setItemAt").invokeExact(Ffi.orNull(self), index, Ffi.orNull(obj));
            Ffi.checkError(err, "daqList_setItemAt");
        } catch (Throwable t) { throw Ffi.rethrow(t); }
    }

}
