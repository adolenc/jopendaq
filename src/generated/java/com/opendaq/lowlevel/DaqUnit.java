// This file is autogenerated by tools/generate_low_level_bindings.py. Do not edit.

package com.opendaq.lowlevel;

import java.lang.foreign.Arena;
import java.lang.foreign.FunctionDescriptor;
import java.lang.foreign.MemorySegment;
import java.lang.invoke.MethodHandle;

import static java.lang.foreign.ValueLayout.*;

/** Raw FFI bindings for the daqUnit_* functions of the openDAQ C API. */
public final class DaqUnit {

    private DaqUnit() {}

    private static final MethodHandle MH_createUnit =
        Ffi.downcallOrNull("daqUnit_createUnit", FunctionDescriptor.of(JAVA_INT, ADDRESS, JAVA_LONG, ADDRESS, ADDRESS, ADDRESS));
    private static final MethodHandle MH_getId =
        Ffi.downcallOrNull("daqUnit_getId", FunctionDescriptor.of(JAVA_INT, ADDRESS, ADDRESS));
    private static final MethodHandle MH_getInterfaceId =
        Ffi.downcallOrNull("daqUnit_getInterfaceId", FunctionDescriptor.ofVoid(ADDRESS));
    private static final MethodHandle MH_getName =
        Ffi.downcallOrNull("daqUnit_getName", FunctionDescriptor.of(JAVA_INT, ADDRESS, ADDRESS));
    private static final MethodHandle MH_getQuantity =
        Ffi.downcallOrNull("daqUnit_getQuantity", FunctionDescriptor.of(JAVA_INT, ADDRESS, ADDRESS));
    private static final MethodHandle MH_getSymbol =
        Ffi.downcallOrNull("daqUnit_getSymbol", FunctionDescriptor.of(JAVA_INT, ADDRESS, ADDRESS));

    /** Creates a Unit struct with its `id`, `symbol`, `name`, and `quantity` fields configured.  Calls {@code daqUnit_createUnit()}. */
    public static MemorySegment createUnit(long id, MemorySegment symbol, MemorySegment name, MemorySegment quantity) {
        try (Arena arena = Arena.ofConfined()) {
            MemorySegment objSlot = arena.allocate(ADDRESS);
            int err = (int) Ffi.require(MH_createUnit, "daqUnit_createUnit").invokeExact(objSlot, id, Ffi.orNull(symbol), Ffi.orNull(name), Ffi.orNull(quantity));
            Ffi.checkError(err, "daqUnit_createUnit");
            return objSlot.get(ADDRESS, 0);
        } catch (Throwable t) { throw Ffi.rethrow(t); }
    }

    /** Gets the unit ID as defined in &lt;a href="https://unece.org/trade/cefact/UNLOCODE-Download"&gt;Codes for Units of Measurement used in International Trade&lt;/a&gt;.  Calls {@code daqUnit_getId()}. */
    public static long getId(MemorySegment self) {
        try (Arena arena = Arena.ofConfined()) {
            MemorySegment idSlot = arena.allocate(JAVA_LONG);
            int err = (int) Ffi.require(MH_getId, "daqUnit_getId").invokeExact(Ffi.orNull(self), idSlot);
            Ffi.checkError(err, "daqUnit_getId");
            return idSlot.get(JAVA_LONG, 0);
        } catch (Throwable t) { throw Ffi.rethrow(t); }
    }

    /** Calls {@code daqUnit_getInterfaceId()} with caller-allocated out slots. */
    public static void getInterfaceId(MemorySegment intfId) {
        try {
            Ffi.require(MH_getInterfaceId, "daqUnit_getInterfaceId").invokeExact(Ffi.orNull(intfId));
        } catch (Throwable t) { throw Ffi.rethrow(t); }
    }

    /** Gets the full name of the unit, i.e. "meters per second".  Calls {@code daqUnit_getName()}. */
    public static MemorySegment getName(MemorySegment self) {
        try (Arena arena = Arena.ofConfined()) {
            MemorySegment nameSlot = arena.allocate(ADDRESS);
            int err = (int) Ffi.require(MH_getName, "daqUnit_getName").invokeExact(Ffi.orNull(self), nameSlot);
            Ffi.checkError(err, "daqUnit_getName");
            return nameSlot.get(ADDRESS, 0);
        } catch (Throwable t) { throw Ffi.rethrow(t); }
    }

    /** Gets the quantity represented by the unit, i.e. "Velocity"  Calls {@code daqUnit_getQuantity()}. */
    public static MemorySegment getQuantity(MemorySegment self) {
        try (Arena arena = Arena.ofConfined()) {
            MemorySegment quantitySlot = arena.allocate(ADDRESS);
            int err = (int) Ffi.require(MH_getQuantity, "daqUnit_getQuantity").invokeExact(Ffi.orNull(self), quantitySlot);
            Ffi.checkError(err, "daqUnit_getQuantity");
            return quantitySlot.get(ADDRESS, 0);
        } catch (Throwable t) { throw Ffi.rethrow(t); }
    }

    /** Gets the symbol of the unit, i.e. "m/s".  Calls {@code daqUnit_getSymbol()}. */
    public static MemorySegment getSymbol(MemorySegment self) {
        try (Arena arena = Arena.ofConfined()) {
            MemorySegment symbolSlot = arena.allocate(ADDRESS);
            int err = (int) Ffi.require(MH_getSymbol, "daqUnit_getSymbol").invokeExact(Ffi.orNull(self), symbolSlot);
            Ffi.checkError(err, "daqUnit_getSymbol");
            return symbolSlot.get(ADDRESS, 0);
        } catch (Throwable t) { throw Ffi.rethrow(t); }
    }

}
