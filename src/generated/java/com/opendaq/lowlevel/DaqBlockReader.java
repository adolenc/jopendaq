// This file is autogenerated by tools/generate_low_level_bindings.py. Do not edit.

package com.opendaq.lowlevel;

import java.lang.foreign.Arena;
import java.lang.foreign.FunctionDescriptor;
import java.lang.foreign.MemorySegment;
import java.lang.invoke.MethodHandle;

import static java.lang.foreign.ValueLayout.*;
import com.opendaq.ReadMode;
import com.opendaq.SampleType;

/** Raw FFI bindings for the daqBlockReader_* functions of the openDAQ C API. */
public final class DaqBlockReader {

    private DaqBlockReader() {}

    private static final MethodHandle MH_createBlockReader =
        Ffi.downcallOrNull("daqBlockReader_createBlockReader", FunctionDescriptor.of(JAVA_INT, ADDRESS, ADDRESS, JAVA_LONG, JAVA_INT, JAVA_INT, JAVA_INT));
    private static final MethodHandle MH_createBlockReaderFromExisting =
        Ffi.downcallOrNull("daqBlockReader_createBlockReaderFromExisting", FunctionDescriptor.of(JAVA_INT, ADDRESS, ADDRESS, JAVA_INT, JAVA_INT, JAVA_LONG));
    private static final MethodHandle MH_createBlockReaderFromPort =
        Ffi.downcallOrNull("daqBlockReader_createBlockReaderFromPort", FunctionDescriptor.of(JAVA_INT, ADDRESS, ADDRESS, JAVA_LONG, JAVA_INT, JAVA_INT, JAVA_INT));
    private static final MethodHandle MH_getBlockSize =
        Ffi.downcallOrNull("daqBlockReader_getBlockSize", FunctionDescriptor.of(JAVA_INT, ADDRESS, ADDRESS));
    private static final MethodHandle MH_getInterfaceId =
        Ffi.downcallOrNull("daqBlockReader_getInterfaceId", FunctionDescriptor.ofVoid(ADDRESS));
    private static final MethodHandle MH_getOverlap =
        Ffi.downcallOrNull("daqBlockReader_getOverlap", FunctionDescriptor.of(JAVA_INT, ADDRESS, ADDRESS));
    private static final MethodHandle MH_read =
        Ffi.downcallOrNull("daqBlockReader_read", FunctionDescriptor.of(JAVA_INT, ADDRESS, ADDRESS, ADDRESS, JAVA_LONG, ADDRESS));
    private static final MethodHandle MH_readWithDomain =
        Ffi.downcallOrNull("daqBlockReader_readWithDomain", FunctionDescriptor.of(JAVA_INT, ADDRESS, ADDRESS, ADDRESS, ADDRESS, JAVA_LONG, ADDRESS));

    /** Calls {@code daqBlockReader_createBlockReader()}. */
    public static MemorySegment createBlockReader(MemorySegment signal, long blockSize, SampleType valueReadType, SampleType domainReadType, ReadMode mode) {
        try (Arena arena = Arena.ofConfined()) {
            MemorySegment objSlot = arena.allocate(ADDRESS);
            int err = (int) Ffi.require(MH_createBlockReader, "daqBlockReader_createBlockReader").invokeExact(objSlot, Ffi.orNull(signal), blockSize, valueReadType.value(), domainReadType.value(), mode.value());
            Ffi.checkError(err, "daqBlockReader_createBlockReader");
            return objSlot.get(ADDRESS, 0);
        } catch (Throwable t) { throw Ffi.rethrow(t); }
    }

    /** Calls {@code daqBlockReader_createBlockReaderFromExisting()}. */
    public static MemorySegment createBlockReaderFromExisting(MemorySegment invalidatedReader, SampleType valueReadType, SampleType domainReadType, long blockSize) {
        try (Arena arena = Arena.ofConfined()) {
            MemorySegment objSlot = arena.allocate(ADDRESS);
            int err = (int) Ffi.require(MH_createBlockReaderFromExisting, "daqBlockReader_createBlockReaderFromExisting").invokeExact(objSlot, Ffi.orNull(invalidatedReader), valueReadType.value(), domainReadType.value(), blockSize);
            Ffi.checkError(err, "daqBlockReader_createBlockReaderFromExisting");
            return objSlot.get(ADDRESS, 0);
        } catch (Throwable t) { throw Ffi.rethrow(t); }
    }

    /** Calls {@code daqBlockReader_createBlockReaderFromPort()}. */
    public static MemorySegment createBlockReaderFromPort(MemorySegment port, long blockSize, SampleType valueReadType, SampleType domainReadType, ReadMode mode) {
        try (Arena arena = Arena.ofConfined()) {
            MemorySegment objSlot = arena.allocate(ADDRESS);
            int err = (int) Ffi.require(MH_createBlockReaderFromPort, "daqBlockReader_createBlockReaderFromPort").invokeExact(objSlot, Ffi.orNull(port), blockSize, valueReadType.value(), domainReadType.value(), mode.value());
            Ffi.checkError(err, "daqBlockReader_createBlockReaderFromPort");
            return objSlot.get(ADDRESS, 0);
        } catch (Throwable t) { throw Ffi.rethrow(t); }
    }

    /** The amount of samples the reader considers as one block.  Calls {@code daqBlockReader_getBlockSize()}. */
    public static long getBlockSize(MemorySegment self) {
        try (Arena arena = Arena.ofConfined()) {
            MemorySegment sizeSlot = arena.allocate(JAVA_LONG);
            int err = (int) Ffi.require(MH_getBlockSize, "daqBlockReader_getBlockSize").invokeExact(Ffi.orNull(self), sizeSlot);
            Ffi.checkError(err, "daqBlockReader_getBlockSize");
            return sizeSlot.get(JAVA_LONG, 0);
        } catch (Throwable t) { throw Ffi.rethrow(t); }
    }

    /** Calls {@code daqBlockReader_getInterfaceId()} with caller-allocated out slots. */
    public static void getInterfaceId(MemorySegment intfId) {
        try {
            Ffi.require(MH_getInterfaceId, "daqBlockReader_getInterfaceId").invokeExact(Ffi.orNull(intfId));
        } catch (Throwable t) { throw Ffi.rethrow(t); }
    }

    /** The amount of block overlapping.  Calls {@code daqBlockReader_getOverlap()}. */
    public static long getOverlap(MemorySegment self) {
        try (Arena arena = Arena.ofConfined()) {
            MemorySegment overlapSlot = arena.allocate(JAVA_LONG);
            int err = (int) Ffi.require(MH_getOverlap, "daqBlockReader_getOverlap").invokeExact(Ffi.orNull(self), overlapSlot);
            Ffi.checkError(err, "daqBlockReader_getOverlap");
            return overlapSlot.get(JAVA_LONG, 0);
        } catch (Throwable t) { throw Ffi.rethrow(t); }
    }

    /** Result of {@link #read}. */
    public record ReadResult(long count, MemorySegment status) {}

    /** Copies at maximum the next `count` blocks of unread samples to the values buffer. The amount actually read is returned through the `count` parameter.  Calls {@code daqBlockReader_read()}. */
    public static ReadResult read(MemorySegment self, MemorySegment blocks, long count, long timeoutMs) {
        try (Arena arena = Arena.ofConfined()) {
            MemorySegment countSlot = arena.allocate(JAVA_LONG);
            countSlot.set(JAVA_LONG, 0, count);
            MemorySegment statusSlot = arena.allocate(ADDRESS);
            int err = (int) Ffi.require(MH_read, "daqBlockReader_read").invokeExact(Ffi.orNull(self), Ffi.orNull(blocks), countSlot, timeoutMs, statusSlot);
            Ffi.checkError(err, "daqBlockReader_read");
            return new ReadResult(countSlot.get(JAVA_LONG, 0), statusSlot.get(ADDRESS, 0));
        } catch (Throwable t) { throw Ffi.rethrow(t); }
    }

    /** Result of {@link #readWithDomain}. */
    public record ReadWithDomainResult(long count, MemorySegment status) {}

    /** Copies at maximum the next `count` blocks of unread samples and clock-stamps to the `dataBlocks` and `domainBlocks` buffers. The amount actually read is returned through the `count` parameter.  Calls {@code daqBlockReader_readWithDomain()}. */
    public static ReadWithDomainResult readWithDomain(MemorySegment self, MemorySegment dataBlocks, MemorySegment domainBlocks, long count, long timeoutMs) {
        try (Arena arena = Arena.ofConfined()) {
            MemorySegment countSlot = arena.allocate(JAVA_LONG);
            countSlot.set(JAVA_LONG, 0, count);
            MemorySegment statusSlot = arena.allocate(ADDRESS);
            int err = (int) Ffi.require(MH_readWithDomain, "daqBlockReader_readWithDomain").invokeExact(Ffi.orNull(self), Ffi.orNull(dataBlocks), Ffi.orNull(domainBlocks), countSlot, timeoutMs, statusSlot);
            Ffi.checkError(err, "daqBlockReader_readWithDomain");
            return new ReadWithDomainResult(countSlot.get(JAVA_LONG, 0), statusSlot.get(ADDRESS, 0));
        } catch (Throwable t) { throw Ffi.rethrow(t); }
    }

}
