// This file is autogenerated by tools/generate_low_level_bindings.py. Do not edit.

package com.opendaq.lowlevel;

import java.lang.foreign.Arena;
import java.lang.foreign.FunctionDescriptor;
import java.lang.foreign.MemorySegment;
import java.lang.invoke.MethodHandle;

import static java.lang.foreign.ValueLayout.*;

/** Raw FFI bindings for the daqDict_* functions of the openDAQ C API. */
public final class DaqDict {

    private DaqDict() {}

    private static final MethodHandle MH_clear =
        Ffi.downcallOrNull("daqDict_clear", FunctionDescriptor.of(JAVA_INT, ADDRESS));
    private static final MethodHandle MH_createDict =
        Ffi.downcallOrNull("daqDict_createDict", FunctionDescriptor.of(JAVA_INT, ADDRESS));
    private static final MethodHandle MH_createDictWithExpectedTypes =
        Ffi.downcallOrNull("daqDict_createDictWithExpectedTypes", FunctionDescriptor.of(JAVA_INT, ADDRESS, Ffi.INTF_ID, Ffi.INTF_ID));
    private static final MethodHandle MH_deleteItem =
        Ffi.downcallOrNull("daqDict_deleteItem", FunctionDescriptor.of(JAVA_INT, ADDRESS, ADDRESS));
    private static final MethodHandle MH_get =
        Ffi.downcallOrNull("daqDict_get", FunctionDescriptor.of(JAVA_INT, ADDRESS, ADDRESS, ADDRESS));
    private static final MethodHandle MH_getCount =
        Ffi.downcallOrNull("daqDict_getCount", FunctionDescriptor.of(JAVA_INT, ADDRESS, ADDRESS));
    private static final MethodHandle MH_getInterfaceId =
        Ffi.downcallOrNull("daqDict_getInterfaceId", FunctionDescriptor.ofVoid(ADDRESS));
    private static final MethodHandle MH_getKeyList =
        Ffi.downcallOrNull("daqDict_getKeyList", FunctionDescriptor.of(JAVA_INT, ADDRESS, ADDRESS));
    private static final MethodHandle MH_getKeys =
        Ffi.downcallOrNull("daqDict_getKeys", FunctionDescriptor.of(JAVA_INT, ADDRESS, ADDRESS));
    private static final MethodHandle MH_getValueList =
        Ffi.downcallOrNull("daqDict_getValueList", FunctionDescriptor.of(JAVA_INT, ADDRESS, ADDRESS));
    private static final MethodHandle MH_getValues =
        Ffi.downcallOrNull("daqDict_getValues", FunctionDescriptor.of(JAVA_INT, ADDRESS, ADDRESS));
    private static final MethodHandle MH_hasKey =
        Ffi.downcallOrNull("daqDict_hasKey", FunctionDescriptor.of(JAVA_INT, ADDRESS, ADDRESS, ADDRESS));
    private static final MethodHandle MH_remove =
        Ffi.downcallOrNull("daqDict_remove", FunctionDescriptor.of(JAVA_INT, ADDRESS, ADDRESS, ADDRESS));
    private static final MethodHandle MH_set =
        Ffi.downcallOrNull("daqDict_set", FunctionDescriptor.of(JAVA_INT, ADDRESS, ADDRESS, ADDRESS));

    /** Removes all elements from the list.  Calls {@code daqDict_clear()}. */
    public static void clear(MemorySegment self) {
        try {
            int err = (int) Ffi.require(MH_clear, "daqDict_clear").invokeExact(Ffi.orNull(self));
            Ffi.checkError(err, "daqDict_clear");
        } catch (Throwable t) { throw Ffi.rethrow(t); }
    }

    /** Calls {@code daqDict_createDict()}. */
    public static MemorySegment createDict() {
        try (Arena arena = Arena.ofConfined()) {
            MemorySegment objSlot = arena.allocate(ADDRESS);
            int err = (int) Ffi.require(MH_createDict, "daqDict_createDict").invokeExact(objSlot);
            Ffi.checkError(err, "daqDict_createDict");
            return objSlot.get(ADDRESS, 0);
        } catch (Throwable t) { throw Ffi.rethrow(t); }
    }

    /** Calls {@code daqDict_createDictWithExpectedTypes()}. */
    public static MemorySegment createDictWithExpectedTypes(MemorySegment keyType, MemorySegment valueType) {
        try (Arena arena = Arena.ofConfined()) {
            MemorySegment objSlot = arena.allocate(ADDRESS);
            int err = (int) Ffi.require(MH_createDictWithExpectedTypes, "daqDict_createDictWithExpectedTypes").invokeExact(objSlot, Ffi.orNull(keyType), Ffi.orNull(valueType));
            Ffi.checkError(err, "daqDict_createDictWithExpectedTypes");
            return objSlot.get(ADDRESS, 0);
        } catch (Throwable t) { throw Ffi.rethrow(t); }
    }

    /** Deletes the element with the specified key.  Calls {@code daqDict_deleteItem()}. */
    public static void deleteItem(MemorySegment self, MemorySegment key) {
        try {
            int err = (int) Ffi.require(MH_deleteItem, "daqDict_deleteItem").invokeExact(Ffi.orNull(self), Ffi.orNull(key));
            Ffi.checkError(err, "daqDict_deleteItem");
        } catch (Throwable t) { throw Ffi.rethrow(t); }
    }

    /** Gets the element with the specified key.  Calls {@code daqDict_get()}. */
    public static MemorySegment get(MemorySegment self, MemorySegment key) {
        try (Arena arena = Arena.ofConfined()) {
            MemorySegment valueSlot = arena.allocate(ADDRESS);
            int err = (int) Ffi.require(MH_get, "daqDict_get").invokeExact(Ffi.orNull(self), Ffi.orNull(key), valueSlot);
            Ffi.checkError(err, "daqDict_get");
            return valueSlot.get(ADDRESS, 0);
        } catch (Throwable t) { throw Ffi.rethrow(t); }
    }

    /** Gets the number of elements contained in the dictionary.  Calls {@code daqDict_getCount()}. */
    public static long getCount(MemorySegment self) {
        try (Arena arena = Arena.ofConfined()) {
            MemorySegment sizeSlot = arena.allocate(JAVA_LONG);
            int err = (int) Ffi.require(MH_getCount, "daqDict_getCount").invokeExact(Ffi.orNull(self), sizeSlot);
            Ffi.checkError(err, "daqDict_getCount");
            return sizeSlot.get(JAVA_LONG, 0);
        } catch (Throwable t) { throw Ffi.rethrow(t); }
    }

    /** Calls {@code daqDict_getInterfaceId()} with caller-allocated out slots. */
    public static void getInterfaceId(MemorySegment intfId) {
        try {
            Ffi.require(MH_getInterfaceId, "daqDict_getInterfaceId").invokeExact(Ffi.orNull(intfId));
        } catch (Throwable t) { throw Ffi.rethrow(t); }
    }

    /** Gets the list of all keys in the dictionary.  Calls {@code daqDict_getKeyList()}. */
    public static MemorySegment getKeyList(MemorySegment self) {
        try (Arena arena = Arena.ofConfined()) {
            MemorySegment keysSlot = arena.allocate(ADDRESS);
            int err = (int) Ffi.require(MH_getKeyList, "daqDict_getKeyList").invokeExact(Ffi.orNull(self), keysSlot);
            Ffi.checkError(err, "daqDict_getKeyList");
            return keysSlot.get(ADDRESS, 0);
        } catch (Throwable t) { throw Ffi.rethrow(t); }
    }

    /** Gets the iterable interface of the keys.  Calls {@code daqDict_getKeys()}. */
    public static MemorySegment getKeys(MemorySegment self) {
        try (Arena arena = Arena.ofConfined()) {
            MemorySegment iterableSlot = arena.allocate(ADDRESS);
            int err = (int) Ffi.require(MH_getKeys, "daqDict_getKeys").invokeExact(Ffi.orNull(self), iterableSlot);
            Ffi.checkError(err, "daqDict_getKeys");
            return iterableSlot.get(ADDRESS, 0);
        } catch (Throwable t) { throw Ffi.rethrow(t); }
    }

    /** Gets the list of all elements in the dictionary.  Calls {@code daqDict_getValueList()}. */
    public static MemorySegment getValueList(MemorySegment self) {
        try (Arena arena = Arena.ofConfined()) {
            MemorySegment valuesSlot = arena.allocate(ADDRESS);
            int err = (int) Ffi.require(MH_getValueList, "daqDict_getValueList").invokeExact(Ffi.orNull(self), valuesSlot);
            Ffi.checkError(err, "daqDict_getValueList");
            return valuesSlot.get(ADDRESS, 0);
        } catch (Throwable t) { throw Ffi.rethrow(t); }
    }

    /** Gets the iterable interface of the elements.  Calls {@code daqDict_getValues()}. */
    public static MemorySegment getValues(MemorySegment self) {
        try (Arena arena = Arena.ofConfined()) {
            MemorySegment iterableSlot = arena.allocate(ADDRESS);
            int err = (int) Ffi.require(MH_getValues, "daqDict_getValues").invokeExact(Ffi.orNull(self), iterableSlot);
            Ffi.checkError(err, "daqDict_getValues");
            return iterableSlot.get(ADDRESS, 0);
        } catch (Throwable t) { throw Ffi.rethrow(t); }
    }

    /** Checks if the element with the specified key exists in the dictionary.  Calls {@code daqDict_hasKey()}. */
    public static boolean hasKey(MemorySegment self, MemorySegment key) {
        try (Arena arena = Arena.ofConfined()) {
            MemorySegment hasKeySlot = arena.allocate(JAVA_BYTE);
            int err = (int) Ffi.require(MH_hasKey, "daqDict_hasKey").invokeExact(Ffi.orNull(self), Ffi.orNull(key), hasKeySlot);
            Ffi.checkError(err, "daqDict_hasKey");
            return hasKeySlot.get(JAVA_BYTE, 0) != 0;
        } catch (Throwable t) { throw Ffi.rethrow(t); }
    }

    /** Removes the element with the specified key.  Calls {@code daqDict_remove()}. */
    public static MemorySegment remove(MemorySegment self, MemorySegment key) {
        try (Arena arena = Arena.ofConfined()) {
            MemorySegment valueSlot = arena.allocate(ADDRESS);
            int err = (int) Ffi.require(MH_remove, "daqDict_remove").invokeExact(Ffi.orNull(self), Ffi.orNull(key), valueSlot);
            Ffi.checkError(err, "daqDict_remove");
            return valueSlot.get(ADDRESS, 0);
        } catch (Throwable t) { throw Ffi.rethrow(t); }
    }

    /** Sets the element with the specified key.  Calls {@code daqDict_set()}. */
    public static void set(MemorySegment self, MemorySegment key, MemorySegment value) {
        try {
            int err = (int) Ffi.require(MH_set, "daqDict_set").invokeExact(Ffi.orNull(self), Ffi.orNull(key), Ffi.orNull(value));
            Ffi.checkError(err, "daqDict_set");
        } catch (Throwable t) { throw Ffi.rethrow(t); }
    }

}
