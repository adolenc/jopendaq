// This file is autogenerated by tools/generate_high_level_bindings.py. Do not edit.

package com.opendaq;

import java.lang.foreign.Arena;
import java.lang.foreign.MemorySegment;
import java.lang.foreign.ValueLayout;
import java.lang.ref.Reference;
import java.util.List;
import java.util.Map;

import com.opendaq.lowlevel.*;

/**
 * Packet that contains data sent by a signal. The data can be either explicit, or implicit.
 * Explicit data is contained within a signal's buffer, accessible through `getRawData`, while implicit
 * packets do not carry any data. Their values are calculated given a rule, packet offset, and the index of
 * a sample within the data buffer.
 * To obtain implicitly calculated, or scaled values, `getData` should be used. The data descriptor and sample
 * count provide information about the type and amount of data available at the obtained address.
 *
 * &lt;p&gt;Wraps the openDAQ {&#64;code daqDataPacket} interface.
 */
public class DataPacket extends Packet {

    /** Adopt an owned reference (see {@link DaqObject#DaqObject}). */
    public DataPacket(MemorySegment pointer) {
        super(pointer);
    }

    /**
     * Creates a Data packet with a given descriptor, sample count, memory size of each sample, and an optional packet offset.
     * &#64;param descriptor The descriptor of the signal sending the data.
     * &#64;param sampleCount The number of samples in the packet.
     * &#64;param offset Optional packet offset parameter, used to calculate the data of the packet if the Data rule of the Signal descriptor is not explicit.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqDataPacket_createDataPacket()}.
     */
    public DataPacket(DataDescriptor descriptor, long sampleCount, Object offset) {
        super(create$createDataPacket(descriptor, sampleCount, offset));
    }

    private static MemorySegment create$createDataPacket(DataDescriptor descriptor, long sampleCount, Object offset) {
        try {
            MemorySegment offset$n = Box.boxNumberOwned(offset);
            try {
                return DaqDataPacket.createDataPacket((descriptor == null ? MemorySegment.NULL : descriptor.requireLivePointer()), sampleCount, offset$n);
            } finally { Ffi.releaseRef(offset$n); }
        } finally {
            Reference.reachabilityFence(descriptor);
        }
    }

    /**
     * Creates a Data packet with a given constat rule descriptor, initial constant value, and other constant values.
     * &#64;param domainPacket The Data packet carrying domain data.
     * &#64;param descriptor The descriptor of the signal sending the data.
     * &#64;param sampleCount The number of samples in the packet.
     * &#64;param initialValue The initial constant value.
     * &#64;param otherValues The other constant values.
     * &#64;param otherValueCount The number of other constant values.
     * The values in the packet are calculated by constant rule. The initial value is taken as a constant.
     * Other values are used to change the constant value within the packet. Any number of other constant values
     * can be used. Other values are passed as an array of struct with uint32_t sample position field and value field.
     * Value field (as well as initial value) are of type defined as sample type in the descriptor.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqDataPacket_createConstantDataPacketWithDomain()}.
     */
    public static DataPacket createConstantDataPacketWithDomain(DataPacket domainPacket, DataDescriptor descriptor, long sampleCount, MemorySegment initialValue, MemorySegment otherValues, long otherValueCount) {
        return DaqObject.wrap(create$createConstantDataPacketWithDomain(domainPacket, descriptor, sampleCount, initialValue, otherValues, otherValueCount), DataPacket.class);
    }

    private static MemorySegment create$createConstantDataPacketWithDomain(DataPacket domainPacket, DataDescriptor descriptor, long sampleCount, MemorySegment initialValue, MemorySegment otherValues, long otherValueCount) {
        try {
            return DaqDataPacket.createConstantDataPacketWithDomain((domainPacket == null ? MemorySegment.NULL : domainPacket.requireLivePointer()), (descriptor == null ? MemorySegment.NULL : descriptor.requireLivePointer()), sampleCount, Ffi.orNull(initialValue), Ffi.orNull(otherValues), otherValueCount);
        } finally {
            Reference.reachabilityFence(domainPacket);
            Reference.reachabilityFence(descriptor);
        }
    }

    /**
     * Creates a Data packet with a given descriptor, sample count, memory size of each sample, and an optional implicit value.
     * &#64;param domainPacket The Data packet carrying domain data.
     * &#64;param descriptor The descriptor of the signal sending the data.
     * &#64;param sampleCount The number of samples in the packet.
     * &#64;param offset Optional packet offset parameter, used to calculate the data of the packet if the Data rule of the Signal descriptor is not explicit.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqDataPacket_createDataPacketWithDomain()}.
     */
    public static DataPacket createDataPacketWithDomain(DataPacket domainPacket, DataDescriptor descriptor, long sampleCount, Object offset) {
        return DaqObject.wrap(create$createDataPacketWithDomain(domainPacket, descriptor, sampleCount, offset), DataPacket.class);
    }

    private static MemorySegment create$createDataPacketWithDomain(DataPacket domainPacket, DataDescriptor descriptor, long sampleCount, Object offset) {
        try {
            MemorySegment offset$n = Box.boxNumberOwned(offset);
            try {
                return DaqDataPacket.createDataPacketWithDomain((domainPacket == null ? MemorySegment.NULL : domainPacket.requireLivePointer()), (descriptor == null ? MemorySegment.NULL : descriptor.requireLivePointer()), sampleCount, offset$n);
            } finally { Ffi.releaseRef(offset$n); }
        } finally {
            Reference.reachabilityFence(domainPacket);
            Reference.reachabilityFence(descriptor);
        }
    }

    /**
     * Creates a Data packet with a given descriptor, sample count, an optional packet offset, external memory location for data, custom deleter function and buffer size of external memory.
     * &#64;param descriptor The descriptor of the signal sending the data.
     * &#64;param sampleCount The number of samples in the packet.
     * &#64;param offset Optional packet offset parameter, used to calculate the data of the packet if the Data rule of the Signal descriptor is not explicit.
     * &#64;param externalMemory The pointer to the location of buffer data.
     * &#64;param deleter Custom deleter callback that is called when the packet is destroyed
     * &#64;param bufferSize The size of the external memory.
     * Use this factory when pointer to create a packet which uses an existing memory. The memory should not be
     * freed or written until the packet is destroyed. A deleter callback should be passed as a parameter that
     * is called when the packet is destroyed. The callback should free the memory provided. If bufferSize parameter
     * is equal to SizeT max, the buffer size will be calculated from the descriptor.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqDataPacket_createDataPacketWithExternalMemory()}.
     */
    public static DataPacket createDataPacketWithExternalMemory(DataPacket domainPacket, DataDescriptor descriptor, long sampleCount, Object offset, MemorySegment externalMemory, Deleter deleter, long bufferSize) {
        return DaqObject.wrap(create$createDataPacketWithExternalMemory(domainPacket, descriptor, sampleCount, offset, externalMemory, deleter, bufferSize), DataPacket.class);
    }

    private static MemorySegment create$createDataPacketWithExternalMemory(DataPacket domainPacket, DataDescriptor descriptor, long sampleCount, Object offset, MemorySegment externalMemory, Deleter deleter, long bufferSize) {
        try {
            MemorySegment offset$n = Box.boxNumberOwned(offset);
            try {
                return DaqDataPacket.createDataPacketWithExternalMemory((domainPacket == null ? MemorySegment.NULL : domainPacket.requireLivePointer()), (descriptor == null ? MemorySegment.NULL : descriptor.requireLivePointer()), sampleCount, offset$n, Ffi.orNull(externalMemory), (deleter == null ? MemorySegment.NULL : deleter.requireLivePointer()), bufferSize);
            } finally { Ffi.releaseRef(offset$n); }
        } finally {
            Reference.reachabilityFence(domainPacket);
            Reference.reachabilityFence(descriptor);
            Reference.reachabilityFence(deleter);
        }
    }

    /**
     * Gets the signal descriptor of the signal that sent the packet at the time of sending.
     * &#64;param[out] descriptor The signal descriptor.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqDataPacket_getDataDescriptor()}.
     */
    public DataDescriptor getDataDescriptor() {
        try {
            return DaqObject.wrap(DaqDataPacket.getDataDescriptor(requireLivePointer()), DataDescriptor.class);
        } finally {
            Reference.reachabilityFence(this);
        }
    }

    /**
     * Gets size of data buffer in bytes.
     * &#64;param[out] dataSize the size of data buffer in bytes.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqDataPacket_getDataSize()}.
     */
    public long getDataSize() {
        try {
            return DaqDataPacket.getDataSize(requireLivePointer());
        } finally {
            Reference.reachabilityFence(this);
        }
    }

    /**
     * Gets the associated domain Data packet.
     * &#64;param[out] packet The domain data packet.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqDataPacket_getDomainPacket()}.
     */
    public DataPacket getDomainPacket() {
        try {
            return DaqObject.wrap(DaqDataPacket.getDomainPacket(requireLivePointer()), DataPacket.class);
        } finally {
            Reference.reachabilityFence(this);
        }
    }

    /**
     * Gets the data packet last value.
     * &#64;param[out] value The IBaseObject value can be a nullptr if there is no value, or if the data type is not supported by the function.
     * &#64;param typeManager Optional ITypeManager value can be provided to enable getLastValue for IStruct.
     * If a value is assigned, it can be cast based on the signal description to IFloat if the type is Float32 or Float64,
     * to IInteger if the type is Int8 through Int64 or UInt8 through UInt64, to IComplexNumber if the type is ComplexFloat32 or ComplexFloat64,
     * to IRange if the type is RangeInt64, to IString if the type is String, to IStruct if the type is Struct, and to IList of the forementioned types if there is exactly
     * one dimension.
     * For String type signals in binary data packets, the string data must be encoded as UTF-8 strings. The string length is
     * determined by the sample size, and the string does not need to be null-terminated. The method extracts the string value
     * from the packet data and returns it as an IString object.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqDataPacket_getLastValue()}.
     */
    public BaseObject getLastValue(TypeManager typeManager) {
        try {
            return DaqObject.wrap(DaqDataPacket.getLastValue(requireLivePointer(), (typeManager == null ? MemorySegment.NULL : typeManager.requireLivePointer())), BaseObject.class);
        } finally {
            Reference.reachabilityFence(this);
            Reference.reachabilityFence(typeManager);
        }
    }

    /** getLastValue with trailing arguments at their openDAQ defaults. */
    public BaseObject getLastValue() {
        return getLastValue((TypeManager) null);
    }

    /**
     * Gets current packet offset. This offset is later applied to the data rule used by a signal to calculate actual data value. This value is usually a time or other domain value. Packet offset is particularly useful when one wants to transfer a gap in otherwise equidistant samples. If we have a linear data rule, defined by equation f(x) = k*x + n, then the data value will be calculated by the equation g(x) = offset + f(x).
     * &#64;param[out] offset The packet offset
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqDataPacket_getOffset()}.
     */
    public NumberObject getOffset() {
        try {
            return DaqObject.wrap(DaqDataPacket.getOffset(requireLivePointer()), NumberObject.class);
        } finally {
            Reference.reachabilityFence(this);
        }
    }

    /**
     * Gets the unique packet id.
     * &#64;param[out] packetId The packet unique id.
     * The packet id is automatically created on packet construction.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqDataPacket_getPacketId()}.
     */
    public long getPacketId() {
        try {
            return DaqDataPacket.getPacketId(requireLivePointer());
        } finally {
            Reference.reachabilityFence(this);
        }
    }

    /**
     * Gets size of raw data buffer in bytes.
     * &#64;param[out] dataSize the size of raw data buffer in bytes.
     * Raw data size is 0 if signal's data rule is implicit.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqDataPacket_getRawDataSize()}.
     */
    public long getRawDataSize() {
        try {
            return DaqDataPacket.getRawDataSize(requireLivePointer());
        } finally {
            Reference.reachabilityFence(this);
        }
    }

    /**
     * Gets the last raw value of the data packet.
     * &#64;param[out] value A pointer to a memory location where the raw value will be stored.
     * This method writes the raw value of the last sample to the provided memory location.
     * It does not allocate memory and assumes that sufficient memory has already been allocated.
     * The value is not cast to any specific type; it remains a raw data pointer.
     * The method does not verify whether the allocated memory size matches the sample size.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqDataPacket_getRawLastValue()}.
     */
    public void getRawLastValue(MemorySegment value) {
        try {
            DaqDataPacket.getRawLastValue(requireLivePointer(), Ffi.orNull(value));
        } finally {
            Reference.reachabilityFence(this);
        }
    }

    /**
     * Gets the raw value of the data packet at the specified index.
     * &#64;param[out] value A pointer to a memory location where the raw value will be stored.
     * &#64;param index The index of the sample to retrieve.
     * This method writes the raw value of the sample at the specified index to the provided memory location.
     * It does not allocate memory and assumes that sufficient memory has already been allocated.
     * The value is not cast to any specific type; it remains a raw data pointer.
     * The method does not verify whether the allocated memory size matches the sample size.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqDataPacket_getRawValueByIndex()}.
     */
    public void getRawValueByIndex(MemorySegment value, long index) {
        try {
            DaqDataPacket.getRawValueByIndex(requireLivePointer(), Ffi.orNull(value), index);
        } finally {
            Reference.reachabilityFence(this);
        }
    }

    /**
     * Gets the number of samples in the packet.
     * &#64;param[out] sampleCount the number of samples.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqDataPacket_getSampleCount()}.
     */
    public long getSampleCount() {
        try {
            return DaqDataPacket.getSampleCount(requireLivePointer());
        } finally {
            Reference.reachabilityFence(this);
        }
    }

    /**
     * Gets the data packet value at the specified index.
     * &#64;param[out] value The IBaseObject value can be a nullptr if there is no value, or if the data type is not supported by the function.
     * &#64;param index Index of the sample to obtain.
     * &#64;param typeManager Optional ITypeManager value can be provided to enable getLastValue for IStruct.
     * If a value is assigned, it can be cast based on the signal description to IFloat if the type is Float32 or Float64,
     * to IInteger if the type is Int8 through Int64 or UInt8 through UInt64, to IComplexNumber if the type is ComplexFloat32 or
     * ComplexFloat64, to IRange if the type is RangeInt64, to IString if the type is String, to IStruct if the type is Struct, and to IList of the forementioned types if
     * there is exactly one dimension.
     * For String type signals in binary data packets, the string data must be encoded as UTF-8 strings. The string length is
     * determined by the sample size, and the string does not need to be null-terminated. The method extracts the string value
     * from the packet data and returns it as an IString object.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqDataPacket_getValueByIndex()}.
     */
    public BaseObject getValueByIndex(long index, TypeManager typeManager) {
        try {
            return DaqObject.wrap(DaqDataPacket.getValueByIndex(requireLivePointer(), index, (typeManager == null ? MemorySegment.NULL : typeManager.requireLivePointer())), BaseObject.class);
        } finally {
            Reference.reachabilityFence(this);
            Reference.reachabilityFence(typeManager);
        }
    }

    /** getValueByIndex with trailing arguments at their openDAQ defaults. */
    public BaseObject getValueByIndex(long index) {
        return getValueByIndex(index, (TypeManager) null);
    }

    // ------------------------------------------------------------------
    // Hand-written part (tools/manual/DataPacket.java.inc)
    // ------------------------------------------------------------------

    // daqDataPacket_getData / getRawData return the sample buffer through a
    // void** whose element type is only known at runtime from the packet's
    // data descriptor; these accessors surface it as typed Java arrays.

    /**
     * The packet's calculated/scaled sample data as a typed Java array sized
     * to the sample count: a primitive array for scalar numeric sample
     * types, a 2-D array for dimensioned signals, {@link ComplexValue}[] for
     * the complex-float types.  Throws for sample types that are not a flat
     * numeric buffer (binary, string, struct, range).
     */
    public Object getData() {
        try {
            DataDescriptor descriptor = getDataDescriptor();
            SampleType type = descriptor.getSampleType();
            int count = (int) getSampleCount();
            int width = SampleArrays.descriptorWidth(descriptor);
            MemorySegment buffer = dataAddress(false)
                .reinterpret(SampleArrays.byteSize(type, count, width));
            return SampleArrays.toArray(buffer, type, count, width);
        } finally {
            Reference.reachabilityFence(this);
        }
    }

    /**
     * Write samples into this packet's buffer in place, coercing each element
     * to the packet's descriptor sample type (reals to a float type, rounded
     * to an integer type).  The packet owns the buffer and must already be
     * sized to hold them (its sample count).
     */
    public void setData(double[] samples) {
        writeData(samples);
    }

    public void setData(long[] samples) {
        writeData(samples);
    }

    public void setData(ComplexValue[] samples) {
        writeData(samples);
    }

    private void writeData(Object samples) {
        try {
            DataDescriptor descriptor = getDataDescriptor();
            SampleType type = descriptor.getSampleType();
            int length = java.lang.reflect.Array.getLength(samples);
            MemorySegment buffer = dataAddress(false)
                .reinterpret(SampleArrays.byteSize(type, length, 1));
            SampleArrays.fromArray(buffer, type, samples);
        } finally {
            Reference.reachabilityFence(this);
        }
    }

    /**
     * The packet's raw (pre-scaling) memory as a byte array.  Reinterpret it
     * via the data descriptor when a typed view is needed; for decoded values
     * use {@link #getData()}.
     */
    public byte[] getRawData() {
        try {
            int size = (int) getRawDataSize();
            MemorySegment buffer = dataAddress(true).reinterpret(Math.max(size, 1));
            return buffer.asSlice(0, size).toArray(ValueLayout.JAVA_BYTE);
        } finally {
            Reference.reachabilityFence(this);
        }
    }

    private MemorySegment dataAddress(boolean raw) {
        try (Arena arena = Arena.ofConfined()) {
            MemorySegment slot = arena.allocate(ValueLayout.ADDRESS);
            if (raw) {
                DaqDataPacket.getRawData(requireLivePointer(), slot);
            } else {
                DaqDataPacket.getData(requireLivePointer(), slot);
            }
            return slot.get(ValueLayout.ADDRESS, 0);
        }
    }

}
