// This file is autogenerated by tools/generate_high_level_bindings.py. Do not edit.

package com.opendaq;

import java.lang.foreign.Arena;
import java.lang.foreign.MemorySegment;
import java.lang.foreign.ValueLayout;
import java.lang.ref.Reference;
import java.util.List;
import java.util.Map;

import com.opendaq.lowlevel.*;

/**
 * Represents a collection of key/value pairs.
 *
 * &lt;p&gt;Wraps the openDAQ {&#64;code daqDict} interface.
 */
public class Dict extends BaseObject {

    /** Adopt an owned reference (see {@link DaqObject#DaqObject}). */
    public Dict(MemorySegment pointer) {
        super(pointer);
    }

    /**
     * Calls the openDAQ C function {&#64;code daqDict_createDict()}.
     */
    public Dict() {
        super(create$createDict());
    }

    private static MemorySegment create$createDict() {
        return DaqDict.createDict();
    }

    /**
     * Removes all elements from the list.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqDict_clear()}.
     */
    public void clear() {
        try {
            DaqDict.clear(requireLivePointer());
        } finally {
            Reference.reachabilityFence(this);
        }
    }

    /**
     * Deletes the element with the specified key.
     * &#64;param key The key of the element to delete.
     * If the client needs the element deleted, it should use `removeAt` method.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqDict_deleteItem()}.
     */
    public void deleteItem(Object key) {
        try {
            Box.Boxed key$b = Box.box(key);
            try {
                DaqDict.deleteItem(requireLivePointer(), key$b.segment());
            } finally { Box.releaseBoxed(key$b); }
        } finally {
            Reference.reachabilityFence(this);
        }
    }

    /**
     * Gets the element with the specified key.
     * &#64;param key The key of the element to get.
     * &#64;param[out] value The element with the specified key.
     * The reference count of the element that is retrieved is incremented. The client is
     * responsible for calling `releaseRef` when the element is no longer needed.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqDict_get()}.
     */
    public BaseObject get(Object key) {
        try {
            Box.Boxed key$b = Box.box(key);
            try {
                return DaqObject.wrap(DaqDict.get(requireLivePointer(), key$b.segment()), BaseObject.class);
            } finally { Box.releaseBoxed(key$b); }
        } finally {
            Reference.reachabilityFence(this);
        }
    }

    /**
     * Gets the number of elements contained in the dictionary.
     * &#64;param[out] size The number of elements contained in the dictionary.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqDict_getCount()}.
     */
    public long getCount() {
        try {
            return DaqDict.getCount(requireLivePointer());
        } finally {
            Reference.reachabilityFence(this);
        }
    }

    /**
     * Gets the list of all keys in the dictionary.
     * &#64;param[out] keys The list of the keys.
     * The order of the keys is not defined.
     * The client is responsible for calling `releaseRef` when the list is no longer needed.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqDict_getKeyList()}.
     */
    public java.util.List<Object> getKeyList() {
        try {
            return Wrap.list(DaqDict.getKeyList(requireLivePointer()), Wrap::unboxOwned);
        } finally {
            Reference.reachabilityFence(this);
        }
    }

    /**
     * Gets the iterable interface of the keys.
     * &#64;param[out] iterable The iterable interface of the keys.
     * The Iterable interface enables iteration through the keys.
     * The client is responsible for calling `releaseRef` when the interface is no longer needed.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqDict_getKeys()}.
     */
    public Iterable getKeys() {
        try {
            return DaqObject.wrap(DaqDict.getKeys(requireLivePointer()), Iterable.class);
        } finally {
            Reference.reachabilityFence(this);
        }
    }

    /**
     * Gets the list of all elements in the dictionary.
     * &#64;param[out] values The list of the elements.
     * The order of the elements is not defined.
     * The client is responsible for calling `releaseRef` when the list is no longer needed.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqDict_getValueList()}.
     */
    public java.util.List<Object> getValueList() {
        try {
            return Wrap.list(DaqDict.getValueList(requireLivePointer()), Wrap::unboxOwned);
        } finally {
            Reference.reachabilityFence(this);
        }
    }

    /**
     * Gets the iterable interface of the elements.
     * &#64;param[out] iterable The iterable interface of the elements.
     * The Iterable interface enables iteration through the elements.
     * The client is responsible for calling `releaseRef` when the interface is no longer needed.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqDict_getValues()}.
     */
    public Iterable getValues() {
        try {
            return DaqObject.wrap(DaqDict.getValues(requireLivePointer()), Iterable.class);
        } finally {
            Reference.reachabilityFence(this);
        }
    }

    /**
     * Checks if the element with the specified key exists in the dictionary.
     * &#64;param key The key of the element to check.
     * &#64;param[out] hasKey True if the element exists, False otherwise.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqDict_hasKey()}.
     */
    public boolean hasKey(Object key) {
        try {
            Box.Boxed key$b = Box.box(key);
            try {
                return DaqDict.hasKey(requireLivePointer(), key$b.segment());
            } finally { Box.releaseBoxed(key$b); }
        } finally {
            Reference.reachabilityFence(this);
        }
    }

    /**
     * Removes the element with the specified key.
     * &#64;param key The key of the element to remove.
     * &#64;param[out] value The element with the specified key.
     * The client is responsible for calling `releaseRef` when the element is no longer needed.
     * If the client does not need the element after it is removed, it should call `delete` method.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqDict_remove()}.
     */
    public BaseObject remove(Object key) {
        try {
            Box.Boxed key$b = Box.box(key);
            try {
                return DaqObject.wrap(DaqDict.remove(requireLivePointer(), key$b.segment()), BaseObject.class);
            } finally { Box.releaseBoxed(key$b); }
        } finally {
            Reference.reachabilityFence(this);
        }
    }

    /**
     * Sets the element with the specified key.
     * &#64;param key The key of the element to set.
     * &#64;param value The element with the specified key.
     * The reference count of the key and the element is incremented.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqDict_set()}.
     */
    public void set(Object key, Object value) {
        try {
            Box.Boxed key$b = Box.box(key);
            try {
                Box.Boxed value$b = Box.box(value);
                try {
                    DaqDict.set(requireLivePointer(), key$b.segment(), value$b.segment());
                } finally { Box.releaseBoxed(value$b); }
            } finally { Box.releaseBoxed(key$b); }
        } finally {
            Reference.reachabilityFence(this);
        }
    }

}
