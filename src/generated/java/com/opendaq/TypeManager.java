// This file is autogenerated by tools/generate_high_level_bindings.py. Do not edit.

package com.opendaq;

import java.lang.foreign.Arena;
import java.lang.foreign.MemorySegment;
import java.lang.foreign.ValueLayout;
import java.lang.ref.Reference;
import java.util.List;
import java.util.Map;

import com.opendaq.lowlevel.*;

/**
 * Container for Type objects. The Type manager is used when creating certain types of objects (eg. Structs and Property object classes). The Types stored within the manager contain pre-defined fields, as well as constraints specifying how objects should look.
 * The currently available types in openDAQ that should be added to the Type manager are the Struct type
 * and the Property object class. The former is used to validate Structs when they are created, while the latter
 * contains pre-defined properties that are added to Property objects on construction.
 * When adding a Property object class to the manager, they can form inheritance chains with one-another.
 * Thus, a parent of a given class must be added to the manager before any of its children. Likewise, a class
 * cannot be removed before its children are removed.
 *
 * &lt;p&gt;Wraps the openDAQ {&#64;code daqTypeManager} interface.
 */
public class TypeManager extends BaseObject {

    /** Adopt an owned reference (see {@link DaqObject#DaqObject}). */
    public TypeManager(MemorySegment pointer) {
        super(pointer);
    }

    /**
     * Calls the openDAQ C function {&#64;code daqTypeManager_createTypeManager()}.
     */
    public TypeManager() {
        super(create$createTypeManager());
    }

    private static MemorySegment create$createTypeManager() {
        return DaqTypeManager.createTypeManager();
    }

    /**
     * Adds a type to the manager.
     * &#64;param type The Type to be added.
     * &#64;retval OPENDAQ_ERR_ALREADYEXISTS if a type with the same name is already added.
     * &#64;retval OPENDAQ_ERR_INVALIDPARAMETER if either the type name is an empty string.
     * The type name must be unique and. If a Property object class specifies a parent class,
     * then the parent class must be added before it.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqTypeManager_addType()}.
     */
    public void addType(TypeObject type) {
        try {
            DaqTypeManager.addType(requireLivePointer(), (type == null ? MemorySegment.NULL : type.requireLivePointer()));
        } finally {
            Reference.reachabilityFence(this);
            Reference.reachabilityFence(type);
        }
    }

    /**
     * Gets an added Type by name.
     * &#64;param typeName The Type's name.
     * &#64;param[out] type The Type with name equal to `name`.
     * &#64;retval OPENDAQ_ERR_NOTFOUND if a Type with the specified name is not added.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqTypeManager_getType()}.
     */
    public TypeObject getType(String typeName) {
        try {
            MemorySegment typeName$s = Wrap.daqStringOrNull(typeName);
            try {
                return DaqObject.wrap(DaqTypeManager.getType(requireLivePointer(), typeName$s), TypeObject.class);
            } finally { Ffi.releaseRef(typeName$s); }
        } finally {
            Reference.reachabilityFence(this);
        }
    }

    /**
     * Gets a list of all added Types.
     * &#64;param[out] types The list of all added Types.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqTypeManager_getTypes()}.
     */
    public java.util.List<String> getTypes() {
        try {
            return Wrap.list(DaqTypeManager.getTypes(requireLivePointer()), p -> (String) Wrap.unboxOwned(p));
        } finally {
            Reference.reachabilityFence(this);
        }
    }

    /**
     * Checks if a type with the specified name is already added.
     * &#64;param typeName The name of the checked type.
     * &#64;param[out] hasType True if the type is aready added to the manager; False otherwise.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqTypeManager_hasType()}.
     */
    public boolean hasType(String typeName) {
        try {
            MemorySegment typeName$s = Wrap.daqStringOrNull(typeName);
            try {
                return DaqTypeManager.hasType(requireLivePointer(), typeName$s);
            } finally { Ffi.releaseRef(typeName$s); }
        } finally {
            Reference.reachabilityFence(this);
        }
    }

    /**
     * Removes the type from the manager.
     * &#64;param typeName The type's name.
     * &#64;retval OPENDAQ_ERR_NOTFOUND if the class is not registered.
     * The removed class must not be a parent of another added class. If it is, those classes must be removed
     * before it.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqTypeManager_removeType()}.
     */
    public void removeType(String typeName) {
        try {
            MemorySegment typeName$s = Wrap.daqStringOrNull(typeName);
            try {
                DaqTypeManager.removeType(requireLivePointer(), typeName$s);
            } finally { Ffi.releaseRef(typeName$s); }
        } finally {
            Reference.reachabilityFence(this);
        }
    }

}
