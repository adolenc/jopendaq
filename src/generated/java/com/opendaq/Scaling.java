// This file is autogenerated by tools/generate_high_level_bindings.py. Do not edit.

package com.opendaq;

import java.lang.foreign.Arena;
import java.lang.foreign.MemorySegment;
import java.lang.foreign.ValueLayout;
import java.lang.ref.Reference;
import java.util.List;
import java.util.Map;

import com.opendaq.lowlevel.*;

/**
 * Signal descriptor field that defines a scaling transformation, which should be applied to data carried by the signal's packets when read.
 * Each scaling specifies its `scalingType` and parses the parameters accordingly. The parameters
 * are to be interpreted and used as specified by each specific scaling type as detailed below.
 * Additionally, each Scaling object states is input and output data types. The inputDataType
 * describes the raw data type of the signal value (that data is input into the scaling scaling),
 * while the outputDataType should match the sample type of the signal's value descriptor.
 * Scaling objects implement the Struct methods internally and are Core type `ctStruct`.
 * &#64;subsection scaling_types Scaling types
 * &#64;subsubsection scaling_types_linear Linear scaling
 * Linear scaling parameters must have two entries:
 * - Scale: coefficient by which the input data is to be multiplied
 * - Offset: a constant that is added to the &lt;em&gt;scale * value&lt;/em&gt; multiplication result
 * The linear scaling output is calculated as follows: &lt;em&gt;inputValue * scale + offset&lt;/em&gt;
 *
 * &lt;p&gt;Wraps the openDAQ {&#64;code daqScaling} interface.
 */
public class Scaling extends BaseObject {

    /** Adopt an owned reference (see {@link DaqObject#DaqObject}). */
    public Scaling(MemorySegment pointer) {
        super(pointer);
    }

    /**
     * Creates a Scaling object with given input/output types, Scaling type and parameters.
     * &#64;param inputDataType The type of input data expected by the rule.
     * &#64;param outputDataType The data type output by the rule after calculation.
     * &#64;param scalingType The type of the scaling.
     * &#64;param parameters Tha parameters of the Dimension rule.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqScaling_createScaling()}.
     */
    public Scaling(SampleType inputDataType, ScaledSampleType outputDataType, ScalingType scalingType, java.util.Map<?, ?> parameters) {
        super(create$createScaling(inputDataType, outputDataType, scalingType, parameters));
    }

    private static MemorySegment create$createScaling(SampleType inputDataType, ScaledSampleType outputDataType, ScalingType scalingType, java.util.Map<?, ?> parameters) {
        MemorySegment parameters$d = (parameters == null ? MemorySegment.NULL : Box.boxDictOwned(parameters));
        try {
            return DaqScaling.createScaling(inputDataType, outputDataType, scalingType, parameters$d);
        } finally { Ffi.releaseRef(parameters$d); }
    }

    /**
     * Creates a Scaling object from Builder
     * &#64;param builder Scaling Builder
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqScaling_createScalingFromBuilder()}.
     */
    public Scaling(ScalingBuilder builder) {
        super(create$createScalingFromBuilder(builder));
    }

    private static MemorySegment create$createScalingFromBuilder(ScalingBuilder builder) {
        try {
            return DaqScaling.createScalingFromBuilder((builder == null ? MemorySegment.NULL : builder.requireLivePointer()));
        } finally {
            Reference.reachabilityFence(builder);
        }
    }

    /**
     * Creates a Scaling with a Linear scaling type configuration. The returned Scaling object is already frozen.
     * &#64;param scale Coefficient by which the input data is to be multiplied.
     * &#64;param offset Constant that is added to the &lt;em&gt;scale * value&lt;/em&gt; multiplication result.
     * &#64;param inputDataType The scaling's input data type.
     * &#64;param outputDataType The scaling's output data type.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqScaling_createLinearScaling()}.
     */
    public static Scaling createLinearScaling(Object scale, Object offset, SampleType inputDataType, ScaledSampleType outputDataType) {
        return DaqObject.wrap(create$createLinearScaling(scale, offset, inputDataType, outputDataType), Scaling.class);
    }

    private static MemorySegment create$createLinearScaling(Object scale, Object offset, SampleType inputDataType, ScaledSampleType outputDataType) {
        MemorySegment scale$n = Box.boxNumberOwned(scale);
        try {
            MemorySegment offset$n = Box.boxNumberOwned(offset);
            try {
                return DaqScaling.createLinearScaling(scale$n, offset$n, inputDataType, outputDataType);
            } finally { Ffi.releaseRef(offset$n); }
        } finally { Ffi.releaseRef(scale$n); }
    }

    /**
     * Gets the scaling's input data type.
     * &#64;param[out] type The input data type
     * The input data type corresponds to the raw values passed through the signal path in
     * data packets.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqScaling_getInputSampleType()}.
     */
    public SampleType getInputSampleType() {
        try {
            return DaqScaling.getInputSampleType(requireLivePointer());
        } finally {
            Reference.reachabilityFence(this);
        }
    }

    /**
     * Gets the scaling's output data type.
     * &#64;param[out] type The output data type
     * The output data type corresponds to the sample type specified in the value descriptor of
     * a signal, and is the type in which said signal's data should be read in after having
     * the scaling applied to it.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqScaling_getOutputSampleType()}.
     */
    public ScaledSampleType getOutputSampleType() {
        try {
            return DaqScaling.getOutputSampleType(requireLivePointer());
        } finally {
            Reference.reachabilityFence(this);
        }
    }

    /**
     * Gets the dictionary of parameters that are used to calculate the scaling in conjunction with the input data.
     * &#64;param[out] parameters The dictionary of parameters.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqScaling_getParameters()}.
     */
    public java.util.Map<String, Object> getParameters() {
        try {
            return Wrap.map(DaqScaling.getParameters(requireLivePointer()), p -> (String) Wrap.unboxOwned(p), Wrap::unboxOwned);
        } finally {
            Reference.reachabilityFence(this);
        }
    }

    /**
     * Gets the type of the scaling that determines how the scaling parameters should be interpreted and how the scaling should be calculated.
     * &#64;param[out] type The type of the scaling.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqScaling_getType()}.
     */
    public ScalingType getType() {
        try {
            return DaqScaling.getType(requireLivePointer());
        } finally {
            Reference.reachabilityFence(this);
        }
    }

}
