// This file is autogenerated by tools/generate_high_level_bindings.py. Do not edit.

package com.opendaq;

import java.lang.foreign.Arena;
import java.lang.foreign.MemorySegment;
import java.lang.foreign.ValueLayout;
import java.lang.ref.Reference;
import java.util.List;
import java.util.Map;

import com.opendaq.lowlevel.*;

/**
 * Provides an interface to manipulate the configuration of a device's (server's) network interface. Offers methods to update the IP configuration and retrieve the currently active one, if the corresponding feature supported by the device. Additionally, includes a helper method to create a prebuilt property object with valid default configuration.
 * The configuration property object, passed as a parameter to said methods, should include the following properties:
 * - **"dhcp4"**: A boolean property indicating whether DHCP is enabled for IPv4. Defaults to `True` (DHCP enabled).
 * If set to `False` (DHCP disabled), non-empty static configuration properties are required.
 * - **"address4"**: A string property specifying the statically assigned IPv4 address in the format `address/netmask` (e.g. 192.168.1.2/24).
 * This property is ignored when DHCP is enabled for IPv4. However, if DHCP is disabled, the list must include at least one address. Defaults to an empty string.
 * - **"gateway4"**: A string property specifying the IPv4 gateway address. This is required if DHCP is disabled and ignored otherwise. Defaults to an empty string.
 * - **"dhcp6"**, **"address6"**, **"gateway6"**: These properties follow the same format and rules as their IPv4 counterparts but apply to IPv6 configuration.
 *
 * &lt;p&gt;Wraps the openDAQ {&#64;code daqNetworkInterface} interface.
 */
public class NetworkInterface extends BaseObject {

    /** Adopt an owned reference (see {@link DaqObject#DaqObject}). */
    public NetworkInterface(MemorySegment pointer) {
        super(pointer);
    }

    /**
     * Calls the openDAQ C function {&#64;code daqNetworkInterface_createNetworkInterface()}.
     */
    public NetworkInterface(String name, String ownerDeviceManufacturerName, String ownerDeviceSerialNumber, Object moduleManager) {
        super(create$createNetworkInterface(name, ownerDeviceManufacturerName, ownerDeviceSerialNumber, moduleManager));
    }

    private static MemorySegment create$createNetworkInterface(String name, String ownerDeviceManufacturerName, String ownerDeviceSerialNumber, Object moduleManager) {
        MemorySegment name$s = Wrap.daqStringOrNull(name);
        try {
            MemorySegment ownerDeviceManufacturerName$s = Wrap.daqStringOrNull(ownerDeviceManufacturerName);
            try {
                MemorySegment ownerDeviceSerialNumber$s = Wrap.daqStringOrNull(ownerDeviceSerialNumber);
                try {
                    Box.Boxed moduleManager$b = Box.box(moduleManager);
                    try {
                        return DaqNetworkInterface.createNetworkInterface(name$s, ownerDeviceManufacturerName$s, ownerDeviceSerialNumber$s, moduleManager$b.segment());
                    } finally { Box.releaseBoxed(moduleManager$b); }
                } finally { Ffi.releaseRef(ownerDeviceSerialNumber$s); }
            } finally { Ffi.releaseRef(ownerDeviceManufacturerName$s); }
        } finally { Ffi.releaseRef(name$s); }
    }

    /**
     * Creates a property object containing default configuration values for a network interface.
     * &#64;param[out] defaultConfig The configuration object containing default settings for configuring device's network interface.
     * The created object can be modified or directly submitted using the `submitConfiguration` method.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqNetworkInterface_createDefaultConfiguration()}.
     */
    public PropertyObject createDefaultConfiguration() {
        try {
            return DaqObject.wrap(DaqNetworkInterface.createDefaultConfiguration(requireLivePointer()), PropertyObject.class);
        } finally {
            Reference.reachabilityFence(this);
        }
    }

    /**
     * Requests the currently active configuration for the network interface.
     * &#64;param[out] config The property object containing the currently active configuration.
     * &#64;retval OPENDAQ_ERR_NOTIMPLEMENTED if the device doesn't support retrieving the active configuration.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqNetworkInterface_requestCurrentConfiguration()}.
     */
    public PropertyObject requestCurrentConfiguration() {
        try {
            return DaqObject.wrap(DaqNetworkInterface.requestCurrentConfiguration(requireLivePointer()), PropertyObject.class);
        } finally {
            Reference.reachabilityFence(this);
        }
    }

    /**
     * Submits a new configuration for the network interface.
     * &#64;param config The new configuration to apply.
     * &#64;retval OPENDAQ_SUCCESS if the new configuration is applied successfully; otherwise, returns an informative error code.
     * The provided configuration must adhere to the required properties, including "dhcp4", "address4", "gateway4", and their IPv6 equivalents,
     * as described in the class-level documentation.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqNetworkInterface_submitConfiguration()}.
     */
    public void submitConfiguration(PropertyObject config) {
        try {
            DaqNetworkInterface.submitConfiguration(requireLivePointer(), (config == null ? MemorySegment.NULL : config.requireLivePointer()));
        } finally {
            Reference.reachabilityFence(this);
            Reference.reachabilityFence(config);
        }
    }

}
