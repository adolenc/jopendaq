// This file is autogenerated by tools/generate_high_level_bindings.py. Do not edit.

package com.opendaq;

import java.lang.foreign.Arena;
import java.lang.foreign.MemorySegment;
import java.lang.foreign.ValueLayout;
import java.lang.ref.Reference;
import java.util.List;
import java.util.Map;

import com.opendaq.lowlevel.*;

/**
 * Describes a measurement unit with IDs as defined in &lt;a href="https://unece.org/trade/cefact/UNLOCODE-Download"&gt;Codes for Units of Measurement used in International Trade&lt;/a&gt;.
 * Unit objects implement the Struct methods internally and are Core type `ctStruct`.
 * &#64;subsection unit_components Unit components
 * - ID: The unit ID as defined in &lt;a href="https://unece.org/trade/cefact/UNLOCODE-Download"&gt;Codes for Units of Measurement used in International Trade&lt;/a&gt;.
 * Should be set to -1 if the unit ID is not available.
 * - Symbol: The symbol of the unit, i.e. "m/s". It is mandatory and must be set before the Unit object is frozen.
 * - Name: The full name of the unit, i.e. "meters per second". The unit's name is optional.
 * - Quantity: The quantity represented by the unit, i.e. "Velocity". The unit's quantity is optional.
 *
 * &lt;p&gt;Wraps the openDAQ {&#64;code daqUnit} interface.
 */
public class Unit extends BaseObject {

    /** Adopt an owned reference (see {@link DaqObject#DaqObject}). */
    public Unit(MemorySegment pointer) {
        super(pointer);
    }

    /**
     * Creates a Unit struct with its `id`, `symbol`, `name`, and `quantity` fields configured.
     * &#64;param id The unit ID as defined in &lt;a href="https://unece.org/trade/cefact/UNLOCODE-Download"&gt;Codes for Units of Measurement used in International Trade&lt;/a&gt;.
     * &#64;param symbol The symbol of the unit, i.e. "m/s".
     * &#64;param name The full name of the unit, i.e. "meters per second". (optional)
     * &#64;param quantity The quantity represented by the unit, i.e. "Velocity". (optional)
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqUnit_createUnit()}.
     */
    public Unit(long id, String symbol, String name, String quantity) {
        super(create$createUnit(id, symbol, name, quantity));
    }

    private static MemorySegment create$createUnit(long id, String symbol, String name, String quantity) {
        MemorySegment symbol$s = Wrap.daqStringOrNull(symbol);
        try {
            MemorySegment name$s = Wrap.daqStringOrNull(name);
            try {
                MemorySegment quantity$s = Wrap.daqStringOrNull(quantity);
                try {
                    return DaqUnit.createUnit(id, symbol$s, name$s, quantity$s);
                } finally { Ffi.releaseRef(quantity$s); }
            } finally { Ffi.releaseRef(name$s); }
        } finally { Ffi.releaseRef(symbol$s); }
    }

    /**
     * Gets the unit ID as defined in &lt;a href="https://unece.org/trade/cefact/UNLOCODE-Download"&gt;Codes for Units of Measurement used in International Trade&lt;/a&gt;.
     * &#64;param[out] id The unit ID.
     * Returns -1 if the unit id is not available.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqUnit_getId()}.
     */
    public long getId() {
        try {
            return DaqUnit.getId(requireLivePointer());
        } finally {
            Reference.reachabilityFence(this);
        }
    }

    /**
     * Gets the full name of the unit, i.e. "meters per second".
     * &#64;param[out] name The unit's full name.
     * `nullptr` if not set.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqUnit_getName()}.
     */
    public String getName() {
        try {
            return Ffi.readDaqStringAndRelease(DaqUnit.getName(requireLivePointer()));
        } finally {
            Reference.reachabilityFence(this);
        }
    }

    /**
     * Gets the quantity represented by the unit, i.e. "Velocity"
     * &#64;param[out] quantity The unit's quantity.
     * `nullptr` if not set.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqUnit_getQuantity()}.
     */
    public String getQuantity() {
        try {
            return Ffi.readDaqStringAndRelease(DaqUnit.getQuantity(requireLivePointer()));
        } finally {
            Reference.reachabilityFence(this);
        }
    }

    /**
     * Gets the symbol of the unit, i.e. "m/s".
     * &#64;param[out] symbol The unit's symbol.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqUnit_getSymbol()}.
     */
    public String getSymbol() {
        try {
            return Ffi.readDaqStringAndRelease(DaqUnit.getSymbol(requireLivePointer()));
        } finally {
            Reference.reachabilityFence(this);
        }
    }

}
