// This file is autogenerated by tools/generate_high_level_bindings.py. Do not edit.

package com.opendaq;

import java.lang.foreign.Arena;
import java.lang.foreign.MemorySegment;
import java.lang.foreign.ValueLayout;
import java.lang.ref.Reference;
import java.util.List;
import java.util.Map;

import com.opendaq.lowlevel.*;

/**
 * Rule that defines the labels (alternatively called bins, ticks) of a dimension.
 * Each dimension has a rule, which is queried and parsed by the dimension `getLabels` and `getSize` calls.
 * Three different rule types are supported by openDAQ, with all others having to be set to `custom` type, requiring
 * anyone using them to parse them manually.
 * Dimension rule objects implement the Struct methods internally and are Core type `ctStruct`.
 * &#64;subsection dimension_rule_types Rule types
 * &#64;subsubsection linear_dimension_rule Linear dimension rule
 * The parameters include a `delta`, `start`, and `size` number members. The list of labels of size `size` is generated by
 * the equation: &lt;em&gt;index * delta + start&lt;/em&gt;, where the index starts with 0 and goes up to `size - 1`.
 * In example: `delta = 10, start = 5, size = 5` produces the following list of samples -&gt; [5, 15, 25, 35, 45]
 * &#64;subsubsection logarithmic_dimension_rule Logarithmic dimension rule
 * The parameters include a `delta`, `start`, `base` and `size` number members. The list of labels of size `size` is generated by
 * the equation: &lt;em&gt;base ^ (index * delta + start)&lt;/em&gt;, where the index starts with 0 and goes up to `size - 1`.
 * In example: `delta = 1, start = -2, base = 10 size = 5` produces the following list of samples -&gt; [0.01, 0.1, 1, 10, 20]
 * &#64;subsubsection list_dimension_rule List dimension rule
 * The parameters include a `list` list-typed member. The `list` contains labels, implicitly also defining the dimension size.
 * The labels in the list can be either strings, numbers, or ranges.
 * String example list: `list` = ["banana", "apple", "coconut"]
 * Number example list: = [1.2, 10.5, 20.2, 50.7]
 * Range example list: [1-10, 10-20, 20-30, 30-40, 40-50]
 *
 * &lt;p&gt;Wraps the openDAQ {&#64;code daqDimensionRule} interface.
 */
public class DimensionRule extends BaseObject {

    /** Adopt an owned reference (see {@link DaqObject#DaqObject}). */
    public DimensionRule(MemorySegment pointer) {
        super(pointer);
    }

    /**
     * Creates a Rule with a given type and parameters.
     * &#64;param type The type of the Dimension rule
     * &#64;param parameters Tha parameters of the Dimension rule
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqDimensionRule_createDimensionRule()}.
     */
    public DimensionRule(DimensionRuleType type, java.util.Map<?, ?> parameters) {
        super(create$createDimensionRule(type, parameters));
    }

    private static MemorySegment create$createDimensionRule(DimensionRuleType type, java.util.Map<?, ?> parameters) {
        MemorySegment parameters$d = (parameters == null ? MemorySegment.NULL : Box.boxDictOwned(parameters));
        try {
            return DaqDimensionRule.createDimensionRule(type, parameters$d);
        } finally { Ffi.releaseRef(parameters$d); }
    }

    /**
     * Creates a DimensionRule using Builder
     * &#64;param builder DimensionRule Builder
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqDimensionRule_createDimensionRuleFromBuilder()}.
     */
    public DimensionRule(DimensionRuleBuilder builder) {
        super(create$createDimensionRuleFromBuilder(builder));
    }

    private static MemorySegment create$createDimensionRuleFromBuilder(DimensionRuleBuilder builder) {
        try {
            return DaqDimensionRule.createDimensionRuleFromBuilder((builder == null ? MemorySegment.NULL : builder.requireLivePointer()));
        } finally {
            Reference.reachabilityFence(builder);
        }
    }

    /**
     * Creates a Rule with a Linear rule type configuration.
     * &#64;param delta Coefficient by which the input data is to be multiplied.
     * &#64;param start Constant that is added to the &lt;em&gt;scale * value&lt;/em&gt; multiplication result.
     * &#64;param size The size of the dimension described by the rule
     * The scale and offset are stored within the `parameters` member of the Rule object
     * with the scale being at the first position of the list, and the offset at the second.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqDimensionRule_createLinearDimensionRule()}.
     */
    public static DimensionRule createLinearDimensionRule(Object delta, Object start, long size) {
        return DaqObject.wrap(create$createLinearDimensionRule(delta, start, size), DimensionRule.class);
    }

    private static MemorySegment create$createLinearDimensionRule(Object delta, Object start, long size) {
        MemorySegment delta$n = Box.boxNumberOwned(delta);
        try {
            MemorySegment start$n = Box.boxNumberOwned(start);
            try {
                return DaqDimensionRule.createLinearDimensionRule(delta$n, start$n, size);
            } finally { Ffi.releaseRef(start$n); }
        } finally { Ffi.releaseRef(delta$n); }
    }

    /**
     * Creates a Rule with a List rule type configuration.
     * &#64;param list The list of dimension labels.
     * The list is stored within the `parameters` member of the Rule object.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqDimensionRule_createListDimensionRule()}.
     */
    public static DimensionRule createListDimensionRule(java.util.List<?> list) {
        return DaqObject.wrap(create$createListDimensionRule(list), DimensionRule.class);
    }

    private static MemorySegment create$createListDimensionRule(java.util.List<?> list) {
        MemorySegment list$l = (list == null ? MemorySegment.NULL : Box.boxListOwned(list));
        try {
            return DaqDimensionRule.createListDimensionRule(list$l);
        } finally { Ffi.releaseRef(list$l); }
    }

    /**
     * Creates a Rule with a Logarithmic rule type configuration.
     * &#64;param delta Coefficient by which the input data is to be multiplied.
     * &#64;param start Constant that is added to the &lt;em&gt;scale * value&lt;/em&gt; multiplication result.
     * &#64;param base The base of the logarithm.
     * &#64;param size The size of the dimension described by the rule.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqDimensionRule_createLogarithmicDimensionRule()}.
     */
    public static DimensionRule createLogarithmicDimensionRule(Object delta, Object start, Object base, long size) {
        return DaqObject.wrap(create$createLogarithmicDimensionRule(delta, start, base, size), DimensionRule.class);
    }

    private static MemorySegment create$createLogarithmicDimensionRule(Object delta, Object start, Object base, long size) {
        MemorySegment delta$n = Box.boxNumberOwned(delta);
        try {
            MemorySegment start$n = Box.boxNumberOwned(start);
            try {
                MemorySegment base$n = Box.boxNumberOwned(base);
                try {
                    return DaqDimensionRule.createLogarithmicDimensionRule(delta$n, start$n, base$n, size);
                } finally { Ffi.releaseRef(base$n); }
            } finally { Ffi.releaseRef(start$n); }
        } finally { Ffi.releaseRef(delta$n); }
    }

    /**
     * Gets a dictionary of string-object key-value pairs representing the parameters used to evaluate the rule.
     * &#64;param[out] parameters The dictionary containing the rule parameter members.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqDimensionRule_getParameters()}.
     */
    public java.util.Map<String, Object> getParameters() {
        try {
            return Wrap.map(DaqDimensionRule.getParameters(requireLivePointer()), p -> (String) Wrap.unboxOwned(p), Wrap::unboxOwned);
        } finally {
            Reference.reachabilityFence(this);
        }
    }

    /**
     * Gets the type of the dimension rule.
     * &#64;param[out] type The type of the dimension rule.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqDimensionRule_getType()}.
     */
    public DimensionRuleType getType() {
        try {
            return DaqDimensionRule.getType(requireLivePointer());
        } finally {
            Reference.reachabilityFence(this);
        }
    }

}
