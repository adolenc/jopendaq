// This file is autogenerated by tools/generate_high_level_bindings.py. Do not edit.

package com.opendaq;

import java.lang.foreign.Arena;
import java.lang.foreign.MemorySegment;
import java.lang.foreign.ValueLayout;
import java.lang.ref.Reference;
import java.util.List;
import java.util.Map;

import com.opendaq.lowlevel.*;

/**
 * Represents string variable as `IString` interface. Use this interface to wrap string
 * variable when you need to add the variable to lists, dictionaries and other containers which
 * accept `IBaseObject` and derived interfaces.
 * Available factories:
 * &#64;code
 * // Creates a new String object. Throws exception if not successful.
 * IString* String_Create(ConstCharPtr value)
 * // Creates a new String object with predefined length. Returns error code if not successful.
 * ErrCode createString(IString** obj, ConstCharPtr value)
 * // Creates a new String object with predefined length. Throws exception if not successful.
 * IString* StringN_Create(ConstCharPtr data, SizeT length)
 * // Creates a new String object. Returns error code if not successful.
 * ErrCode createStringN(IString** obj, ConstCharPtr data, SizeT length)
 * &#64;endcode
 *
 * &lt;p&gt;Wraps the openDAQ {&#64;code daqString} interface.
 */
public class StringObject extends BaseObject {

    /** Adopt an owned reference (see {@link DaqObject#DaqObject}). */
    public StringObject(MemorySegment pointer) {
        super(pointer);
    }

    /**
     * Calls the openDAQ C function {&#64;code daqString_createString()}.
     */
    public StringObject(String str) {
        super(create$createString(str));
    }

    private static MemorySegment create$createString(String str) {
        try (Arena arena = Arena.ofConfined()) {
            return DaqString.createString((str == null ? MemorySegment.NULL : arena.allocateFrom(str)));
        }
    }

    /**
     * Calls the openDAQ C function {&#64;code daqString_createStringN()}.
     */
    public static StringObject createStringN(String str, long length) {
        return DaqObject.wrap(create$createStringN(str, length), StringObject.class);
    }

    private static MemorySegment create$createStringN(String str, long length) {
        try (Arena arena = Arena.ofConfined()) {
            return DaqString.createStringN((str == null ? MemorySegment.NULL : arena.allocateFrom(str)), length);
        }
    }

    /**
     * Gets a string value stored in the object.
     * &#64;param[out] value Stored string value.
     * &#64;return OPENDAQ_SUCCESS if succeeded, error code otherwise.
     * Call this method to extract the string value that is stored in the object. Method extracts the
     * value as a pointer to 8-bit char type.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqString_getCharPtr()}.
     */
    public MemorySegment getCharPtr() {
        try {
            return DaqString.getCharPtr(requireLivePointer());
        } finally {
            Reference.reachabilityFence(this);
        }
    }

    /**
     * Gets length of string.
     * &#64;param[out] size The size of the string.
     * &#64;return OPENDAQ_SUCCESS if succeeded, error code otherwise.
     * Call this method to get the length of the string. Null char terminator is not included in
     * the size of the string.
     *
     * &lt;p&gt;Calls the openDAQ C function {&#64;code daqString_getLength()}.
     */
    public long getLength() {
        try {
            return DaqString.getLength(requireLivePointer());
        } finally {
            Reference.reachabilityFence(this);
        }
    }

}
